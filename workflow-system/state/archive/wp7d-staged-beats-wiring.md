# Feature: WP7d — Staged-beats wiring (bookends + graduation)

**Workflow:** feature
**State:** COMPLETED 2026-07-22 — commit ae733ab on main, not pushed
**Created:** 2026-07-22
**drive_mode:** autopilot

## Problem Statement

WP7b/WP7c shipped the `tutorial-*` onboarding family with two beats left **forward-declared** rather than written: the **handoff → restore emotional-peak bookend** (§3 step 7, both arms) and the **drive-modes graduation reveal + named-at-close pointers** (§3 step 8, both arms). Both arm skills carry explicit `> **WP7d wiring touchpoint (forward-declared).**` blocks at those two positions saying "This arm marks the beat + position; WP7d writes the scene copy." WP7d's job is to **replace those forward-declaration blocks with the actual scene-by-scene choreography + per-beat pre-framing copy** (spec §3 bookend/graduation, §6 per-beat pre-framing rules, §7 staged-set discipline) — grounded in the real `/session-handoff` → `/session-restore` mechanics (handoff writes `.session.md` + a state-file marker; restore reads the pointer, restores context, offers a mode menu, strips the marker, deletes `.session.md`). WP7d **also** consumes 2 backlogged WP7b MINORs that its authors explicitly deferred here (dispatcher "control does not return" over-statement; greenfield product-entry unbounded vs. spec's "light taste") plus 2 low WP7a spec MINORs addressable at the same authoring pass (the split-greenfield-grounding one-clause pointer; the §3 legend disposition-token cross-pointer). No-runtime, prose-only, edits existing files only — **no new skill dir, no transition, no `transitions.md`/three-places sync.** Verify every consumed finding against the real text first (review-finding-actions-are-hypotheses).

## Work Tree

- [x] Phase 1: Wire the greenfield arm's two staged beats + consume its deferred findings  <!-- status: [x] — all impl + verify leaves complete -->>
  **Observable outcomes:** (prose-only skill-content feature — no runtime surface; outcomes are grep/CLI smoke-tests against the shipped skill file + the structural check)
  - CLI: `grep -c 'WP7d wiring touchpoint (forward-declared)' skills/tutorial-greenfield-workflow-tour/SKILL.md` → `0` (both forward-declaration blocks replaced with real copy)
  - CLI: Step 7 of `skills/tutorial-greenfield-workflow-tour/SKILL.md` now names the concrete bookend choreography — `grep -F '/session-handoff' skills/tutorial-greenfield-workflow-tour/SKILL.md && grep -F '/session-restore' skills/tutorial-greenfield-workflow-tour/SKILL.md` both exit 0, and the step contains explicit "enact the leave" + "context survives / from the file" narration (manual read-confirm)
  - CLI: Step 8 drive-modes reveal keeps the un-pushed framing — `grep -Fq 'not recommended yet' skills/tutorial-greenfield-workflow-tour/SKILL.md` exits 0 AND `grep -Eiq '(quick|only).{0,3}5.?min|5-minute' skills/tutorial-greenfield-workflow-tour/SKILL.md` exits **1** (no "5-min" claim introduced — §6 honest-framing invariant preserved)
  - CLI: Step 2 now bounds the product entry to a "light taste" — `grep -Eiq 'light taste|then pivot|keep .* light' skills/tutorial-greenfield-workflow-tour/SKILL.md` exits 0 (consumes `SURFACE-2026-07-22-QUALITY-GREENFIELD-PRODUCT-ENTRY-UNBOUNDED`)
  - CLI: no bare `.claude/` introduced — `./tests/check-structure.sh` Phase 12 passes (path-qualification mandate; run full check at verify-auto)
  - [x] P1.1 Replace the Step 7 (handoff→restore bookend) forward-declaration block in `skills/tutorial-greenfield-workflow-tour/SKILL.md` with real scene-by-scene choreography: run `/session-handoff` (writes `.session.md` + state-file marker), enact "leave" (agent narrates the user stepping away — no real process exit needed), `/session-restore` (reads pointer → restores context → offers mode menu → strips marker → deletes `.session.md`), with per-beat pre-framing copy per §6. Ground the narration in the real skill mechanics (the file the user opened in Step 3 IS the surviving state).  <!-- status: [x] — three-scene choreography (pre-frame+handoff / enact-leave / restore+reveal), grounded in .session.md + WIP-file mechanics, ties payoff back to beat A -->
  - [x] P1.2 Replace the Step 8 (drive-modes graduation + named-at-close) forward-declaration block with real copy: drive-modes reveal LAST + explicitly un-pushed ("not recommended yet"), then the named-at-close pointers (full Hierarchy, Reflect/Capture-learns-you) — NAMED, never staged, per §7. No "5-min" claim.  <!-- status: [x] — reveal→un-push→named-close; "Not recommended yet." present (L191); no false 5-min claim (only honest ~10–15 min + the §6 prohibition) -->
  - [x] P1.3 Consume `SURFACE-2026-07-22-QUALITY-GREENFIELD-PRODUCT-ENTRY-UNBOUNDED`: add an explicit "keep the product entry to a light taste, then pivot to the small unit" bound to Step 2 (verify against real Step-2 text + spec §3 row 2 first — the finding is a hypothesis).  <!-- status: [x] — verified finding real (Step 2 was unbounded); added "Keep it a light taste, then pivot" para + overshoot guard -->
  - [x] verify-auto  <!-- status: [x] — check-structure.sh 472/0 (path-qual Phase 12 held); all 6 Phase-1 grep observables pass (corrected checks: case-insensitive un-pushed framing; honest-framing+prohibition present, no affirmative 5-min promise) -->
  - [x] verify-self  <!-- status: [x] — No integration boundary (within-skill prose only). feature-verify-self-runner subagent independently read the file + spec: all 5 outcomes PASS (coherence read, not just grep) — forward-decls gone, Step 7 3-scene handoff→restore choreography mechanics-faithful + ties to beat A, Step 8 reveal LAST+un-pushed+named-close, honest ~10–15 min framing + prohibition both present no affirmative 5-min promise, Step 2 light-taste bound consumes the finding. 0 BLOCKING / 0 COSMETIC. -->
  - [x] verify-human  <!-- status: [x] — AUTO-SKIP (F11): all 4 gates clean (autopilot; verify-self all-PASS; no integration boundary — within-skill prose only; no outcome cites a consuming surface — /session-handoff+/session-restore are tour CONTENT not modified surfaces). Affirmation block printed as operator read-time veto; broader hands-on acceptance tracked at SURFACE-2026-07-22-WP7C-OPERATOR-HANDS-ON-ACCEPTANCE-DEFERRED. -->
  - [x] verify-codify  <!-- status: [x] — lightweight (WP7e owns full scenarios+pins, deferred by design). 11/11 codified regression assertions PASS + committed check-structure.sh 472/0. 2 FAILs triaged as check-authoring bugs (obsolete/incorrect test, high-confidence) — fixed the checks, no SKILL.md prose changed (see ## Test Triage). More phases remain → F15. -->

- [x] Phase 2: Wire the brownfield arm's two staged beats + consume the dispatcher finding + spec MINORs  <!-- status: [x] — all impl + verify leaves complete -->
  **Observable outcomes:** (prose-only — grep/CLI smoke-tests)
  - CLI: `grep -c 'WP7d wiring touchpoint (forward-declared)' skills/tutorial-brownfield-workflow-tour/SKILL.md` → `0` (both forward-declaration blocks replaced)
  - CLI: Step 7 brownfield bookend names the real choreography on the real repo — `grep -F '/session-handoff' skills/tutorial-brownfield-workflow-tour/SKILL.md && grep -F '/session-restore' skills/tutorial-brownfield-workflow-tour/SKILL.md` both exit 0, narration ties the surviving state to the revised `CLAUDE.md` opened in Step 4 (manual read-confirm)
  - CLI: Step 8 keeps un-pushed framing + no "5-min" claim — `grep -Fq 'not recommended yet' skills/tutorial-brownfield-workflow-tour/SKILL.md` exits 0 AND `grep -Eiq '(quick|only).{0,3}5.?min|5-minute' skills/tutorial-brownfield-workflow-tour/SKILL.md` exits **1**
  - CLI: dispatcher control-return finding consumed — `skills/tutorial-getting-started/SKILL.md` Step 3 no longer reads as "control does not return" without qualification; `grep -Fq "run" skills/tutorial-getting-started/SKILL.md` and a manual read confirm the half-sentence clarifier ("the arm runs to close; you don't resume the dispatcher afterward") is present (consumes `SURFACE-2026-07-22-QUALITY-DISPATCHER-CONTROL-RETURN-PHRASING`)
  - CLI: spec MINORs consumed — `workflow-system/product/onboarding-flow-spec.md` §3 greenfield step 2 carries the "probe-first is a natural BEAT; only verify-self is STAGED" one-clause pointer AND §3 legend carries a "see §7 for disposition tokens" cross-pointer (manual read-confirm; consumes `SURFACE-2026-07-22-QUALITY-SPLIT-GREENFIELD-GROUNDING` + `SURFACE-2026-07-22-QUALITY-SECTION3-LEGEND-NO-DISPOSITION-TOKENS`)
  - CLI: `./tests/check-structure.sh` passes clean (no regression; path-qualification held)
  - [x] P2.1 Replace the Step 7 (handoff→restore bookend) forward-declaration block in `skills/tutorial-brownfield-workflow-tour/SKILL.md` with real choreography on the real repo, tying the surviving state to the revised `CLAUDE.md` (Step 4) — parallel shape to greenfield P1.1 but brownfield-flavored (real repo, not sample), per-beat pre-framing per §6.  <!-- status: [x] — 3-scene choreography on the real repo; mechanics-faithful (.session.md + WIP marker); payoff ties to the durable context file opened in Step 4 + answers the brownfield pain (drift/forgetting across sessions) -->
  - [x] P2.2 Replace the Step 8 (drive-modes graduation + named-at-close) forward-declaration block: reveal LAST + un-pushed, then NAMED close (Hierarchy CUT-on-brownfield, Reflect/Capture). No "5-min" claim.  <!-- status: [x] — reveal LAST + un-pushed ("Not recommended yet."), no-live-demo stated, NAMED close with Hierarchy explicitly CUT-on-brownfield + Reflect/Capture; no affirmative 5-min promise -->
  - [x] P2.3 Consume `SURFACE-2026-07-22-QUALITY-DISPATCHER-CONTROL-RETURN-PHRASING`: in `skills/tutorial-getting-started/SKILL.md` Step 3 (+ the `## Category` bullet if it repeats the claim), add a half-sentence so the divergence semantics read unambiguously without over-stating one-shot Skill-tool mechanics ("the arm runs to close; you don't resume the dispatcher afterward"). Verify against real Step-3 text first.  <!-- status: [x] — verified finding real (both Category L23 + Step 3 carried the over-stated claim); replaced "control does not return" with divergence-semantics phrasing in BOTH spots (arm runs to close; you don't resume this dispatcher) -->
  - [x] P2.4 Consume the 2 low WP7a spec MINORs at the same authoring pass: (a) `SURFACE-2026-07-22-QUALITY-SPLIT-GREENFIELD-GROUNDING` — add the one-clause pointer at spec §3 greenfield step 2; (b) `SURFACE-2026-07-22-QUALITY-SECTION3-LEGEND-NO-DISPOSITION-TOKENS` — add the "see §7 for disposition tokens" cross-pointer at §3 legend. Both verified against the real doc text first.  <!-- status: [x] — both verified real; §3 legend now defines/points-to the STAGED/BEAT/FRAME/NAMED/CUT tokens (§7); greenfield step 2 grounding row flags probe-first as BEAT/NAMED not staged, only verify-self step 5 is STAGED -->
  - [x] verify-auto  <!-- status: [x] — path-qualification CLEAN on all 3 edited files (no bare .claude/); check-structure.sh 472/0; 15/15 Phase-2 grep observables pass (corrected checks) -->
  - [x] verify-self  <!-- status: [x] — No integration boundary (within-skill prose + a spec doc; nothing consumes them). feature-verify-self-runner subagent independently read all 3 files + spec: all 6 outcomes PASS (coherence + mechanics-faithfulness read) — brownfield forward-decls gone, Step 7 3-scene bookend ties to Step-4 CLAUDE.md + brownfield pain, Step 8 reveal LAST+un-pushed+Hierarchy-CUT+named-close, honest framing preserved, dispatcher divergence-semantics fixed in BOTH Category + Step 3, both spec MINORs consumed correctly. 0 BLOCKING / 0 COSMETIC. -->
  - [x] verify-human  <!-- status: [x] — AUTO-SKIP (F11): all 4 gates clean (autopilot; verify-self all-PASS; no integration boundary — 2 tutorial skills + 1 spec doc, nothing consumes them; no outcome cites a consuming surface). Affirmation block printed as operator read-time veto; broader hands-on acceptance at SURFACE-2026-07-22-WP7C-OPERATOR-HANDS-ON-ACCEPTANCE-DEFERRED. -->
  - [x] verify-codify  <!-- status: [x] — lightweight (WP7e owns full scenarios+pins). 14/14 codified regression assertions PASS + committed check-structure.sh 472/0. 1 FAIL triaged as check-string line-wrap artifact (obsolete/incorrect test, high-confidence) — fixed the check, no SKILL.md prose changed (see ## Test Triage). All 3 review findings confirmed consumed in real prose. All phases complete → F16 to ship. -->

## Current Node
- **Path:** Feature > finalize
- **Active scope:** ship (ae733ab) + review-quality complete (0C/0MAJ/2 MINOR auto-backlogged, F39); ready for /feature-finalize
- **Blocked:** none
- **Unvisited:** none — both phases complete, shipped, reviewed. Next: finalize.
- **Open discoveries:** none
- **Note for verify-codify:** the "un-pushed framing present" observable must be **case-insensitive** (copy uses `**Not recommended yet.**` with capital N); the "no 5-min claim" observable must NOT use a naive `grep 5.?min` — it will false-positive on the honest `~10–15 min` framing and on the §6 prohibition line. Assert honest-framing PRESENT + prohibition PRESENT, per the WP7e forward pin-spec.

## Discoveries
<!-- Format: [SURFACED-<date>] <target node> — <summary>
     Each entry is also logged to workflow-system/state/backlog.md -->

[NOTE-2026-07-22 for WP7e] Grep-pin authoring gotcha, hit 3× in WP7d codify (not a SURFACE — a note-to-self for WP7e's structural pins, which will target this same prose): SKILL.md prose is hard-wrapped at ~99 cols AND uses `**bold**` inline, so naive single-line `grep -F '<multi-word span>'` FALSE-FAILs when the span (a) crosses a line-wrap ("runs the tour / through to its close"; "you do / not resume this dispatcher") or (b) has a `**` mid-phrase ("**not** demonstrate"). Also the naive `grep 5.?min` FALSE-PASSES on the honest "~10–15 min" framing. WP7e pins MUST: anchor on short wrap-safe substrings (not full sentences), be `**`-tolerant, and for the no-5-min-claim pin assert honest-framing-PRESENT + prohibition-PRESENT (never a bare `5.?min` count). This matches the WP7e forward pin-spec in the archived WP7b WIP.

## Test Triage — WP7d Phase 2 codify assertion "dispatcher divergence semantics (both spots)"
Classification: Obsolete/incorrect test (check-string precision bug; the SKILL.md prose is correct). NOT a code regression.
Confidence: high
Evidence: the Step-3 phrasing spans a line break — line 105 ends "…the arm runs the / tour through to its close…", so my compound-AND grep for the single-line span `runs the tour through to its close` found nothing even though the sentence is present + correct. The Category spot (`runs the tour to its close`, L23) + both no-resume anchors (`do not resume this dispatcher` L24; `do not come back here` L106) confirm both spots carry unambiguous divergence semantics. verify-self subagent already confirmed both verbatim.
Action: replaced the line-wrap-fragile compound grep with per-spot anchors that tolerate wrapping. No SKILL.md prose changed. Throwaway codify grep, not a committed test.

## Test Triage — WP7d Phase 1 codify assertions O3c & O4c
Classification: Obsolete/incorrect test (the ad-hoc grep *check* was wrong; the SKILL.md prose under test is correct). NOT a code regression.
Confidence: high
Evidence:
  - O3c: the no-live-demo prose IS present at `skills/tutorial-greenfield-workflow-tour/SKILL.md:193` — "Do **not** demonstrate autopilot/FSD live in the tour". My check grepped the fixed string `not demonstrate autopilot/FSD live`, but the copy bolds the word as `**not** demonstrate`, so the literal substring differs by the `**` markers. Check-string bug.
  - O4c: this is the **exact naive-grep false positive** the WIP note + WP7e forward pin-spec warned about. `grep 5.?minute` matches "1**5 minute**" inside the HONEST "~10–15 minute" framing (L28), which is correct+required prose, not an affirmative 5-min promise. The only *literal* "5-minute" token is the required prohibition (L31 "quick 5-minute"). Naive-grep check bug — NOT prose. The durable WP7e pin must assert honest-framing PRESENT + prohibition PRESENT, never a bare `grep 5.?min`.
Action: fixed all three ad-hoc check expressions (bold-tolerant substring for O3c; and for O4c: assert the ONLY literal "quick 5-minute" occurrence is the prohibition — no affirmative "the tour is/takes 5 minutes" promise — rather than a range-blind `5.?minute` grep) and re-ran. These are throwaway verify-auto/codify greps, not committed tests. The durable structural pins are WP7e's charter (deferred by design); check-structure.sh (472/0) is the committed regression gate and stays green. No SKILL.md prose was changed.

## Code-Quality Review — wp7d-staged-beats-wiring

Reviewer: `code-quality-reviewer` subagent against ship commit `ae733ab` (drive_mode=autopilot → Mode-3: 0 CRITICAL / 0 MAJOR / 2 MINOR auto-backlogged, F39).

### Strengths
- Bookend mechanics faithfully narrated: Step-7 three-scene choreography matches the real `/session-handoff` (writes `.session.md` pointer + state-file marker) and `/session-restore` (reads pointer, reopens WIP, offers mode menu, strips marker, deletes `.session.md`) skills verbatim — no invented mechanics; honors the §6 "narrated real run, not a demo reel" invariant.
- §6 honest-framing preserved with care: no "5-min" claim introduced, `~10–15 min` framing + explicit prohibition both kept, un-push "Not recommended yet." counterweight placed exactly where §7 puts it (LAST, un-pushed, named-not-staged).
- Greenfield/brownfield asymmetry respected end-to-end: brownfield Step 7 ties surviving state to the Step-4 `CLAUDE.md` + the drift/forgetting pain; hierarchy CUT-then-named on brownfield vs. light-taste on greenfield — arm-flavored, not copy-pasted.
- Each staged beat pre-framed before the move it narrates (§6 per-beat pre-framing).
- Every consumed finding verified against real text first (review-finding-actions-are-hypotheses); the two codify FAILs correctly triaged as check-authoring bugs with zero prose changed to chase a bad grep.

### Issues
**CRITICAL** — (none)
**MAJOR** — (none)
**MINOR**
- [`skills/tutorial-greenfield-workflow-tour/SKILL.md` Category §, `skills/tutorial-brownfield-workflow-tour/SKILL.md` Category §] Both arm-skill Category sections still say "it does not return control to the dispatcher" — the identical literal-mechanics overstatement the consumed DISPATCHER-CONTROL-RETURN-PHRASING finding flagged and that P2.3 softened *in the dispatcher only*. A one-shot `Skill` invocation does mechanically return to its caller; the finding's concern applies verbatim to these two arm copies. Scope-symmetry gap (repo's "Scope-symmetry at mitigate time" convention). MINOR because the surrounding "the two paths diverge and stay diverged" already softens it. — *Reintroduces the exact ambiguity the consumed finding removed, one hop away.*
- [both arm skills, closing paragraphs] The closing lines hand the user "back to their real work" but neither closing paragraph states the terminal action explicitly (stop; emit no transition) — the run-end is only stated in the separate `## Transitions` section. A one-clause "the tour ends here — nothing further to invoke" would close the loop. — *A prose-only skill's terminal behavior should be unambiguous at the close.*

### Assessment
Well-built prose feature that advances the codebase without accruing debt. Wiring is coherent, mechanics-faithful, beat dispositions match §7 exactly, §6 honest-framing survives intact, all four consumed findings genuinely resolved (verified-against-code first), and the codify triage correctly refused to mutate correct prose for a fragile grep. Only residual is the scope-symmetry MINOR (the dispatcher fix not mirrored into the two arm Category blocks) — genuinely minor, cheaply swept. Nothing requires a refactor.

### If you disagree
Operator: dismiss any finding by editing this section and marking the line `[DISMISSED]` before `feature-finalize` archives the WIP.

## Retrospect
- **What changed in our understanding:** Nothing about the *design* — WP7a's spec + WP7b/WP7c's forward-declarations gave WP7d fully-specified targets, so this was straight execution. What *did* surface (twice, then a third time) is a reusable **test-authoring** lesson: naive single-line grep observables FALSE-FAIL against this repo's hard-wrapped + `**bold**` SKILL.md prose (a phrase crossing a line-wrap; a `**` mid-phrase) and FALSE-PASS on the honest "~10–15 min" framing (`5.?min` matches "1**5 min**"). Recorded as a NOTE-for-WP7e in the WIP Discoveries so WP7e's durable pins don't inherit the trap.
- **Assumptions that held:** prose-only + no-transition + no-new-skill-dir (all 3 skills already symlinked live → install.sh untouched); Mode-3 auto-skip gate applied cleanly (no integration boundary — within-skill prose + a spec doc); all 4 consumed findings were real on contact (verified-against-code first, none was a mis-scoped hypothesis).
- **Assumptions that were wrong:** none material. The only friction was my own throwaway grep checks (3 FAILs, all triaged as check-authoring bugs, zero prose changed) — a self-inflicted verification-tooling issue, not a feature-shape surprise.
- **Approach delta:** matched the plan exactly (Phase 1 greenfield → Phase 2 brownfield + dispatcher + spec MINORs). One additive review-finding surfaced (scope-symmetry: the dispatcher control-return fix wants mirroring into the two arm Category blocks) — correctly Mode-3 auto-backlogged, not pulled into this feature.

## Communicate
> **Feature complete:** WP7d (staged-beats wiring) has shipped. It replaces the two forward-declared beat placeholders in both `tutorial-*` tour arms with real scene-by-scene choreography — the `/session-handoff`→`/session-restore` emotional-peak bookend and the LAST/un-pushed drive-modes graduation reveal — and consumes 4 prior review findings. Verify by reading Steps 7–8 of `skills/tutorial-{greenfield,brownfield}-workflow-tour/SKILL.md` (commit ae733ab); the real acceptance is the operator's upcoming hands-on `/tutorial-getting-started` run.

Requester = operator — closure notice for self-record.

## Notes on codify + verify-human
- **verify-codify (both phases):** WP7e owns the behavioral scenarios + `tutorial-`-prefix structural pins for the whole family (explicitly deferred there by WP7b/WP7c). WP7d's codify is therefore **lightweight**: assert the grep-able observable outcomes above (forward-declaration blocks gone, `/session-handoff`+`/session-restore` present, "not recommended yet" present, no "5-min" claim, "light taste" bound present) hold, and note that the richer behavioral coverage is WP7e's charter. Do NOT pull WP7e's scenario/pin work forward.
- **verify-human:** the real acceptance for the tour copy is the operator's hands-on `/tutorial-getting-started` run (already tracked as `SURFACE-2026-07-22-WP7C-OPERATOR-HANDS-ON-ACCEPTANCE-DEFERRED`). WP7d is prose-only with no integration boundary and verify-self will be all grep/read confirms — the Mode-3 auto-skip gate may apply; the affirmation block still prints as the operator's read-time veto.
- **Consumed-finding resolution:** on finalize, the 3 quality-finding SURFACEs actually resolved by WP7d edits (`GREENFIELD-PRODUCT-ENTRY-UNBOUNDED`, `DISPATCHER-CONTROL-RETURN-PHRASING`, `SPLIT-GREENFIELD-GROUNDING`, `SECTION3-LEGEND-NO-DISPOSITION-TOKENS`) get `**Backlog resolved:**` CHANGELOG lines + delete-on-resolve from `backlog-quality-findings.md` + their pointer stubs in `backlog.md`. The greet-TODO / mktemp / --help WP7c findings are NOT WP7d's (they're scaffold code, not tour copy) — leave them.
```
