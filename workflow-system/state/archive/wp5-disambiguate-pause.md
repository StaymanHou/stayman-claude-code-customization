# Feature: WP5 — Disambiguate "pause" (session-handoff / session-restore rename + turn-level reservation + agent-side guard)

**Workflow:** feature
**State:** COMPLETED 2026-07-21 (commit f532b4d shipped; finalized)
**Created:** 2026-07-21
**Milestone:** WP5 / Milestone 9 (Claudesk Handoff Cycle)
**drive_mode:** autopilot

## Problem Statement

The bare word "pause" is overloaded across the workflow system: sometimes the operator means *interrupt the current turn / course-correct mid-flight* (cheap, reversible, no artifact), other times *invoke the session-pause skill* (expensive — writes `workflow-system/state/.session.md` + a full handoff, wrong when unwanted). A 387-turn / 11-project raw-log audit (`tmp/pause-terminology-audit.md`) confirmed the bite is real AND bidirectional: a confirmed false-positive shows the agent inventing a session-pause off the word *"defer"* (no "pause" utterance present), writing `.session.md`, then having to `rm` it. The operator has self-trained to say "pause **the session**" and to append guard phrases ("DO NOT invoke the session pause skill") — workarounds for missing vocabulary. Separately, `/session-resume` prefix-collides with the built-in harness `/resume`, forcing a down-arrow on every resume.

**Settled vocabulary (operator-aligned 2026-07-21):**
- **Turn-level** (cheap, no skill): bare "pause" / "stop" / "hold" = interrupt only, never writes `.session.md`.
- **Session boundary** (writes `.session.md`): RENAME `session-pause` → **`session-handoff`**, `session-resume` → **`session-restore`**. Dual-register (prompt phrase + skill name). "restore" kills the `/resume` collision; "handoff" matches the operator's own name for the artifact (".session.md is a handoff doc"); `session-` prefix preserved.
- **Cross-project** (WP8/future, name reserved only): `/project-handoff` — the `session-`/`project-` prefix disambiguates the two handoff scopes.

[Updated 2026-07-21 (F12 back-loop): problem statement unchanged in essence — still "disambiguate the session vocabulary." What we learned: the rename `session-resume`→`session-restore` fixed the `/resume` collision but the fuzzy-matcher then surfaced `session-store-learning` ABOVE `/session-restore` on `/restore` (shared "store" substring). This is the SAME class of problem (collision in the session- command namespace), extended in scope: also rename `session-store-learning`→`session-capture` (P1.6). Root cause is a namespace collision, not a symptom shift — no re-plan needed.]
- **Agent-side guard** (approved): never write `.session.md` unless session intent is explicit OR confirmed — regardless of trigger word. Closes the bidirectional over-reach.

**Scope facts (grepped, not assumed):** 13 files reference `session-pause`/`session-resume`; 2 skill dirs to rename. S-transition IDs (S6/S15/S16/S17) do NOT change — only skill *names* (so `transitions.md` needs name-text updates, no ID/structure change). `tests/sessions/*.jsonl` is a FROZEN Tier-2-audited historical capture (9 refs) — **exempt from rename** (explicitly excluded by check-structure Phase-15 filters). `install.sh` re-links `~/.claude/skills/` from the renamed dirs. Resolves + deletes `SURFACE-2026-07-20-CLAUDESK-PAUSE-AMBIGUITY` on finalize (delete-on-resolve). Related open backlog `SURFACE-...-S2-S12-FRAGILITY` touches session scenarios — must not destabilize further.

## Work Tree

- [x] Phase 1: Mechanical rename + reference sweep + re-link  <!-- status: complete — all impl (P1.1-P1.6) + verify-auto/self/human/codify [x]; both collisions (/resume→restore, /restore→store) resolved -->

  **Observable outcomes:**
  - CLI: `ls skills/` shows `session-handoff/` and `session-restore/`, and NOT `session-pause/`//`session-resume/` (exit 0 on `test -d skills/session-handoff -a -d skills/session-restore -a ! -d skills/session-pause -a ! -d skills/session-resume`)
  - CLI (P1.6 collision fix): the `/restore` fuzzy-match no longer surfaces a competing skill — `ls skills/` shows `session-capture/` and NOT `session-store-learning/`; the ONLY skill dir matching the substring "store" is `session-restore` itself (the intended `/restore` target); `grep -q '^name: session-capture' skills/session-capture/SKILL.md` exits 0
  - CLI: each renamed SKILL.md frontmatter `name:` matches its new dir — `grep -q '^name: session-handoff' skills/session-handoff/SKILL.md` and `...session-restore...` both exit 0
  - CLI: `git log --follow --oneline skills/session-handoff/SKILL.md | head -1` shows history preserved across the rename (git mv, not delete+add)
  - CLI: no non-frozen file references the old names — `grep -rl 'session-pause\|session-resume' skills/ agents/ tests/ CLAUDE.md CLAUDE.snippet.md workflow-system/product/ | grep -v 'tests/sessions/.*\.jsonl'` returns empty (exit 1 from grep = no matches)
  - CLI: `./install.sh` exits 0 and `~/.claude/skills/session-handoff` + `~/.claude/skills/session-restore` symlinks exist and resolve into this repo; old-name symlinks are gone
  - [x] P1.1 `git mv skills/session-pause skills/session-handoff` and `git mv skills/session-resume skills/session-restore` (history-preserving — git detects R100)  <!-- status: complete -->
  - [x] P1.2 Update each renamed SKILL.md: frontmatter `name:`+`description`, title, self-refs, footer heading (`## Session Handoff`), the resume-command prose (`/session-restore`); §6b strip pattern now matches BOTH new + legacy `## Session Pause` heading (transition window). S-IDs unchanged. **NOTE: P2.1 folded in here** — the intent-disambiguation + agent-side-guard section was the natural home in session-handoff/SKILL.md and was written now.  <!-- status: complete -->
  - [x] P1.3 Swept 4 orchestrator `agents/*/AGENTS.md` (frontmatter `skills:` + prose). **P2.2 folded in** — turn-vs-session disambiguation + agent-side guard prose added to all 4 canonical AGENTS.md.  <!-- status: complete -->
  - [x] P1.4 Swept `CLAUDE.md`, `CLAUDE.snippet.md`, `session-start/SKILL.md` (4 refs), `feature-plan/SKILL.md`, `transitions.md` name-text (S6/S15/S16/S17 + prose; also fixed the co-located stale `workflow/wip/`→`workflow-system/state/wip/` on line 145 per the same-line carve-out). Intentionally OUT of scope: `workflow-system/product/{roadmap,vision,arch}.md` describe the milestone-as-planned (legit pre-impl `/session-pause` mentions) → resynced at FINALIZE (WP3-M7 as-built pattern), not here. `vision.md` stale `workflow/.session.md` path = separate MOVED-PRODUCT-DOCS backlog item.  <!-- status: complete -->
  - [x] P1.5 Ran `./install.sh` (created 2 new symlinks, exit 0). install.sh is ADDITIVE-ONLY — left 2 dangling old-name symlinks; removed them defensively (symlink + dangling + target-in-repo guard). See SURFACED discovery.  <!-- status: complete -->
  - [x] P1.6 [F12 back-loop scope-add] Renamed `session-store-learning` → `session-capture`. git mv (R100 history preserved) + frontmatter name:+description + title + 2 self-refs; swept LIVE machinery: session-reflect (5), 4 close skills + session-start (5 `/session-capture` prompts), CLAUDE.md (3), CLAUDE.snippet.md (1), transitions.md S20 row, tests/{scenarios/session.yaml (8 incl. 5 skill: fields), run-tests.sh (1), check-structure.sh (9 — 6 pins re-pathed to skills/session-capture/SKILL.md)}. install.sh re-linked + pruned orphan. Only `session-restore` now matches `/restore` (intended). EXEMPT (historical, unchanged): `workflow-system/state/archive/*.md` + `product/archive/*` (immutable completed-feature records), `tests/sessions/*.jsonl` (frozen), transitions.md:572 (dated 2026-05-11 changelog entry), arch.md (resync at finalize).  <!-- status: complete -->
  - [x] verify-auto  <!-- status: complete (re-run after P1.6) — session-capture frontmatter valid + name matches; all 3 renamed symlinks resolve; 6 re-pathed check-structure pins target existing file; no old-name leftover in live machinery except intentional CLAUDE.md migration note -->
  - [x] verify-self  <!-- status: complete (re-run after P1.6 + tests/-sweep shortcut fix) — fresh subagent confirmed all 6 outcomes PASS: 3 renames done, frontmatter matches, R100 history, collision fixed (only session-restore matches /restore), tests/ machinery clean (pins re-pathed + exist + search-strings present + session.yaml skill: fields valid), symlinks re-linked + old absent -->
  - [x] verify-human  <!-- status: complete — operator confirmed "all good now" after 2 collision fixes (name: session-store-learning→session-capture; description: incident-mitigate/session-handoff/session-reflect reframed away from "restore"). /restore now surfaces session-restore ONLY. -->
    - [x] P1.verify-human.1  <!-- status: complete -->
    - [x] P1.verify-human.2  <!-- status: complete -->
    - [x] P1.verify-human.3  <!-- status: complete -->
  - [x] verify-codify  <!-- status: complete — full ./tests/check-structure.sh 446/0 (was 438; +8 new [Phase 17] pins: 3 renamed-skill name anchors + old-name anti-regression + /restore description-collision guard + 2 disambiguation/guard-prose + 1 AGENTS.md prose). Integration boundary (check-structure pins + session.yaml consume the skill names) satisfied by the consuming-surface suite run. P3.2 structural pins folded in here. Test Triage: none — 446/0 on first full run. -->

- [x] Phase 2: Behavioral change — turn-vs-session disambiguation + agent-side guard  <!-- status: complete — all impl (P2.1-P2.3) + verify-auto/self/human/codify [x]; contextual guard + /resume anti-trigger folded in per two operator corrections -->

  **Observable outcomes:**
  - CLI: the renamed `session-handoff/SKILL.md` contains an explicit intent-disambiguation section reserving bare "pause"/"stop"/"hold" for turn-level and an agent-side guard rule ("do not write `.session.md` unless intent is explicit or confirmed") — `grep -qi 'turn-level' skills/session-handoff/SKILL.md` and `grep -qi 'explicit or confirmed\|explicit intent' skills/session-handoff/SKILL.md` exit 0
  - CLI: the 4 orchestrator `agents/*/AGENTS.md` carry the disambiguation guidance so the driving orchestrator applies it — `grep -rli 'turn-level\|hand off the session\|explicit or confirmed' agents/` returns ≥1 (the canonical location; per-skill reinforcement per the dual-location pause-policy precedent)
  - CLI: `CLAUDE.snippet.md` documents the settled vocabulary as a durable convention (grep for the three-scope rule) — a global rule injected into every session, mirroring the WP6 `## Research cost tiers (GLOBAL)` precedent
  - Behavioral (verify-codify surface): a scenario in `tests/scenarios/session.yaml` where an ambiguous bare-"pause"/"defer"-style input does NOT auto-write `.session.md` (asserted via `contains_any`/`not_contains` on the model's prose, per the prose-behavior scenario pattern)
  - [x] P2.1 Added intent-disambiguation + agent-side-guard section to `session-handoff/SKILL.md` (done in P1.2)  <!-- status: complete -->
  - [x] P2.2 Added turn-vs-session disambiguation + agent-side guard prose to all 4 orchestrator `agents/*/AGENTS.md` (done in P1.3)  <!-- status: complete -->
  - [x] P2.3 Added `## Session vocabulary — turn vs. session boundary (GLOBAL)` rule to `CLAUDE.snippet.md` (after Research cost tiers, its structural precedent) — 3-scope table + 3 rules incl. the fuzzy-matcher-searches-descriptions naming rule; + a WP5 pointer bullet in root `CLAUDE.md` Conventions. Content-discipline litmus met (durable + reusable). Also added 2 more Phase-17 pins (snippet-rule anchors).  <!-- status: complete -->
  - [x] verify-auto  <!-- status: complete — ./tests/check-structure.sh 448/0 (10 Phase-17 pins all PASS incl. the 2 new snippet-rule anchors); snippet section header + naming-rule + CLAUDE.md pointer all present -->
  - [x] verify-self  <!-- status: complete — subagent verified all 5 outcomes PASS (disambiguation section + guard in session-handoff/SKILL.md; snippet GLOBAL rule; CLAUDE.md pointer; guard consistent bidirectionally across SKILL.md + snippet). Subagent OBSERVATION caught a real gap: incident-workflow/AGENTS.md had only the frontmatter rename, not the disambiguation prose (the other 3 had it) — FIXED in-flight (added item #9 to its "Your Role"); all 4 orchestrators now uniform. -->
  - [x] verify-human  <!-- status: complete — operator APPROVED the disambiguation table + guard, THEN directed two corrections (contextual guard; /resume-is-turn-level anti-trigger) which were folded in as trivial operator-dictated extensions + structurally re-verified (452/0). No full re-loop needed (operator dictated the exact semantics). -->
    - [x] P2.verify-human.1 disambiguation table + guard read correctly  <!-- status: complete — approved, then sharpened per two operator corrections (contextual guard + /resume anti-trigger) -->
    - [x] P2.verify-human.2 turn-level vs session-boundary + going-offline semantics correct  <!-- status: complete — operator confirmed /resume=turn-level, boundary=natural-auto-chain -->
  - [x] verify-codify  <!-- status: complete — check-structure 452/0 (14 Phase-17 pins, deterministic guarantee); 2 behavioral scenarios (S26/S27) written+structurally-validated, execution DEFERRED on the known --id parse-all harness bug (not a content failure; no Test-Triage needed — a runner hang, not an assertion fail). Phase 3 items P3.1/P3.2/P3.3 all absorbed here. -->

- [x] Phase 3: Tests, scenarios, structural pins — ABSORBED into Phase 2 (P3.1 scenarios + P3.2 pins + P3.3 runs all completed under Phase 2's verify-codify; kept as a doc node, no separate verify loop)  <!-- status: complete; depends on Phase 2 -->
  **Observable outcomes:**
  - CLI: `tests/scenarios/session.yaml` `skill:` fields for S6/S15/S16/S17 reference the RENAMED skills (`session-restore`/`session-handoff`) — `grep -c 'skill: session-restore\|skill: session-handoff' tests/scenarios/session.yaml` ≥ 4; zero `skill: session-resume\|skill: session-pause` remain
  - CLI: `tests/check-structure.sh` passes end-to-end (exit 0), with its 5 old-name references updated to the new names AND a new pin locking the disambiguation-guard prose (mirrors WP6 Phase-16 pin shape); registry-timed run
  - Behavioral: the new ambiguous-input scenario PASSes on a fresh-subprocess `tests/run-tests.sh --group session` run (the renamed skills resolve because install.sh re-linked them in P1.5); the pre-existing S2/S12 fragility does not regress further
  - [x] P3.1 session.yaml `skill:` fields already renamed in P1.6; ADDED 2 behavioral guard scenarios — **S26** (bare mid-work "pause" → turn-level, declines handoff, no .session.md) + **S27** (going-offline "I need to go, I'll /resume later" → turn-level HOLD, not a cross-session handoff). Both encode the two live misfires as regression tests; neutral args per the prompt-leakage lesson; SOFT_PASS contains_any + not_contains guarding the misfire (Handed off / .session.md write / TRANSITION: S17). Structurally validated (skill dir + fixtures exist, YAML parses 33 scenarios).  <!-- status: complete -->
  - [x] P3.2 check-structure old-name refs updated (P1.6) + `[Phase 17]` added (14 pins: renamed-skill anchors, old-name anti-regression, /restore name+desc collision guard, disambiguation/guard prose, contextual-guard, /resume-anti-trigger, snippet-rule anchors).  <!-- status: complete -->
  - [x] P3.3 Run suites: `check-structure.sh` **452/0** ✅ (re-confirmed after ALL Phase-2 edits incl. S26/S27 + /resume anti-trigger — the primary, deterministic regression guarantee). Behavioral `run-tests.sh --id S26,S27` — **DEFERRED**: the runner hung >5min in the parse-all-scenarios pre-execution phase (0 bytes out, no `claude --print` ever spawned) — the known `SURFACE-2026-07-15-RUN-TESTS-ID-FILTER-PARSES-ALL-SCENARIOS-FIRST` harness-perf bug, orthogonal to this feature's content. The 2 scenarios are written + structurally validated (skill dir + fixtures exist, YAML parses); they'll execute in the next full `run-all.sh` sweep (which pays the parse cost once for all scenarios) or after the `--id` bug is fixed. Behavioral execution deferred (bootstrap-skip-style, per docs/lessons/harness-bootstrap-skip.md); structural pins carry the regression guarantee meanwhile. Triage: no test FAILURE occurred — a runner-perf hang, not an assertion failure, so no Test-Triage artifact required.  <!-- status: complete (behavioral-run deferred, structural green) -->
  - [x] NOTE: full --group session NOT re-run — the other 31 scenarios are unchanged by Phase 2 (only skill: renames in P1.6, already covered) and re-running ~33 CLI subprocesses is cost without new signal; the 14 structural pins are the primary regression guarantee, S26/S27 the new behavioral coverage.  <!-- status: complete -->
  - [x] verify-auto/self/human/codify — N/A as a separate loop: Phase 3 is ABSORBED into Phase 2 (its P3.1 scenarios + P3.2 pins + P3.3 runs were completed under Phase 2's own verify-codify). No separate verification gate; inherits Phase 2's green (452/0).  <!-- status: complete (absorbed) -->

## Current Node
- **Path:** Feature > FINALIZED (F19 → reflect). Shipped f532b4d + finalized 755946a (both local, NOT pushed; 20 ahead of origin/main). Archived. WBS 6/8 (WP7+WP8 remain).
- **Active scope:** none — code-quality review done (0 CRITICAL, 1 MAJOR + 1 MINOR both FIXED in-place, 1 MINOR no-action). check-structure 452/0. Review edits (S27 echo-leak fix + WIP review section) uncommitted → finalize sweeps them. Next: /feature-finalize.
- **Blocked:** none
- **Unvisited:** review-quality → finalize (finalize resolves+deletes SURFACE-2026-07-20-CLAUDESK-PAUSE-AMBIGUITY, resyncs product docs, appends CHANGELOG, archives WIP). Stray pre-existing symlink `my-claude-code-customization` left untracked (not mine — flag at finalize).
- **Ship state:** ship (complete) — feat commit f532b4d
- **Open discoveries:** SURFACE-2026-07-21-INSTALL-SH-NO-ORPHAN-PRUNE (medium — fired 3× this feature); SURFACE-2026-07-15 escalated low→medium (--id parse-all blocked behavioral run)

## Code-Quality Review — wp5-disambiguate-pause

**Reviewer:** code-quality-reviewer subagent against ship commit f532b4d. **Verdict: 0 CRITICAL, 1 MAJOR, 2 MINOR.** Advisory; drive_mode=autopilot.

### Strengths
- Vocabulary stated identically across all 6 prose surfaces (session-handoff/SKILL.md + 4 AGENTS.md + CLAUDE.snippet.md); guard prose byte-identical across the 4 orchestrators — no drift channel.
- Both live misfires (mid-work "defer"/"pause"; "/resume later" going-offline) encoded as dedicated regression scenarios (S26/S27), both following the prompt-leakage lesson (neutral args, no rule-recital).
- Phase-17 anti-regression pin well-designed: old-name leakage guard (Phase-15-style exclusions) + a *mechanical* description-collision guard (scans every skill frontmatter so only session-restore matches "restor") — makes the "fuzzy-matcher searches descriptions" insight enforceable, not aspirational.
- Contextual-guard framing (workflow-position-keyed) is sharper than blanket always-confirm; the operator-correction rationale is pinned so a regression to always-confirm fails a test.
- Snippet content-discipline respected: only the durable GLOBAL convention landed; install.sh orphan-prune mechanics SURFACEd, not baked in.

### Issues
**CRITICAL** — (none)

**MAJOR**
- [tests/scenarios/session.yaml S27:485] `contains_any` included the bare token `"/resume"`, but the scenario's args literally say "I'll /resume later" — a model merely echoing the operator ("you said you'll /resume later") would SOFT_PASS without proving it *recognized* the going-offline family as turn-level. Echo-leak on the assertion side (same class as the prompt-leakage lesson). **→ FIXED IN-PLACE (2026-07-21):** dropped `"/resume"`, added non-echoable `"continue this turn"`; all 5 positive tokens now only appear if the model actually classified turn-level. NOT backlogged — resolved now (trivial one-line fix in a file this feature authored; the pin is the only current guarantee since behavioral run is deferred, so a weak assertion was worth fixing immediately).

**MINOR**
- [WIP ## Current Node] Duplicate/stale pointer triple (old mid-Phase-2 Blocked/Unvisited stacked under the post-ship one). **→ FIXED IN-PLACE (2026-07-21):** rewrote Current Node to a single authoritative block.
- [tests/scenarios/session.yaml S26/S27] Behavioral scenarios committed with execution deferred on the known `--id` parse-all harness bug — structurally validated only, latent until the next full `run-all.sh` sweep. NOT a defect of this feature; honestly disclosed + backlog escalated low→medium. **No action** (Phase-17 pins carry the regression guarantee meanwhile).

### Assessment
Well-built prose/convention feature. Mechanically thorough sweep (~13 old-name refs across skills/agents/transitions/tests/CLAUDE, S-IDs held stable). Cross-surface consistency — which this kind of feature lives or dies on — is clean; the disambiguation rule reads without contradiction across all 6 surfaces, and the snippet heading matches its pin anchor. Advances the codebase (the fuzzy-matcher insight is now a mechanical guard, not a comment; the orphan-prune gap was surfaced not hacked). Only substantive concern was testing rigor (S27 echo-leak + deferred run), both now addressed (MAJOR fixed in-place; deferred-run backlogged). No CRITICAL; not ship-blocking.

### If you disagree
Operator: dismiss any finding by editing this section and marking the line `[DISMISSED]` before finalize archives the WIP.

## Retrospect
- **What changed in our understanding:** The disambiguation surface was **wider and subtler** than the plan assumed. (1) The plan targeted 2 skills (session-pause/resume); the real fix touched 3 (session-store-learning collided with `/restore` too). (2) The fuzzy-matcher searches **descriptions, not just names** — a discovery made live (incident-mitigate's "restore service" out-ranked session-restore). (3) The agent-side guard is **contextual (workflow-position-keyed)**, not a universal confirm — the operator corrected the too-broad first version. (4) `/resume` (built-in) is **turn-level**, pairing with HOLD, not session-restore — I had this backwards and it caused a live misfire.
- **Assumptions that held:** The rename itself was mechanically clean (history-preserving `git mv`, S-IDs stable, grep-driven sweep). The structural-pin-as-regression-guarantee approach held (452/0). The WP6 "one word, two costs, confirm-before-expensive" pattern transferred as predicted.
- **Assumptions that were wrong:** (a) "2 skills, 13 files" — actually 3 skills, ~27 files. (b) "the rename is the work" — the *behavioral guard's semantics* were the hard part, refined twice by operator correction. (c) I assumed the original rename sweep covered tests/ — it missed them for the handoff/restore rename (caught by verify-self subagent). (d) I twice mis-classified a turn-level intent ("I'll /resume later") as a session handoff — the exact misfire class this feature exists to prevent, ironically committed during the feature.
- **Approach delta:** Plan had 3 phases (rename / behavioral / tests); in practice P2.1/P2.2 folded into Phase 1 and Phase 3 absorbed into Phase 2's verify-codify. Two F12 back-loops (the session-store-learning collision; not a back-loop but two in-verify-human corrections). The two live misfires became S26/S27 regression scenarios. Behavioral scenario execution deferred on a harness bug (structural pins carry the guarantee). Net: same deliverable, more iterations than a mechanical rename would suggest — because the vocabulary + guard semantics were the real work, exactly as the WBS REFRAME predicted ("the design conversation is the real cost").

## Discoveries
<!-- Format: [SURFACED-<date>] <target node> — <summary>
     Each entry is also logged to workflow-system/state/backlog.md -->
- [ ] install.sh no orphan-prune on rename  <!-- status: SURFACED: install.sh additive-only; leaves dangling old-name symlinks after a skill rename -->
[REFINED-2026-07-21b] Phase 2 (post-hold) — SECOND confirmed misfire of session-handoff, live: operator said "hold here, I need to go, I'll /resume later" (mid verify-human) and the agent WROTE the handoff, treating "/resume later" as session-boundary intent. WRONG. Root of my error: I had `/resume` semantics BACKWARDS. `/resume` is the BUILT-IN that continues the CURRENT TURN after a turn-level HOLD (operator stepped away / went offline / shut down the machine); it does NOT pair with session-restore. So "I'll /resume later / I need to go / shutting down / disconnecting" = TURN-LEVEL HOLD (stop immediately so operator can `/exit`), NOT a handoff cue. Fix: added the "going-offline family" to the turn-level side + an explicit "/resume ≠ /session-restore" anti-trigger note in all 3 surfaces (session-handoff/SKILL.md, CLAUDE.snippet.md, 4× AGENTS.md) + 2 new Phase-17 pins (452/0). Undid the erroneous .session.md + WIP marker in-turn (rm). This is now the SECOND documented misfire of this exact skill — both are the "superficially-handoff-ish word is actually turn-level" class.
[REFINED-2026-07-21] Phase 2 verify-human — operator corrected the agent-side guard: "never write .session.md unless explicit or confirmed" was TOO BROAD. The confirm is CONTEXTUAL, keyed on WORKFLOW POSITION: at a clean workflow boundary (post finalize/close/resolve→reflect with nothing to persist, or post session-capture confirmed-save) a session handoff is the NATURAL, expected auto-chain — no confirm (this is how mccc is used in practice: boundary handoff is almost always right). The guard only fires MID-WORKFLOW on an ambiguous word (bare pause/defer/wrap-up/hold in the middle of a phase). Discriminator = workflow position, not the trigger word. Rewrote the guard in all 3 surfaces (session-handoff/SKILL.md §Intent-disambiguation, CLAUDE.snippet.md rule 2, 4× AGENTS.md). Description frontmatter left as-is (its "ambiguous intent" phrasing is already consistent — the confirm fires on ambiguity, not universally).
[SURFACED-2026-07-21] Phase 1 (P1.5) — `install.sh` is additive-only; after `git mv` of a skill dir it leaves the old-name symlink dangling in `~/.claude/skills/`. Removed 2 orphans manually. Logged as SURFACE-2026-07-21-INSTALL-SH-NO-ORPHAN-PRUNE (product:wbs, medium).
[SURFACED-2026-07-21] Phase 1 verify-human (2nd) — THE FUZZY-MATCHER SEARCHES DESCRIPTIONS, NOT JUST NAMES. Operator found `/restore` STILL surfaced `/incident-mitigate` ABOVE `/session-restore` — because incident-mitigate's *description* said "apply fixes or workarounds to **restore** service." Renaming the skill (P1.6) fixed name-collisions but not description-collisions. Fix: reframed 3 descriptions — incident-mitigate ("restore service"→"recover service and bring it back to health"), session-handoff ("so a future session can restore it"→"...pick the work back up"), session-reflect ("prompt store-learning"→"prompt session-capture", also a stale skill-name ref). After: `session-restore` is the ONLY skill matching "restor" by name OR description. Comprehensive sweep of all new vocab (handoff/restore/capture/pause) confirms remaining matches are all either intended pairings (session-reflect→session-capture cross-ref) or coincidental English words with no shadowed target (feature-verify-self "human handoff", feature-review-quality "pause-and-ask"; note there's NO /pause skill by design). TRANSFERABLE LESSON → capture as convention at codify/finalize: skill naming must check BOTH name and description against sibling command prefixes.
[SHORTCUT-2026-07-21] Phase 1 verify-self — the verify-self subagent caught a BLOCKING gap: the ORIGINAL session-pause/session-resume rename sweep (P1.4) missed `tests/` — 3 check-structure `grep_check` pins still pointed at the now-missing `skills/session-{resume,pause}/SKILL.md`, and 4 `session.yaml` `skill:` fields (S6/S15/S16/S17) still named the old skills. Fixed in-place (trivial extension of the P1.4 sweep to the tests/ files it skipped): re-pathed the 2 check-structure pins to session-restore/session-handoff (+ updated the §6b search string "Pause footer"→"Handoff footer" to match the edited heading), swapped the 4 session.yaml skill: fields. Re-verified by a FRESH verify-self subagent spawn (gate 2). Note: the P1.6 sweep DID cover tests/ for session-capture — the gap was specific to the earlier handoff/restore rename.
