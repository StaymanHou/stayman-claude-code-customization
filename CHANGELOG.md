# Changelog

## 2026-07-21

- **Feature shipped:** Doc-layout unification (M7) — the workflow system's two per-project doc folders were physically unified under one root (`docs/product/*` → `workflow-system/product/`, `workflow/*` → `workflow-system/state/`) so a newcomer learns one place instead of two; delivered as a path-anchored 58-file source sweep in this repo, a new `tools/migrate-doc-layout/` migration primitive (idempotent, `--dry-run`, reversible backup, drift-keep-both, git-mv history preservation, 35-assertion test suite) that migrated 9 consuming projects (gospelherald excluded), a `check-structure.sh` Phase-15 anti-regression lock (4 pins, 420/0), and an `arch.md` AS-BUILT resync + M12 return-contract capture of the settled layout for Claudesk M11's `docs_list`.
- **Backlog resolved:** SURFACE-2026-07-20-CLAUDESK-UNIFY-DOC-FOLDERS — closed by the doc-layout-unification feature (this milestone IS that SURFACE; the split-folder confusion it described is removed).
- **Milestone:** Milestone 7 — Unify the workflow-doc layout for new users (WBS WP1 decide + WP2 sweep/tool + WP3-M7 resync, all shipped).
- **Backlog resolved:** SURFACE-2026-07-21-QUALITY-BACKUP-INSIDE-REPO-STAGED — post-ship refactor moved `migrate-doc-layout.sh`'s reversible backup OUTSIDE the repo (`$TMPDIR/migrate-doc-layout-backup<slug>-<date>/`), so a `git add -A` can no longer stage it into the migration commit (closes the claudesk footgun in the tool, not just the operator's loop discipline).
- **Backlog resolved:** SURFACE-2026-07-21-QUALITY-TEST-MISSING-BACKUP-NOT-STAGED-ASSERTION — the same refactor added test assertions guarding it (backup not inside the project, git status clean of migration-backup, working tree clean after the migration commit, external backup still exists); suite grew 35→44.
- **Backlog resolved:** SURFACE-2026-07-21-QUALITY-HELP-FLAG-UNIMPLEMENTED — implemented the advertised `-h`/`--help` case (prints usage, exits 0, migrates nothing) + a test group; README updated.
- **Feature shipped:** Standalone `uninstall.sh` (M8) — a defensive, zero-Claudesk-dependency reversal of everything `install.sh` sets up: into-repo-guarded removal of the skill/agent/hook/claude-time symlinks (foreign links + real files at link paths are skipped), block-only excise of the marker-delimited `~/.claude/CLAUDE.md` block (backup first; non-block content survives), `--project`-gated removal of a project's harness memory *symlink* only (never the real store; realpath-safe slug via `tools/memory-link/lib-slug.sh`), and a print-only settings.json reminder — with `--dry-run`, `--help`, idempotent no-op re-runs, a 45-assertion E2E harness (`tools/uninstall/test/run-tests.sh`, `env HOME=<sandbox>` per-call isolation) covering the install→uninstall→re-install round-trip, and 6 dry-run-only `check-structure.sh` pins (420→426).
- **Backlog resolved:** SURFACE-2026-07-20-CLAUDESK-STANDALONE-UNINSTALL — closed by the uninstall-sh feature (the standalone, no-residue, Claudesk-independent uninstall it asked for; install.sh stays the single source of truth, only prints settings perms so uninstall symmetrically only prints).
- **Backlog resolved:** SURFACE-2026-07-21-UNINSTALL-TEST-HOME-EXPORT-HAZARD — closed in-cycle: the shipped test harness runs every install/uninstall invocation via `env HOME=<sandbox>` (never a top-level `export HOME`) and asserts the outer `$HOME` + live install are untouched, so the mid-build live-`~/.claude` damage cannot recur.
- **Milestone:** Milestone 8 — Standalone `uninstall.sh` + `install.sh` as single source of truth (WBS WP4, shipped).
- **Feature shipped:** Research cost-tier disambiguation (M10) — a 14-day audit of all 602 machine session logs (8 real research invocations) showed the `product-research`/`feature-research`/built-in `deep-research` names never misroute by *topic* (the model disambiguates by workflow layer), so the real bite is a *cost-tier jump* — an ambiguous "do some research" silently escalating into the heavyweight `deep-research` harness when a quick web lookup was wanted; fixed by a new standalone `quick-research` skill (fast web pass → per-claim HIGH/MED/LOW confidence labels + explicit known-unknowns list → an offer to escalate to `deep-research` that is GATED ON HUMAN CONFIRMATION and never auto-launched, even in autopilot), a `## Research cost tiers (GLOBAL)` rule in `CLAUDE.snippet.md`, orchestrator reinforcement in both `agents/{feature,product}-workflow/AGENTS.md`, sharpened-but-not-renamed `product-research`/`feature-research` descriptions, `tests/scenarios/research.yaml` (2 behavioral scenarios) + `check-structure.sh` [Phase 16] (11 pins, 438/0), plus an inline fix of a pre-existing Phase-15 failure (CLAUDE.md migration-mapping prose).
- **Backlog resolved:** SURFACE-2026-07-20-CLAUDESK-RESEARCH-SKILL-COLLISION — closed by the research-cost-tier-disambiguation feature; the collision is resolved by adding the missing light tier (`quick-research`) + a confirm-before-deep gate + sharpened workflow-scoped descriptions (no rename — the audit proved no topic misfire).
- **Milestone:** Milestone 10 — Resolve the "research" skill collision (WBS WP6, shipped).

## 2026-07-15

- **Feature shipped:** Delete-on-resolve backlog convention — terminal-close skills (`feature-finalize`, `task-close`, `incident-resolve`, `product-finalize`) now DELETE a resolved backlog entry on resolve (gated by a CHANGELOG-then-delete hard invariant: no delete without the `**Backlog resolved:**` line landing in the same commit) rather than marking it `Status: resolved`, making `workflow/backlog.md` + `workflow/backlog-quality-findings.md` open-work-only and CHANGELOG.md the sole resolved-item record; landed with the convention in `CLAUDE.snippet.md` + `arch.md` + project `CLAUDE.md`, 5 `check-structure.sh` [Phase 11] pins, 4 `*-delete-on-resolve` behavioral scenarios + fixture, and a migration that deleted the ~18 already-resolved entries backlogged before the convention existed.
- **Backlog resolved:** SURFACE-2026-07-14-RESOLVED-ENTRY-AUDIT-TRAIL-CLUTTER — closed by the delete-on-resolve-backlog-convention feature (the first entry ever deleted-on-resolve by the convention it shipped).

## 2026-07-14

- **Feature shipped:** WP6 of the backlog-paydown-2026-07-13 sweep — `tests/run-tests.sh` now honors two per-scenario fixture keys (`fixtures.claude_md` to pick a scenario-specific CLAUDE.md, and `budget` to override `--max-budget-usd`), backed by a new `CLAUDE-with-tracking-override.md` fixture, an `S20-global-override-tracked` scenario, and a `check-structure.sh` [Phase 3f] property-test; the 4 `DP-consult-*` scenarios were also rewritten to present their decision context neutrally so they test the loaded SKILL.md consult contract rather than prompt obedience.
- **Backlog resolved:** SURFACE-2026-06-25-PER-SCENARIO-CLAUDE-MD-FIXTURE — the runner now parses and honors the per-scenario `claude_md` key (with a `[-f]`-guarded fallback to the default fixture), unlocking per-scenario CLAUDE.md fixtures and the tracked-override behavioral test.
- **Backlog resolved:** SURFACE-2026-06-26-QUALITY-CONSULT-SCENARIOS-PROMPT-LEAKAGE — the 4 DP-consult scenarios no longer recite the expected answer in their prompt; verified 4/4 PASS on haiku driven by the skill prose, closing the over-infer-guard coverage weakness.

## 2026-07-13

- **Task closed:** WP0 of the backlog-paydown-2026-07-13 sweep — removed 5 already-resolved SURFACE blocks from `workflow/backlog.md` and 1 from `workflow/backlog-quality-findings.md` (plus collapsed a duplicate `disableClaudeAiConnectors` drift SURFACE into its 06-26 twin), leaving only genuinely open items in the active backlog.
- **Task closed:** WP1 of the backlog-paydown-2026-07-13 sweep — resolved 8 docs/skill-prose code-quality findings in one co-located bundle (arch.md util-*/debug-* heading-divergence note + stale-comment deletion, new `docs/lessons/README.md`, design-priors corpus Q1/Q2 marked resolved, a grammar fix, an ordering-note promotion, a debug-minimal-harness threshold clarification, and a fixture Phase→Milestone rename), with the structural suite confirming no pin regressed.
- **Backlog resolved:** SURFACE-2026-06-13-QUALITY-CATEGORY-HEADING-DRIFT — documented the intentional util-*/debug-* `## Category` vs `## Category Context` divergence in arch.md rather than renaming (which would have erased the category distinction).
- **Backlog resolved:** SURFACE-2026-06-13-QUALITY-LESSON-FILE-SCHEMA-AMBIGUOUS — wrote `docs/lessons/README.md` declaring the open lesson-file schema.
- **Backlog resolved:** SURFACE-2026-06-13-QUALITY-ARCH-INLINE-COMMENT-REDUNDANT — deleted the redundant same-day-edit HTML comment from arch.md.
- **Backlog resolved:** SURFACE-2026-06-26-QUALITY-CORPUS-OPEN-QUESTIONS-STALE — marked design-priors-corpus Q1/Q2 RESOLVED inline.
- **Backlog resolved:** SURFACE-2026-06-30-QUALITY-UTIL-PAYDOWN-GRAMMAR-AN-A — fixed the `An`→`A` grammar slip in the util-backlog-paydown Rule-1 parenthetical.
- **Backlog resolved:** SURFACE-2026-06-30-QUALITY-UTIL-PAYDOWN-ORDERING-NESTING — promoted the "risk outranks impact in ordering" clarification to a top-level note in both the skill and the lesson.
- **Backlog resolved:** SURFACE-2026-06-23-QUALITY-MINHARNESS-ROUND-THRESHOLD-NOTE — clarified the 3-round (inconclusive-escalation) vs 5-round (optional traceability) threshold distinction in debug-minimal-harness.
- **Backlog resolved:** SURFACE-2026-06-26-QUALITY-FIXTURE-USES-PHASE-ALIAS — renamed "Phase 1/2/3" to "Milestone 1/2/3" in the design-priors-consult roadmap fixture.
- **Task closed:** WP2 of the backlog-paydown-2026-07-13 sweep — extended `check-structure.sh`'s `strip_host_specific()` with a path-specific machine-local-key allowlist (fixing the long-standing Phase-7 settings-fixture drift) and tightened the container-down and propose-never-auto-write pins to their actual contract phrases; structural suite now fully green (354/0), with a negative test confirming repo-owned keys stay drift-checked.
- **Backlog resolved:** SURFACE-2026-06-26-SETTINGS-FIXTURE-DRIFT-DISABLECLAUDEAICONNECTORS — machine-local settings keys (`disableClaudeAiConnectors`, `tui`, `cleanupPeriodDays`, `statusLine.padding`, `env.CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC`) are now stripped path-specifically from both sides before drift detection.
- **Backlog resolved:** SURFACE-2026-06-19-QUALITY-CONTAINER-DOWN-PIN-OVER-BROAD — tightened the container-down structural pin from an over-broad OR-branch to the distinctive `Start the container(s) yourself` clause anchor.
- **Backlog resolved:** SURFACE-2026-06-26-QUALITY-PROPOSE-PIN-TOO-LOOSE — the Phase-13 capture-move pin now requires the full "propose … never … auto-write" contract phrase (tolerant of both the hyphenated and comma surface forms) instead of bare "propose".
- **Task closed:** WP3 of the backlog-paydown-2026-07-13 sweep — added a `[Phase 3a] Frontmatter YAML parseability` check to `check-structure.sh` that parses every SKILL.md/AGENTS.md frontmatter, which on its first run caught and drove the fix of a live latent bug (an unquoted inner-colon `argument-hint` in `feature-build/SKILL.md` that would have silently broken the skill registry); suite now 401/0.
- **Backlog resolved:** SURFACE-2026-06-13-CHECK-STRUCTURE-MISSING-YAML-PARSE-PIN — the new Phase 3a check validates all 47 skill/agent frontmatter blocks against `yaml.safe_load`, property-tested against both valid and deliberately-broken input.
- **Backlog resolved:** SURFACE-2026-06-13-QUALITY-YAML-PIN-PLACEMENT-NOTE — the check landed as Phase 3a (between Phase 3 and 3b), not a tail phase, keeping the Phase-11 close-commit block and PASS-count sequence undisturbed.
- **Task closed:** WP4 of the backlog-paydown-2026-07-13 sweep — renamed the design-priors consult heading in `product-roadmap`/`product-wbs` to `## Step 0: Product context + design-priors consult` (a distinct suffix, since the block does both product-doc-listing and the consult), reserving the pinned `## Step 0: Available product context` string for the six actual entry-point skills, and updated transitions.md to match; suite stays 401/0.
- **Backlog resolved:** SURFACE-2026-06-26-QUALITY-STEP0-ON-NON-ENTRY-SKILLS — the `## Step 0` heading-convention overload on the two non-entry-point planning skills is resolved by a suffix rename (no Phase-13 pin edit needed — the pins anchor on the `design-priors.md` substring, not the heading).
- **Task closed:** WP5 of the backlog-paydown-2026-07-13 sweep — captured the "odd-shape findings are a probe-more signal, not a ship signal" heuristic as a `feedback`-type project memory (with the self-applicable "this is the shape because…" test and its claudesk-WP2 origin), recording that the sibling verify-self-runner prompt-augmentation was intentionally deferred so it won't be re-proposed.
- **Backlog resolved:** SURFACE-2026-06-16-ODD-SHAPE-FINDINGS-PROBE-MORE-HEURISTIC — landed as a memory-only implementation (option a); the verify-self-runner enhancement (option b) is deferred per operator ruling.

## 2026-07-03

- **Feature shipped:** `session-reflect` filter rules — reflect now pre-filters and pre-scopes candidate learnings (drop workflow-commentary/single-observation/self-documenting; route already-captured insights to a cited "already-persisted" tier that carries no store decision; default surviving candidates to `[PROJECT]`, with `[GLOBAL]` gated behind a 3-part workflow-mechanism test and an mccc carve-out) and presents them in three tiers, so the operator stops manually pruning ~3–4× per session; derived from an audit of all 145 real `session-reflect` invocations across 8 projects, pinned by 6 structural `grep_check`s and behavioral scenarios R1/R2/R3 (R1 a real logged `[GLOBAL]`→`[PROJECT]` re-scope case).
- **Feature shipped:** project-memory location symlink — a project's harness memory store (`~/.claude/projects/<slug>/memory`) is now a symlink to the git-tracked repo dir (`<proj-dir>/.claude/memory/`), so memories are both version-controlled and auto-loaded at session start from one physical copy; ships a reusable `tools/memory-link/` primitive (idempotent realpath-safe ensure-link + one-time merge/drift-keep-both/backup migration + 27-assertion test suite), wires the ensure-link check into both `product-context` and `session-start` (closing the non-product reachability gap), codifies a `.claude/`-tracked-by-default gitignore convention, and ran a one-time migration across 8 `docs/product/`-bearing projects (2 blanket-ignore projects fixed); 13 new check-structure Phase-14 pins.
- **Backlog resolved:** SURFACE-2026-07-03-MEMORY-LOCATION-SYMLINK — closed by the memory-location-symlink feature (Direction A symlink shipped; spike passed; system-wide migration done; convention codified and wired into two hosts).

## 2026-06-30

- **Feature shipped:** `util-backlog-paydown` — a standalone `util-*` operator skill that runs a between-milestone backlog-paydown sweep, scoring the standing code-quality/debt backlog on a 3-axis disposition model (Impact / Effort / Risk) and assigning one of five actions (Sweep / Discuss / Defer / Bury / Delete) with operator-veto as a first-class trigger, then emitting a priority/risk-ordered `shape: temporary-wbs` (fold-back-and-delete, no roadmap slot); resolved via cross-validation against two real sessions (Claudesk debt sweep; replicator-1-0 five-sweep family) whose dispositions the model predicted ~100% of, wired as a util-* skill (no transition, no orchestrator change, no new structural pin per the documented status quo) with an advisory non-chaining pointer from `product-finalize` and a new `tests/scenarios/util.yaml`.
- **Task closed:** Renamed the two drive-mode names across the state machine — `full-autopilot` → `fsd` (display "FSD") and `step-by-step` → `stepping` (display "Stepping") — touching `drive_mode:` frontmatter values, Mode 1/Mode 4 table headers, menu/prose display names, scenario assertions, and the renamed `feature-shipped-fsd.md` fixture; old names kept as accepted input synonyms in `session-start` and generic "step-by-step recipe" prose left untouched, with util-* skills' separate Step-by-step label deliberately unchanged.
- **Incident resolved:** `session-store-learning` proposed the wrong project-scope file (`<proj-dir>/.claude/CLAUDE.md`) for a code-contract Context Rule that belonged in the project-root `<proj-dir>/CLAUDE.md` — fixed by correcting contradictory SKILL.md prose at four sites and adding a read-before-propose gate (never route a learning to a file the skill has not opened), pinned by new scenario `S25-project-context-rule-root-claude-md` (tagged `model: sonnet`).

## 2026-06-26

- **Feature shipped:** Design Priors — a per-project `docs/product/design-priors.md` recording the operator's product-design decision leans (focus-vs-breadth, perf-vs-ship, anti-persona, …) with an inferred-why + corrected-why gap; planning skills (product-roadmap/wbs, feature-spec) consult it under five weighting rules including an over-infer guard so the 90% common-sense path stays untouched, and six checkpoints + a session-reflect backstop propose new priors (propose-never-auto-write, operator reviews the why), enforced by `check-structure.sh [Phase 13]` (+33 pins, 303→334) and 7 behavioral scenarios `tests/scenarios/product.yaml::DP-*`, with a `pre-design-priors` git tag + grep-enumerable revert recipe as an easy-rollback net.

## 2026-06-25

- **Incident resolved:** Autopilot AUTO transitions intermittently paused via `AskUserQuestion` after the Opus 4.8 upgrade — root cause was an incomplete AUTO-exit prohibition (the Hard rule named only the passive narrative-summary stop, not the active `AskUserQuestion` stop); mitigated by explicitly forbidding `AskUserQuestion` and any user-input tool on AUTO transitions across 11 SKILL.md cheat-sheets + 4 orchestrator AGENTS.md, codified with 13 structural pins + the `F8-autopilot-no-askuser` scenario (check-structure 290/0).
- **Backlog resolved:** SURFACE-2026-06-23-SETTINGS-FIXTURE-DRIFT-CLAUDESK-HOOK — closed by commit 93677f0, which strips host-specific claudesk hooks from both live and fixture via `strip_host_specific()` before the Phase 7 drift diff, keeping the repo-owned claude-time hook fully checked.
- **Feature shipped:** Deterministic artifact tracking policy — codified a GLOBAL track-by-default rule + canonical track/ignore MAP + per-project override mechanism in `CLAUDE.snippet.md`, made `session-store-learning` a policy-follower (one canonical learnings path, git behavior keyed on the project's CLAUDE.md override instead of gitignore inspection), mandated explicit `~/.claude/` vs `<proj-dir>/.claude/` path qualification across all skill prompts, gave `product-context` ownership of once-per-project `.gitignore` reconciliation, and switched `session-reflect` to a leading `[GLOBAL]`/`[PROJECT]` scope label — closing the non-determinism behind learnings landing in inconsistent locations across sessions (+13 structural pins, 303/0).
- **Backlog resolved:** SURFACE-2026-06-25-TRACK-CLAUDE-DIR-AND-LEARNINGS-MEMORIES-CONVENTIONS — closed by the artifact-tracking-policy feature, which reconciled the broken gitignore-based scope discriminator and documented the tracking policy + path-qualification mandate as durable conventions.

## 2026-06-24

- **Task closed:** Removed the Telegram notification hook — deleted `hooks/notify-telegram.sh`, unwired it from live `~/.claude/settings.json` (hook entry, env vars, curl permission), stripped its `install.sh`/`check-structure.sh`/`run-tests.sh`/fixture wiring, and dropped the Telegram mentions from `CLAUDE.md`, `README.md`, and three SKILL.md prose hints; claudesk and claude-time hooks left intact.
- **Backlog resolved:** SURFACE-2026-06-16-TELEGRAM-NOTIFY-FIRES-ON-BACKGROUND-AUTOBACKGROUND — removing the hook eliminates the spurious Telegram pings on auto-background entirely, so the payload-discriminator filter is no longer needed.
- **Task closed:** Phase 7 settings-drift check now strips host-specific claudesk hooks from both live and fixture before diffing (via `strip_host_specific()`) instead of excluding whole hook events, restoring full drift detection on the repo-owned claude-time hook (`UserPromptSubmit` is now exactly diffed; a broken claude-time command FAILs Phase 7 — verified) while keeping the test harness and the claudesk wrapper app fully decoupled.

## 2026-06-23

- **Feature shipped:** `debug-minimal-harness` — a third `debug-*` sidebar skill that, when a behavioral fix (drag/click/focus/keyboard, CLI under real argv/stdin, HTTP under a real client, a race under real concurrency) has been handed back untested ≥2× on the same behavior AND that behavior is drivable in a surface the agent controls (even when the shipping target is native), has the agent stop handing back and instead build a minimal standalone reproduction and drive it with REAL input — not synthetic dispatch, which false-passes by masking pointer-capture/focus/hit-test/re-render bugs — before re-presenting to the human; codifies the claudesk M4 WP3 drag-reorder learning, wired across all three caller skills + three orchestrator AGENTS.md + transitions.md with 16 new structural pins (check-structure.sh 297/1) and 3 behavioral scenarios.

## 2026-06-19

- **Feature shipped:** `docker-daemon-vs-container-distinction` — refined the Variant-A generated-CLAUDE.md Docker rule in `skills/product-context/SKILL.md` to distinguish two cases the old single line conflated: daemon UNREACHABLE (`docker ps` errors → STOP and ask the user, no host-OS fallback — the hard-blocker, unchanged) versus daemon REACHABLE but the project's containers DOWN (`docker ps` exits 0 → start the container(s) yourself with `docker compose up -d` / `up --build`, wait for healthy, resume — do NOT pause), codified by two new `grep_check` structural pins mirroring the randomize-host-ports precedent (suite 269/0); the change originated from a 2026-06-19 replicator-1-0 learning where "container down" was misread as the daemon-unreachable hard-blocker, wasting a round-trip.

## 2026-06-18

- **Feature shipped:** `milestone-terminology-and-wbs-scope` — the product roadmap's strategic decomposition unit is renamed from "Phase" to "Milestone" (flat singly-numbered, cosmetic "Group" headings, "phase" kept as a backward-compat read-alias), `product-wbs` now decomposes only the immediate next milestone instead of the whole roadmap, and the rename was applied across all 9 product-workflow files plus the tripartite-sync third leg in `transitions.md` while the feature Work Tree's separate "Phase" schema was deliberately left intact with a new disambiguation note in `CLAUDE.md`.
- **Backlog resolved:** SURFACE-2026-06-18-PRODUCT-SKILLS-MILESTONE-TERMINOLOGY-AND-WBS-SCOPE — closed by feature `milestone-terminology-and-wbs-scope` (all three rules applied, +13 structural pins locking terminology, flat numbering, next-milestone scope, and the feature-Work-Tree-Phase-untouched invariant).

## 2026-06-13

- **Task closed:** `quality-findings-dedicated-file` — redirected `feature-review-quality` auto-backlog writes (Case-B Mode-3 MAJOR + Case-C Modes-2-3 MINOR) from `workflow/backlog.md` to a dedicated `workflow/backlog-quality-findings.md` file, grouped by source feature under `# <feature-name> — <YYYY-MM-DD>` sections, with exactly one pointer entry per feature in main `backlog.md`. Three prose edits: `skills/feature-review-quality/SKILL.md:161` (Case-B with idempotency-on-re-run clause for the F40 back-loop case), `:164` (Case-C cross-references Case-B's idempotency note), and `agents/code-quality-reviewer/AGENTS.md:18` (informational reference updated for accuracy — the agent emits text, the skill writes the file). Canonical shape already existed (hand-built 2026-06-12 during the close-commit-discipline finalize hotfix); this task wires the auto-write to it. No test scenarios needed — scenarios test transition emission, not write-target paths. Structural verification: 4 literal-string grep assertions all PASS; `./tests/check-structure.sh` 232 PASS / 2 known-baseline FAIL (no new failures). Bite-verify (re-run review-quality on next feature ship; main backlog volume +1 pointer not +N findings) is the deferred end-to-end confirmation. Plan executed exactly as drafted, zero back-loops.
- **Backlog resolved:** SURFACE-2026-06-12-ADJUST-QUALITY-AGENT-USE-DEDICATED-FILE — closed by task `quality-findings-dedicated-file`. Main `workflow/backlog.md` volume per future `feature-review-quality` pass drops from +N findings to +1 pointer entry; full content lives in `workflow/backlog-quality-findings.md` grouped by source feature. The collapsed-pointer shape established yesterday by the close-commit-discipline finalize hotfix is now the auto-write target, not an ad-hoc operator-grep cleanup.
- **Feature shipped:** `verify-sh-contains-required` — added `contains_required` (AND-fanout) and `contains_required_any` (OR-fanout) fields to the test harness's `verify_result` function, enabling test scenarios to hard-assert content presence in `result_text` even when `transition_id` matches (closes the SOFT_PASS-fallback gap where `contains_any` was only consulted as a fallback). Pilot scenario `P10b` in `tests/scenarios/product.yaml` exercises the primitive against a real haiku invocation that surfaces the randomize-host-ports guidance under Variant A; mutation-verified mid-feature (removed SKILL.md bullet → P10b FAILed with exact wording `"Structured match on P10 but required-any content missing — none of: ephemeral|49152|randomize"` → restored → PASS again — confirming the primitive is mechanically biting, not trivially-true). Implementation: 2 phases (harness primitive + plumbing → pilot scenario + doc), commit `e0c2917`, 8 files / +277 / -11. Backward-compatibility structurally guarded: empty-string defaults in the 7th + 8th positional args preserve all prior scenario behavior, with Phase 3e's 13 new unit cases (A-M) pinning both the new AND/ANY semantics and the prior `transition_id_any` + `not_contains_strict` + `contains_any` fallback semantics; Phase 2 added 4 grep_check pins for the scenario+doc contract. Reviewer subagent post-ship verdict: 0 CRITICAL / 0 MAJOR / 3 MINOR (all auto-backlogged to `workflow/backlog-quality-findings.md`, documentation-polish nits only). Zero back-loops across both phases; mirrors the structural precedent of the 2026-05-06 `transition_id_any` + `not_contains_strict` extension. `CLAUDE.md` `## Conventions` bullet at line 227 documents both new fields with the AND vs ANY distinction + pointer to the P10b pilot use for future contributors.
- **Backlog resolved:** SURFACE-2026-06-06-VERIFY-SH-NO-HARD-CONTENT-ASSERT — closed by feature `verify-sh-contains-required`. The harness limitation that made `contains_any` only fire as SOFT_PASS fallback (twice-bitten 2026-06-06 during the codify-randomize-host-ports-test-coverage task's P10b attempts) is resolved by the new `contains_required` / `contains_required_any` primitive, which enforces content presence even on `transition_id` match. The convention surface in `CLAUDE.md` is updated so future authors writing content-presence assertions know which field to reach for; the integration-boundary rule's "verify-codify must include a test on the consuming surface" presupposition is now actually achievable for prose-emission contracts (P10b is the worked example).
- **Task closed:** `phase-3d-regex-test-misses-tr-prefix` — added the missing `tr -d '*'` segment to `regex_test` in `tests/check-structure.sh` so the Phase 3d property test mirrors verify.sh's production pipeline (`tr -d '*' | sed -n …`). Also applied symmetrically to `regex_test_negative` on the same file (no-op for current negative cases but keeps both helpers pipeline-faithful). Two previously-perpetual FAILs (`regex: markdown bold (F1)`, `regex: hyphenated debug token with markdown bold (SKIP)`) now PASS; full structural sweep reports 251 PASS / 0 FAIL. Single-line `replace_all` edit; zero back-loops.
- **Backlog resolved:** SURFACE-2026-06-12-PHASE-3D-REGEX-TEST-MISSES-TR-PREFIX — closed by task `phase-3d-regex-test-misses-tr-prefix`. The property test now matches the production pipeline's full shape; the 2 perpetual FAILs flagged at the SURFACE filing time are converted to PASS, so `./tests/check-structure.sh`'s clean-baseline assertion no longer requires the operator to mentally subtract 2 known-baseline FAILs.
- **Task closed:** `sweep-quality-findings-2026-06-13` — combined sweep of 8 MINOR code-quality findings auto-backlogged by `feature-review-quality` across two source features (close-commit-discipline 2026-06-12, 5 findings; verify-sh-contains-required 2026-06-13, 3 findings). Six findings resolved via code/doc edits: product-finalize §6b operational-sequence paragraph restructured into a 4-step numbered list matching the other 3 close skills' shape (T6); 4 Phase 11 no-push grep pins tightened from `"Do NOT.*git push"` to `"Do NOT \`git push\`"` (T5); 4 no-push test scenarios annotated with `# Part of the no-push family` family-marker comments (T7); S20-amend-head's `contains_any` deduplicated by dropping `"--amend"` (T2); Phase 3e header comment range updated `G-K → G-M` to reflect cases L+M added during verify-sh-contains-required (T1); `verify_result` docstring annotated with `(The \`_csv\` suffix on multi-value args is historical — separator is the pipe \`|\`, not comma.)` to close the documentation hazard without the larger local-variable rename (T3); 2 CLAUDE.md grep pins simplified from `"X:.*Y|Y.*X:"` alternation to `"X.*Y"` form (T4). Two findings closed as documented decisions: 4-way no-push prose duplication left as-is (SURFACE's suggested action (a); Phase 11 pins enforce contract mechanically), `_csv` arg full-rename deferred (option (b) parenthetical took the smaller blast radius). `./tests/check-structure.sh`: 251 PASS / 0 FAIL preserved at commit `6ee10b8`. 10 files / +19 / −103 (the negative line count comes mostly from `workflow/backlog-quality-findings.md` clearing the 2 resolved sections). Plan executed exactly as drafted, zero back-loops; combined-sweep shape avoided the per-finding overhead of 8 individual task cycles.
- **Backlog resolved:** SURFACE-2026-06-12-QUALITY-NO-PUSH-CLAUSE-DUPLICATED-4X — closed by task `sweep-quality-findings-2026-06-13` via the SURFACE's own suggested action (a): leave the 4-way no-push prose duplication as-is. Phase 11 structural pins (now tightened by T5 to literal `Do NOT \`git push\``) guarantee the contract holds mechanically across all 4 close skills; snippet extraction to `CLAUDE.snippet.md` would cost more (4 SKILL.md edits + injection logic) than the maintenance value of DRY across 4 nearly-identical paragraphs at this borderline count. Revisit if a 5th close skill ever joins the family.
- **Backlog resolved:** SURFACE-2026-06-12-QUALITY-PRODUCT-FINALIZE-NO-PUSH-FORMAT-ASYMMETRY — closed by task `sweep-quality-findings-2026-06-13`. `skills/product-finalize/SKILL.md` §6b's "Operational sequence" is now a 4-step numbered list with the no-push clause as step 4, matching the other 3 close skills' numbered-list shape (feature-finalize §3c, task-close §6, incident-resolve §4b). Visual symmetry across the four close skills restored.
- **Backlog resolved:** SURFACE-2026-06-12-QUALITY-PHASE-11-GREP-PATTERN-LOOSE — closed by task `sweep-quality-findings-2026-06-13`. The 4 Phase 11 grep pins now use literal `"Do NOT \`git push\`"` (with escaped backticks in the bash-quoted regex argument) instead of the loose `"Do NOT.*git push"` form. The tighter pattern rejects hypothetical inverted constructions (e.g. "Do NOT skip the git push step") and documents intent more clearly at the grep-call site.
- **Backlog resolved:** SURFACE-2026-06-12-QUALITY-S20-CONTAINS-ANY-REDUNDANT-ANCHOR — closed by task `sweep-quality-findings-2026-06-13`. `tests/scenarios/session.yaml` S20-amend-head's `contains_any` no longer carries both `"--amend"` and `"git commit --amend"` — kept only the more specific latter, since any string containing it also contains the prefix. Scenario behavior unchanged; one less redundant anchor for future scenario-template readers.
- **Backlog resolved:** SURFACE-2026-06-12-QUALITY-NO-PUSH-SCENARIO-FAMILY-COMMENT — closed by task `sweep-quality-findings-2026-06-13`. The 4 `*-no-auto-push` scenarios (`feature.yaml::F19-no-auto-push`, `task.yaml::T10-no-auto-push`, `incident.yaml::I10-no-auto-push`, `product.yaml::P13-no-auto-push`) each carry a `# Part of the no-push family — keep contains_any in sync across F19/T10/I10/P13-no-auto-push` trailing comment, surfacing the cross-scenario relationship at the grep-call site for future scenario authors.
- **Backlog resolved:** SURFACE-2026-06-13-QUALITY-PHASE-3E-HEADER-CASE-RANGE-STALE — closed by task `sweep-quality-findings-2026-06-13`. `tests/check-structure.sh:378` Phase 3e header comment now reads "Forward cases (G-M) pin the new AND/ANY behavior" — matches the 7 forward cases (G-M) actually implemented and the verify-codify note's "13 vr_check unit cases (A-M)" count.
- **Backlog resolved:** SURFACE-2026-06-13-QUALITY-VERIFY-RESULT-CSV-SUFFIX-MISLEADING — closed by task `sweep-quality-findings-2026-06-13` via option (b) of the suggested action: parenthetical clarification on the `verify_result` function-signature comment in `tests/lib/verify.sh:5` ("The `_csv` suffix on multi-value args is historical — separator is the pipe `|`, not comma."). Option (a) full `_csv → _list` rename deliberately not taken — the suffix appears not only on the 4 new arg names but also on 2 pre-existing args used as local-variable names inside `verify_result`'s function body, making a full rename a code refactor rather than a docstring touch-up. The parenthetical closes the documentation-hazard surface without changing the function signature.
- **Backlog resolved:** SURFACE-2026-06-13-QUALITY-CLAUDE-MD-GREP-ALTERNATION-OVERSPECIFIED — closed by task `sweep-quality-findings-2026-06-13`. The 2 CLAUDE.md grep pins at `tests/check-structure.sh:1040-1043` now use the simpler `"contains_required.*AND-fanout"` and `"contains_required_any.*OR-fanout"` patterns (dropped the alternation + colon anchor). Both pins still PASS against the current CLAUDE.md bullet text on the documented one-line shape; the simplified form is easier to read at the grep-call site.
- **Task closed:** `arch-resync-2026-06-13` — added a `## Revision 2026-06-13` section to `docs/product/arch.md` capturing ten architecturally-significant additions that had shipped between the prior 2026-05-02 revision and today: drive modes (1–4 with pause-policy contract), executable-subagent pattern (`tools:` frontmatter marker distinguishing executable subagents from reference-only orchestrator agents, enforced by `tests/check-structure.sh` Phase 10), `debug-*` skill category (agent-pulled sidebars with `DEBUG-<TECHNIQUE>-<OUTCOME>` tokens + `RETURN-TO:` convention, required sections enforced by Phase 3b/3c), per-phase verify-loop extension with `verify-self` (BLOCKING/COSMETIC severity taxonomy + in-place fix shortcut), `task-verify` single-step gate (T5a/T5b/T5c with `docs-only: true` auto-skip), `feature-review-quality` post-ship reviewer subagent (F38–F41 + F17b Mode-4 SKIP; severity-tier action matrix CRITICAL→refactor / MAJOR→pause-or-backlog / MINOR→auto-backlog), `incident-codify` (I19 with speed-aware Paths A/B + defer), `feature-reproduce` (S18 red-green pre-spec/plan), close-commit discipline (no-auto-push + session-store-learning amend-to-HEAD, enforced by Phase 11), CHANGELOG.md convention (terminal-close auto-append contract), and Telegram Notification/Stop hook replacing the model-driven `notify-human` skill. Each subsection ≤8 lines, points to the authoritative source (`transitions.md` / `CLAUDE.md` / SKILL.md path) without restating procedure. Frontmatter `updated:` bumped 2026-04-25 → 2026-06-13. Pure docs-only edit (`docs-only: true` declared at plan time, task-verify auto-skipped); zero back-loops.
- **Feature shipped:** `claude-md-compaction` — compacted the project-root `CLAUDE.md` from 54,528 → 35,720 chars (−34.5%, clears Claude Code's 40k harness performance threshold with 4,280 chars headroom) by deleting one HISTORICAL bullet ("v3 sub-payload routing pattern", retired 2026-06-03) and externalizing 11 multi-paragraph convention bullets into 6 new `docs/lessons/*.md` files (`downstream-contract-impacts`, `debug-skill-template`, `test-scenario-routing-forks`, `test-harness-sweep-discipline`, `harness-bootstrap-skip`, `work-tree-leaf-substitution`) following the existing 3-lesson precedent shape; each externalized bullet replaced by a one-line `See \`docs/lessons/<topic>.md\`.` pointer in CLAUDE.md. Also shipped: a new file-based standalone utility skill `skills/util-prune-claude-md/SKILL.md` (4-mode menu mirroring workflow drive modes 1–4, scope-locked to project-root CLAUDE.md, refuses to touch global `~/.claude/CLAUDE.md`) that the operator can re-invoke whenever the 40k warning fires, plus a `### util-* skill category` subsection in `docs/product/arch.md` Revision 2026-06-13 formalizing util-* as a third skill category distinct from workflow skills (no state machine, no transitions) and `debug-*` sidebars (no `RETURN-TO:`, not caller-pulled). Mid-feature scope adjustments preserved in WIP audit trail: Phase 3 (structural `wc -c ≤ 40000` pin in `tests/check-structure.sh`) built then SKIPPED at operator direction during verify-human — the harness self-surfaces the warning on every session start, so an extra pin adds friction without signal; Phase 4 first attempt (embed prune-cadence prompt in `feature-finalize` §1) built and verify-self-PASSed then reverted at operator direction during verify-human, replaced with the demand-driven standalone util-* skill. 8 DEDUP candidates parked at audit-time as recovery lever (not needed — EXTERNALIZE alone cleared the threshold with comfortable margin). Reviewer subagent post-ship verdict: 0 CRITICAL / 0 MAJOR / 4 MINOR (all auto-backlogged to `workflow/backlog-quality-findings.md` under `# claude-md-compaction — 2026-06-13`, plus one new gap SURFACE for missing YAML-frontmatter-parseability check in `tests/check-structure.sh` filed as P1 TODO). Drive mode switched three times mid-feature (step-by-step → orchestrated → autopilot) — each switch operator-initiated, persisted via WIP frontmatter `drive_mode:` field.

## 2026-06-12

- **Feature shipped:** `verify-self-and-review-quality-subagent-dispatch` — added two new executable subagent definitions at `agents/feature-verify-self-runner/` and `agents/code-quality-reviewer/` (the first executable subagents in the repo, distinguished from the 4 reference-only `*-workflow` orchestrator agents by `tools:` frontmatter marker), and wired both calling skills (`skills/feature-verify-self/SKILL.md` §2 + `skills/feature-review-quality/SKILL.md` §2) to invoke their subagent by `subagent_type` name with unconditional-spawn prose + bootstrap-skip fallback (commit `737af32`, 12 files, +713/−61). The reviewer-prompt body moved from `skills/feature-review-quality/reviewer-prompt.md` (now deleted) into `agents/code-quality-reviewer/AGENTS.md` so the agent definition IS the prompt. New test coverage: Phase 10 "Subagent dispatch wiring" structural-pin block in `tests/check-structure.sh` (self-extending iteration over `agents/*/AGENTS.md` selecting executable subagents by `tools:` frontmatter — adding the third subagent picks up coverage automatically; assertions: name matches dir + description present + tools non-empty + ≥1 skill references + cross-skill `subagent_type:` literal present; **+10 PASS, 200 → 210**); new behavioral scenario `F10b-dispatches-subagent-by-name` in `tests/scenarios/feature.yaml` with new fixture `tests/fixtures/wip/verify-self-cli-verifiable-phase.md` (the kind of CLI-verifiable-shape phase that historically tempted inline dispatch — **PASS strict on haiku, 17s, $0.14, first attempt**). Docs: `CLAUDE.md` "Two kinds of artifacts" section rewritten to distinguish reference-only (`skills:` frontmatter) from executable (`tools:` frontmatter) agents with Phase 10 pin as the enforcement surface; `docs/product/transitions.md` + `CLAUDE.md` `feature-review-quality` bullet updated with forward-pointers to the new agent location. Verify-codify sweep of the 9 edited-skill scenarios on haiku: **8/9 PASS strict + 1 historical SOFT_PASS** on F-boundary-self (confirmed unrelated via prior-run history). 3 phases (subagent definitions → skill wiring → pins/scenario), zero back-loops (F23/F12/F9b/F22/F36 none fired). 3 mid-feature Discoveries surfaced and operator-approved at verify-human: (1) **Agent registry has the bootstrap-skip pattern** (P6 extended); (2) **the 4 reference-only `*-workflow` agents are unintentionally invokable as `subagent_type`** (new P7); (3) **Skill SKILL.md content is also cached at session start, not just the registry** (P6 extended again to cover mid-session edits). Origin: SURFACE-2026-06-11-VERIFY-SELF-AND-REVIEW-QUALITY-SUBAGENT-DISPATCH-UNVALIDATED (operator-flagged at code-quality-reviewer-subagent reflect: "I actually don't think verify-self currently really spawns a subagent" — confirmed via telemetry audit of 79 archived WIPs, then root-cause-reframed by operator mid-spec: the spawn target didn't exist as a subagent definition). **First real F38 → review-quality invocation in repo history** completed at finalize-prep time, with bootstrap-skip on the named `code-quality-reviewer` subagent (registry loaded at session start) cleanly falling back to `general-purpose` per the new SKILL.md prose — empirical validation that the bootstrap-skip fallback works as designed; the feature dogfooded itself end-to-end.
- **Backlog resolved:** SURFACE-2026-06-11-VERIFY-SELF-AND-REVIEW-QUALITY-SUBAGENT-DISPATCH-UNVALIDATED — closed by feature `verify-self-and-review-quality-subagent-dispatch`. The 2026-04-27 arch.md "verify-self runs as a subagent — parent context stays lean across multi-phase features" design property is restored from a discipline norm into a structurally-enforced contract. Subagent dispatch is now codified at three layers: the agent definitions exist on disk, the skill prose names the spawn target by `subagent_type`, and the wiring is regression-gated by both a self-extending structural pin block AND a runtime-validated behavioral scenario.
- **Task closed:** `subagent-dispatch-back-reference-pin` — extended `tests/check-structure.sh` Phase 10 "Subagent dispatch wiring" with a new (f) back-reference pin: for every `subagent_type: '<name>'` reference in `skills/*/SKILL.md`, the pin asserts `agents/<name>/AGENTS.md` exists AND its frontmatter carries `tools:` (executable-subagent marker), NOT `skills:` (reference-only marker). One PASS per reference site; `general-purpose` is allowlisted as a harness-built-in fallback (used by bootstrap-skip prose in `feature-verify-self` and `feature-review-quality` §2, not backed by an in-repo agent file). PASS count 210 → 214 (+4: 2 references × 2 dispatch-aware skills, all pointing at executable subagents). Bite-verified: mutating `feature-verify-self`'s `subagent_type: 'feature-verify-self-runner'` → `'feature-workflow'` (a `skills:`-only orchestrator agent) produces an informative FAIL that names the offending skill, the offending target file, and the SURFACE-ID for traceability; revert restored 214/0. Single-file edit (~44 lines including comment header + allowlist case statement) in `tests/check-structure.sh`; runtime registry bumped to 43s/125000ms. Plan-time miss surfaced during act: the initial pin form had no allowlist and FAILed on `general-purpose` references — resolved by case-statement allowlist before the file-exists check, no formal back-loop. Closes the symmetric-enforcement gap that the parent feature (`verify-self-and-review-quality-subagent-dispatch`) shipped with — the (a)-(e) assertions enforced "every executable subagent must be referenced" but did not enforce "every reference must point at an executable subagent." The category convention codified yesterday in CLAUDE.md ("Category-level conventions need the harness's own marker, not just a documentation marker") is now load-bearing structurally, not just narratively.
- **Backlog resolved:** SURFACE-2026-06-12-QUALITY-SUBAGENT-DISPATCH-PIN-ASYMMETRIC — closed by task `subagent-dispatch-back-reference-pin`. The Phase 10 (f) back-reference pin is the structural enforcement side of the asymmetric-enforcement gap that the `verify-self-and-review-quality-subagent-dispatch` feature shipped with; the convention is now symmetric in both directions (forward: every exec-subagent must be referenced; back-reference: every reference must point at an exec-subagent).
- **Backlog resolved:** SURFACE-2026-06-12-REFERENCE-WORKFLOW-AGENTS-ARE-INVOKABLE — closed by task `subagent-dispatch-back-reference-pin` via path (i) of the suggested action. The 4 reference-only `*-workflow/AGENTS.md` orchestrator files remain registered as invokable `subagent_type` values by the harness (the latent risk root cause is unchanged), but any skill writing `subagent_type:` pointing at them now FAILs the new pin with an informative message. Path (ii) (rename `*-workflow/` directories to a non-`agents/` location to remove the latent risk at the source) deliberately not taken — the lower-effort pin closes the risk surface without restructuring.
- **Task closed:** `duplicate-verify-codify-leaves-convention` — codified the verify-codify leaf-substitution discipline as a new `## Conventions` bullet in `CLAUDE.md`: at leaf-completion time (verify-codify or any other), the `- [ ] verify-codify  <!-- status: NOT-STARTED -->` line must be **substituted** with the completed form, NOT appended above. Empirical anchor: 2026-06-12 code-quality-reviewer MAJOR finding #1 against Phases 2+3 of the `verify-self-and-review-quality-subagent-dispatch` WIP (duplicate leaves left both phases technically NOT cleanly closed per the Work-Tree parent-completion rule). Operator-grep discipline at completion time; option (b) structural pin scanning WIP archives deferred — picks up if recurrence rate justifies it. Same commit (`d4be9c6`) bundled the 2 orphan L1+L2 `## Conventions` bullets written to disk by yesterday's `/session-store-learning` but never committed.
- **Backlog resolved:** SURFACE-2026-06-12-QUALITY-WIP-DUPLICATE-VERIFY-CODIFY-LEAVES — closed by task `duplicate-verify-codify-leaves-convention` via option (a) of the suggested action. The new CLAUDE.md `## Conventions` bullet establishes the leaf-substitution discipline as operator-grep convention; option (b) (structural pin scanning WIP archives for ≥2 verify-codify lines under one Phase node) deliberately not taken — the doc-only path is sufficient unless recurrence rate justifies the pin-scanning logic.
- **Task closed:** `debug-within-skill-structural-pins` — extended `tests/check-structure.sh` Phase 3b's `for debug_skill in skills/debug-*/SKILL.md` iterating loop with 7 new `grep_check` calls per skill, covering the 4 not-previously-pinned required SKILL.md sections (`## Category Context`, `## Procedure`, `## Pitfalls`, `## Termination`), the `argument-hint:` frontmatter field, the Gate-Check-first-`### 1.`-subheading-under-Procedure rule, and the ≥4 `DEBUG-<TECH>-<OUTCOME>` termination-token regex. Self-extending — any future debug-* skill picks up all 7 new pins automatically. **PASS count 214 → 228 (+14: 7 pins × 2 debug-* skills, both `debug-bisect-known-good` and `debug-empirical-telemetry` already satisfied the conventions so all pins PASS clean)**. Bite-verified by mutating `## Category Context` → `## Category Contextual` in debug-bisect-known-good — observed one informative FAIL naming both the offending skill and the pin description; reverted. Plan-time pin-count estimate (+5 per skill) under-counted by 2 due to natural splits at implementation time (Termination heading vs. token regex; argument-hint frontmatter vs. Gate-Check subheading); not a back-loop. CLAUDE.md "reusable template" math updated from "+9 PASS per new debug-* skill" to "+16 PASS per new debug-* skill" to reflect the new Phase 3b auto-iterate count.
- **Backlog resolved:** SURFACE-2026-06-10-DEBUG-WITHIN-SKILL-STRUCTURAL-PINS — closed by task `debug-within-skill-structural-pins`. The category-level SKILL.md convention (6 required sections, `argument-hint:` frontmatter, Gate-Check-first-subheading, ≥4 termination tokens) is now structurally pinned per debug-* skill — future regressions on any of the 7 new properties will FAIL `check-structure.sh` at PR time. Self-extending: no per-skill bespoke code needed when a third debug-* skill is added; the loop picks up coverage automatically.
- **Task closed:** `sonnet-hygiene-and-telemetry-inconclusive-scenario` — landed two sonnet-side scenario fixes in `tests/scenarios/feature.yaml` (F16-triage-ambiguous + F16-triage-flaky, both PASS strict on sonnet after `transition_id_any: [F16, F14]` + `model: sonnet` tag — the SOFT_PASSes had a shared root cause: `feature-verify-codify`'s §5 enumerates only F15/F16/F14 with no canonical token for triage-pause classifications, so sonnet legitimately routes them as either F16 (verify-codify complete) or F14 (back-loop to verify-human)) plus one new behavioral scenario `DEBUG-TELEMETRY-INCONCLUSIVE` in `tests/scenarios/debug.yaml` with new fixture `tests/fixtures/wip/debug-empirical-telemetry-inconclusive.md` (depicts 3 prior telemetry rounds — dequeue boundary / release_lock / downstream sink — with hypothesis + observation + ruled-out conclusion per round, foreclosing a 4th round). The new scenario ships as SOFT_PASS lenient (3 strict-PASS attempts cost $0.25 total; sonnet wraps the TRANSITION token in markdown decoration that breaks the harness regex's alnum-hyphen capture class) — lenient coverage is harness-supported via `contains_any` fallback. End-to-end verify (`./tests/run-tests.sh --id F16-triage-ambiguous,F16-triage-flaky,DEBUG-TELEMETRY-INCONCLUSIVE --model sonnet`): TOTAL 2 PASS + 1 SOFT + 0 FAIL, 68s, $0.29. Plan-time discipline learned: when two scenarios have nearly-identical SOFT_PASS shapes under the same skill (both triage-pause classifications), assume the dual-identity fix applies to both, not just the one with the more obvious symptom.
- **Backlog resolved:** SURFACE-2026-06-09-F16-TRIAGE-AMBIGUOUS-FLAKY-SOFT-PASS-ON-SONNET — closed by task `sonnet-hygiene-and-telemetry-inconclusive-scenario` via `transition_id_any: [F16, F14]` + sonnet-tag on both scenarios. The dual-identity transition shape matches the actual skill behavior; the prose-leak fix (removing `auto-fix` from `not_contains`) is structurally orthogonal but landed together for cleanliness.
- **Task closed:** `debug-telemetry-inconclusive-strict-pass` — fixed two orthogonal harness-regex bugs in `tests/lib/verify.sh` and `tests/run-tests.sh` that together caused `DEBUG-TELEMETRY-INCONCLUSIVE` to SOFT_PASS on sonnet: (1) `tr -d '*'` pre-strip removes markdown bold decoration anywhere on the line (handles `**TRANSITION: DEBUG**-TELEMETRY-INCONCLUSIVE` mid-token bold-end the prior `[*[:space:]]*` prefix-only tolerance could not), and (2) `head -1` → `tail -1` selects the terminal TRANSITION emit instead of the first (debug-empirical-telemetry SKILL.md §1 line 69 intentionally emits `DEBUG-TELEMETRY-START` mid-procedure as informational gate-met signal before the §6/§7 terminal token). The SURFACE described only the markdown-bold issue; the second root cause was discovered mid-act at T3.5 via bite-verify (`transition_found: DEBUG-TELEMETRY-START` instead of the expected INCONCLUSIVE), handled in-task without back-loop since the additional change touched the same files/lines the plan already scoped. Final verify: PASS strict on sonnet, 16s, $0.085. Regression smoke-test (4 scenarios across debug-* and feature): 3 PASS + 1 SOFT (DEBUG-BISECT-GATE-MET on haiku, reproduces pre-fix baseline flakiness — same intermittent behavior on 06-09 baseline; not introduced by this task). YAML inline comment on the test scenario updated to reflect strict-PASS state.
- **Backlog resolved:** SURFACE-2026-06-12-DEBUG-TELEMETRY-INCONCLUSIVE-STRICT-PASS-NEEDED — closed by task `debug-telemetry-inconclusive-strict-pass` via path (b) of the suggested action (harness regex broadening) plus the mid-act-discovered head→tail fix. Both `verify.sh:39` and `run-tests.sh:244` now strip asterisks before capture and select the terminal emit. Path (a) skill-prose fix deliberately not taken — prior bite-verify attempts ($0.25 spend) confirmed sonnet ignores explicit emit-shape instructions; markdown decoration of tokens is a model default not suppressible by prose alone. Path (c) accept-SOFT_PASS deliberately not taken — strict PASS is now achievable at primitive level.
- **Backlog resolved:** SURFACE-2026-06-10-DEBUG-TELEMETRY-INCONCLUSIVE-SCENARIO — closed by task `sonnet-hygiene-and-telemetry-inconclusive-scenario` with lenient SOFT_PASS coverage; new follow-up `SURFACE-2026-06-12-DEBUG-TELEMETRY-INCONCLUSIVE-STRICT-PASS-NEEDED` opened with 3 candidate fix paths (skill-prose, harness regex broadening, or accept SOFT_PASS as steady-state). The original SURFACE entry already anticipated the describe-then-escalate path's fixture-control fragility; lenient coverage is the documented outcome under that warning.
- **Task closed:** `grep-check-helper-pipefail-fix` — defused the `grep_check` helper's count-capture corruption under `set -euo pipefail` in `tests/check-structure.sh` (lines 37-38). Replaced naive `count=$(grep -cE ... || echo 0)` (which produces "0\n0" 3-char string with embedded newline when grep finds 0 matches, because both grep's stdout AND the `|| echo 0` fire) with `count=$( (grep ... || true) | head -1 )` + `count="${count:-0}"` fallback. Inline NB comment documents the pipefail interaction for future maintainers. Bite-verified by adding a temporary `min_count=0` pin pre-fix (observed FAIL with literal "found 0\n0 lines matching..." output, exact bug from SURFACE), applying fix, observing same pin PASS cleanly (229/0 with temp pin, 228/0 after removal). Single ~7-line change including the comment block. Latent bug — not currently triggering since all existing pins use `min_count >= 1`. Same pattern shape as the Phase 10 (f) back-reference pin shipped yesterday (`subagent-dispatch-back-reference-pin`).
- **Backlog resolved:** SURFACE-2026-06-09-GREP-CHECK-HELPER-PIPEFAIL-INTERACTION — closed by task `grep-check-helper-pipefail-fix`. The latent integer-comparison failure path is now defused; future pins that use `min_count=0` (e.g. allowed-form negative pins) will land cleanly without the embedded-newline integer expression error.
- **Backlog resolved:** SURFACE-2026-06-11-SKILL-HARNESS-REGISTRY-LOADED-ONCE-AT-SESSION-START — closed by prior commit `d4be9c6` (earlier today, task `duplicate-verify-codify-leaves-convention`) which bundled the L2 "Bootstrap-skip is broader than registry — also covers edited Skill SKILL.md content" `## Conventions` bullet in `CLAUDE.md`. The L2 bullet contains the exact actionable discipline this SURFACE asked for (option (a) of its Suggested Action): names the broader scope of the bootstrap-skip (covering BOTH skill and agent registries, AND new-artifact + edited-content cases), and prescribes the plan-time discipline: "when planning a feature that edits an existing skill, schedule the empirical validation either via a behavioral test scenario in `tests/scenarios/*.yaml` (harness fresh-subprocess) OR accept the bootstrap-skip-defer pattern (real validation deferred to next session)." No additional commit needed — the documentation work was already shipped under a sibling task's bundle. Options (b) and (c) of the Suggested Action (investigate session-pause/resume registry reload, OR /agents reload slash command) remain deferred — neither has trivial-proof-of-concept evidence yet, and the documented bootstrap-skip-defer workaround is acceptable.
- **Feature shipped:** `close-commit-discipline` — codified two paired commit-discipline contracts across the workflow system's terminal-close + meta-learning skills (commits `40ef434` + `d1c0bb9`, 11 files, +132). (1) `session-store-learning` project-scope writes now `git add` + `git commit --amend --no-edit` after writing the learning file, folding the learning into HEAD (typically the just-completed close commit per the post-reflect cadence) — closes the failure mode where uncommitted learning files between sessions were destroyed by destructive git ops (`git checkout HEAD --`, scaffolder overwrites, stash-replay). Global-scope writes (gitignored `.claude/learnings/`) explicitly opt out of the amend. (2) The four terminal-close skills (`feature-finalize`, `task-close`, `incident-resolve`, `product-finalize`) now carry an explicit "Do NOT `git push`" clause codifying their already-existing local-only behavior as a load-bearing contract so future drift can't reintroduce auto-push. Pushing remains the operator's call. Enforced by 6 new structural pins in `tests/check-structure.sh [Phase 11]` (`grep_check` calls: 4 no-push + 2 amend; baseline 226 PASS → 232 PASS; bite-verify confirmed pin FAILs on mutation with clear actionable error) + 5 new behavioral scenarios (`F19-no-auto-push`, `T10-no-auto-push`, `I10-no-auto-push`, `P13-no-auto-push`, `S20-amend-head` — full 5-scenario sweep through `./tests/run-tests.sh` on haiku: 5 PASS strict / 0 SOFT / 0 FAIL / 0 FLAKY, 95s, $0.35). New `## Conventions` bullet added to `CLAUDE.md` documenting the contract for future readers. Origin: escalated mid-verify from task workflow (original task framing `docs-only: true` was wrong — SKILL.md prose IS a runtime surface; archived task WIP captures the lesson). Code-quality review (first F38 → review-quality dogfood pass that didn't bootstrap-skip): 0 CRITICAL, 0 MAJOR, 5 MINOR auto-backlogged (`SURFACE-2026-06-12-QUALITY-*`). Operator approval at verify-human-Phase-3 was the only forced human pause across the entire autopilot run.
- **Backlog resolved:** SURFACE-2026-05-22-LEARNING-COMMIT-OFTEN-AT-CROSS-FEATURE-BRANCH — closed by feature `close-commit-discipline`. The failure mode (uncommitted learning files lost in destructive cross-feature-pause git ops) is now mechanically prevented by the amend-to-HEAD mechanism in `session-store-learning`. The originally-proposed angles (a) port-rule-to-global-CLAUDE.md and (b) re-surfacing-dampener in session-reflect are both obviated — the rule no longer needs to be a discipline norm; it's a structural property of the procedure.

## 2026-06-11

- **Feature shipped:** `task-workflow-needs-lite-verify` — added `task-verify` as a new single-step gate between task-act and task-close (commit 7073fde, 11 files, +348/−15). Every `task-act` now exits to `/task-verify` (T5a — replaces old T5: act → close), which writes an observable into the WIP, runs the verification command for real (no `--dry-run` proxies), and emits T5b (PASS → /task-close) or T5c (FAIL → back-loop to /task-act with the failed observable as scope marker). Pure-docs tasks may declare `docs-only: true` in the WIP frontmatter at plan time to auto-skip the gate (per OQ-1 resolution: explicit-gate-as-discipline, not heuristic-as-convenience). SURFACED-sibling-bug handling at `task-verify` §4b mirrors `feature-verify-self` §3 in-place fix shortcut: three gates (trivial extension + fresh re-verification + audit-trail `[SHORTCUT-<YYYY-MM-DD>]` entry); broader sibling-bugs SURFACE to backlog. State-machine changes: `docs/product/transitions.md` retires T5 (act → close) and adds T5a/T5b/T5c rows; pause-policy table gains `verify (T5b/T5c gate) | PAUSE | AUTO | AUTO | AUTO` (mirrors task-act, not task-close); `agents/task-workflow/AGENTS.md` updates state diagram (`plan → act → verify → close`), skills frontmatter (+task-verify), transitions table, pause-policy table. Caller-skill updates: `task-act/SKILL.md` Valid transitions cites T5a + §7 Completion hands off to `/task-verify` + new emit-TRANSITION section; `task-close/SKILL.md` §1 advisory precondition note + T5b context in State Machine Context; `task-plan/SKILL.md` WIP template gains `docs-only: false` frontmatter + "About `docs-only:`" prose; `CLAUDE.md` `## Conventions` bullet codifies the new gate. Test coverage: 3 new scenarios (T5a rename, T5b PASS, T5c FAIL — all strict-PASS on haiku, 1 attempt each, 8s/9s/15s respectively, JSON-confirmed); 2 new fixtures (`task-verify-pass.md`, `task-verify-fail.md` — depicting the real 2026-06-09 run-all.sh FORWARD_ARGS/SIGPIPE case for grounding); 7 new structural pins in `tests/check-structure.sh` (`178 → 185 PASS, 0 FAIL`). Mid-Phase-3 test triage event: T2 (existing pre-feature scenario) FAILed on haiku post-Phase-2 — high-confidence regression of haiku's classification of the loading-spinner input from T2 → T3, caused by the docs-only documentation block's prose competing with the small/simple-task routing signal; resolved by sonnet-tagging T2 per CLAUDE.md "haiku-marginal classification call → sonnet-tag" convention (precedent: 6 of 8 scenarios sonnet-tagged in verify-codify-scenarios-need-sonnet-tag task, 2026-06-09). Sonnet T2 confirmed PASSing (FLAKY → PASS on retry, matches historical pattern). Origin: SURFACE-2026-06-09-TASK-WORKFLOW-NEEDS-LITE-VERIFY (the 2026-06-09 `run-all-unbound-forward-args` near-miss demonstrated the gap empirically — user mid-act directive enforced end-to-end verification that surfaced T1b masked sibling SIGPIPE bug; without the directive, a still-broken script would have shipped with a passing close). 3 phases, ~333 LOC net, zero F12/F22/F23/F9b back-loops, zero F26 escalations, 5 human-pause points (3 verify-human + spec/plan reviews) all approved first-pass. Plan-time OQ-4/5/6 resolutions all held through implementation: pause policy = PAUSE/AUTO/AUTO/AUTO mirroring task-act; T5c scope-marker = the failed observable text itself (no leaf-IDs at task scope); parent-inline verification (no Playwright — browser-needing tasks should escalate via T9).
- **Backlog resolved:** SURFACE-2026-06-09-TASK-WORKFLOW-NEEDS-LITE-VERIFY — closed by feature `task-workflow-needs-lite-verify`. Workflow-system structural gap addressed: every `task-act` now goes through a verification gate before close, removing the dependency on the operator catching the right moment with the right "verify before closing" directive. Affects every task in every project using this workflow harness going forward.
- **Feature shipped:** `code-quality-reviewer-subagent` — new `feature-review-quality` skill sits between `feature-ship` and `feature-finalize` in the feature workflow, invoking a one-shot Agent reviewer subagent against the ship commit baseline (commit `915bc4e`, 15 files, +633/−15). Severity-tier action matrix is advisory by default with operator-veto: **CRITICAL** auto-invokes `feature-refactor` (F40, Modes 2-3); **MAJOR** Mode 2 pause-and-ask (F41) or Mode 3 auto-backlog with prominent chat surface (F39, preserves "verify-human is the ONLY autopilot pause" invariant); **MINOR** auto-backlog (F39). Mode 4 (full-autopilot) skips entire skill — `feature-ship` emits F17b directly to finalize when WIP frontmatter shows `drive_mode: full-autopilot`. Transition surface: F38 (ship → review-quality), F39/F40/F41 (review-quality exits), F17b (ship → finalize direct, Mode-4 SKIP); F17 retired. Reviewer prompt is **externalized** at `skills/feature-review-quality/reviewer-prompt.md` (148 lines, tuned to this codebase's SKILL.md/AGENTS.md/scenario-YAML/structural-pin patterns; tripartite output: Strengths / Issues by severity / Assessment) — first externalized subagent-prompt artifact in the repo. Diverges deliberately from `obra/superpowers` per-task "all findings block" model: per-feature post-ship placement makes back-loops on shipped commits expensive, so advisory-default + read-time veto (edit `## Code-Quality Review` section in WIP and mark `[DISMISSED]` before finalize archives) is the recovery surface. Test coverage: 5 new behavioral scenarios (F38, F17b, F39, F40, F41) all PASS strict on haiku first attempt (67s, $0.37 total), 8 new explicit structural pins, Phase 9 + 9b auto-iteration extends drift coverage to new skill — 185 → 200 PASS, 0 FAIL. 3 phases as planned, zero back-loops (F23/F12/F9b/F22/F36/F26 — none fired), 5 human-pause points (spec + plan reviews + 3 verify-human auto-skipped per Mode-3 gate). Bootstrapping limitation surfaced at ship time (`Skill("feature-review-quality")` returned "Unknown skill" because the harness registry was loaded at session start, before the new skill's symlink existed) — one-time bootstrap-skip routed through finalize directly (NOT F17b, conceptually distinct from Mode-4 SKIP); future features in fresh sessions will be the first real exercise of F38 → review-quality. Logged to backlog as SURFACE-2026-06-11-SKILL-HARNESS-REGISTRY-LOADED-ONCE-AT-SESSION-START (P5, low priority). Origin: SURFACE-2026-06-02-CODE-QUALITY-REVIEWER-SUBAGENT (comparative analysis of `obra/superpowers` 2026-06-02, archived at `docs/product/archive/research/2026-06-02-superpowers-comparison.md`).
- **Backlog resolved:** SURFACE-2026-06-02-CODE-QUALITY-REVIEWER-SUBAGENT — closed by feature `code-quality-reviewer-subagent`. The middle question in the verify loop ("is the implementation well-built?" — separate from "did it match the plan?" and "is regression coverage missing?") now has a dedicated skill with curated criteria and severity-tier action matrix. Plus a meta-result: this is the first externalized subagent prompt artifact in the repo, setting precedent for future skills with long subagent prompts.

## 2026-06-10

- **Task closed:** `i20-scenario-missing` — added I20 (codify → investigate back-loop) scenario to `tests/scenarios/incident.yaml`, closing the long-standing coverage gap where the I20 transition was documented across `transitions.md`, `incident-codify/SKILL.md`, and `agents/incident-workflow/AGENTS.md` but had no behavioral test (its siblings I17, I18, I18-defer, and I19 were all covered). Scenario reuses the existing `incident-codify-with-reproduce-artifact.md` fixture and overrides semantics via `system_prompt_extra` describing a codify-time discovery that the prior investigation's single-field root cause was incomplete — a sibling request shape (`?include=preferences`) produces the same NoneType 500 from a different serializer path on a different nullable field (`notification_email`), contradicting the original investigation's conclusion. Asserts `transition_id: I20`, `contains_any: ["/incident-investigate", "back-loop", "root cause"]`, `not_contains: ["/incident-resolve", "/incident-mitigate"]`. Verified by full `./tests/run-tests.sh --id I20` run: strict PASS on haiku in 64s, no SOFT, no FAIL, no retries — no sonnet recon needed. `./tests/check-structure.sh` returns 139/139 PASS, FAIL: 0 (unchanged). Single-file yaml edit (~30 lines), no harness or convention changes.
- **Backlog resolved:** SURFACE-2026-05-10-I20-SCENARIO-MISSING — closed by task `i20-scenario-missing`. The rare codify-time root-cause-misdiagnosis back-loop path is now exercised by the test sweep.
- **Task closed:** `session-resume-strip-pause-footer` — added §6b "Strip the stale Pause footer from `state_file`" to `skills/session-resume/SKILL.md` between the existing §6 (backlog check) and §7 (delete `.session.md`). The new step removes the orphan `## Session Pause — <timestamp>\nPaused. See workflow/.session.md to resume.` block that `/session-pause` always appends to the end of `state_file` (per `session-pause` SKILL.md §3), eliminating a recurring 10–30s cleanup tax at finalize/close time on every paused-then-resumed item (NeoStayman alone had 18+ recurrences across WP4–WP30; this repo hit it on `workflow/backlog.md` during this very morning's session-resume and on `workflow/archive/verify-human-auto-skip-when-no-integration-boundary.md` previously). The procedure is idempotent (no-op if the marker isn't present) and forward-compatible (a documented extension covers a hypothetical mid-document `/session-pause` variant by matching "until next `## ` or EOF"). Regression coverage: new structural `grep_check` pin in `tests/check-structure.sh` Phase 3 (placed next to the In-place fix shortcut pin — the closest precedent for skill-prose subsection anchors) asserting `skills/session-resume/SKILL.md` retains the "Strip the stale Pause footer" anchor; `./tests/check-structure.sh` now reports 140/140 PASS, FAIL: 0 (was 139/139, clean +1 from the new pin). Edit is live without an `install.sh` re-run because `~/.claude/skills/session-resume` is a directory-level symlink into this repo. Single SKILL.md procedural edit + single check-structure pin; no state-machine change, no AGENTS.md edit, no transitions.md edit (the existing S6/S15/S16 transitions remain unchanged — §6b is sub-procedural).
- **Backlog resolved:** SURFACE-2026-06-07-SESSION-RESUME-LEAVES-PAUSE-FOOTER — closed by task `session-resume-strip-pause-footer`. Cross-project learning from NeoStayman (18+ observed recurrences across WP4–WP30) and one in-session observation from this morning's resume both motivated the medium-high priority; fix landed in ~4 file edits and ~30s of verification. The companion learning at `/Users/stayman/Personal/projects/neo-stayman-assistant/.claude/learnings/2026-06-07-session-resume-strip-stale-pause-footer.md` (gitignored, NeoStayman-local) and the NeoStayman backlog reference `SURFACE-2026-05-16-SESSION-RESUME-LEAVES-PAUSE-MARKER` were the source-of-truth pointers.
- **Task closed:** `feature-finalize-tick-wbs-task-checkboxes` — extended `skills/feature-finalize/SKILL.md` §1 "Update Documentation" with a new required sub-bullet "**WBS per-task checkbox tick (required):**" directing the agent, when marking a WP done in `docs/product/wbs.md`, to convert every `- [ ]` to `- [x]` **within that WP's section only** (between the WP's heading and the next WP heading or EOF) after appending the existing `✅ SHIPPED <date> (commit <sha>)` tag to the WP heading; rationale stated inline ("the WP being shipped means by definition all its tasks landed") plus an explicit anti-pattern callout against a global `replace_all` (which would mistakenly tick checkboxes in other WPs still in-progress) plus a re-scope guidance line (if a task genuinely didn't land, the WP should be re-scoped explicitly rather than silently shipped with hidden gaps). Closes a 12-of-12-WP recurrence pattern observed since v3 cycle start in NeoStayman where every finalize produced a `✅ SHIPPED` heading sitting above unticked per-task checkboxes, leaving WBS a partially-trustworthy state surface for downstream planning skills (`feature-spec` reads WBS to determine what's done; `/product-finalize` cycle-close sweep reads it too). Regression coverage: 1 new structural `grep_check` pin in `tests/check-structure.sh` Phase 3 immediately after the existing line-96 `feature-finalize references CHANGELOG convention` pin, anchoring the literal sub-bullet header `"WBS per-task checkbox tick"` (naturally robust to surrounding prose edits) with a load-bearing comment block citing the 12-of-12 recurrences and the partially-trustworthy-state-surface failure mode; `./tests/check-structure.sh` now reports 141/141 PASS, FAIL: 0 (was 140/140 after `session-resume-strip-pause-footer`; clean +1 from the new pin, no triage needed). Edit is live without an `install.sh` re-run because `~/.claude/skills/feature-finalize` is a directory-level symlink into this repo (verified by `grep -c "WBS per-task checkbox tick" ~/.claude/skills/feature-finalize/SKILL.md` returning 1). Single SKILL.md procedural edit + single check-structure pin; no state-machine change, no new transition (F18/F19/F30 unchanged — sub-procedural refinement of §1 only), no AGENTS.md edit, no transitions.md edit. Runtime registry updated for the new 17s wall-clock observation.
- **Backlog resolved:** SURFACE-2026-05-29-FEATURE-FINALIZE-MISSES-WBS-TASK-CHECKBOXES — closed by task `feature-finalize-tick-wbs-task-checkboxes`. Applied the backlog entry's proposed fix exactly ("for the WP being finalized, `replace_all` `- [ ]` → `- [x]` *within that WP's section only*") with structural-pin regression coverage; the discipline now propagates to every future WP finalize so the `✅ SHIPPED + unticked checkboxes` inconsistency doesn't accrue further.
- **Task closed:** `claude-md-claude-time-container-note` — appended a single paragraph to project `CLAUDE.md` under `## Commands` (between the existing workflow-system test-runner narrative ending line 27 and `## Architecture` at line 29) documenting that `tools/claude-time/` tests run inside a Docker container (not on the host), with the lifecycle wrapper at `tools/claude-time/test/run-in-container.sh` and its 7 subcommands (`start`, `stop`, `restart`, `status`, `exec <cmd>`, `logs`, `help`), the image bundle (Python 3.12 + Perl + sqlite3 + jq + Node + Playwright + Chromium), the repo-root bind-mount at `/work` rw, and a pointer to `tools/claude-time/README.md` → "Running tests" for canonical invocations. Closes a long-deferred doc-gap from `claude-time-test-containerization` finalize on 2026-05-22 (originally blocked by cross-feature dirty-tree contention with WP5; WP5 has long since shipped so the pickup window opened cleanly). Single-hunk single-file edit, no SKILL.md changes, no harness changes, no new structural pin (the gap was in prose that has no natural pin anchor — the existing CLAUDE.md content already loads as project context for every Claude Code invocation, so discoverability is automatic). `./tests/check-structure.sh` returns 141/141 PASS, FAIL: 0 (unchanged from baseline — no new pin, no regression).
- **Backlog resolved:** SURFACE-2026-05-22-CLAUDE-MD-MISSING-CLAUDE-TIME-CONTAINER-NOTE — closed by task `claude-md-claude-time-container-note`. Applied the backlog entry's `**Suggested action:**` field verbatim; the doc-gap was trivially shaped once the cross-feature dirty-tree contention cleared.
- **Feature shipped:** `debug-empirical-telemetry-skill` — new `debug-*` sidebar skill that switches the agent from static-analysis debugging to empirical runtime observation (instrument → run → read telemetry → iterate or escalate) when ≥2–3 static-reasoning attempts have failed AND the bug-shape requires runtime evidence (timing/race, intermittent, DB query plan/timing, perf regression, env-dependent state, "wrong value at this line"). Joins `/debug-bisect-known-good` as the second member of the agent-pulled sidebar family — both follow the same SKILL.md shape (6 required sections, Gate Check first, `RETURN-TO:` convention, descriptive `DEBUG-<TECHNIQUE>-<OUTCOME>` tokens outside the F/I/T/P/S transition namespace) and the same three-discoverability-surfaces wiring (caller-prose mentions in `feature-build`/`incident-investigate`/`task-act` SKILL.md §3b/§4b, rows in each relevant orchestrator AGENTS.md "Debug techniques (agent-pulled sidebars)" subsection, mention in `docs/product/transitions.md` → "Sidebar skills (`debug-*` category)"). Procedure encodes the empirical playbook in 7 steps: (1) Gate Check writes both gate confirmations in prose then emits `DEBUG-TELEMETRY-SKIP` on either NO; (2) Pick the smallest discriminating observable — the single observation that, more than any other, would discriminate between current hypotheses; (3) Instrument with categories (log line, timing counter, dict/state dump, EXPLAIN, breakpoint trace, perf sampler) and a `# TELEMETRY` marker for mechanical cleanup; (4) Run and read the telemetry without inferring beyond the observation; (5) Decide (cause located → cleanup; inconclusive → iterate with a different observable; ≥3 inconclusive rounds → escalate; gates no longer hold → exit SKIP); (6) Cleanup with action list + pre-exit written checklist (mirrors bisect's `Bisect runner removed; cause was V_<i>.` post-condition); (7) Inconclusive escalation after ≥3 non-converging rounds emits `DEBUG-TELEMETRY-INCONCLUSIVE` + suggests next debugging mode. 4 termination tokens (`DEBUG-TELEMETRY-{START,SKIP,COMPLETE,INCONCLUSIVE}`) with `RETURN-TO: <caller>` on every termination. 3-phase Size-M feature with clean linear flow: Phase 1 authored the SKILL.md (15.6 KB, all 6 required sections, install.sh re-run); Phase 2 wired the 3 discoverability surfaces (7 additive markdown edits across 7 files); Phase 3 extended `tests/check-structure.sh` Phase 3c with 7 new `grep_check` assertions (structural test 143 → 150 PASS) and added 3 scenarios to `tests/scenarios/debug.yaml` (DEBUG-TELEMETRY-GATE-MET, -INSUFFICIENT-ATTEMPTS, -STATIC-DERIVABLE) with 3 fixture files, all 3 strict-PASS on haiku in 33s with no sonnet retry needed. No F12/F22/F23 back-loops, no F26 escalations, no test-triage on this feature's own scenarios — the plan's 3-phase structure mapped 1:1 onto implementation. Auto-skip gate fired correctly for verify-human on P1 and P2 (no integration boundary — only new artifacts in P1 + additive markdown edits in P2); P3 verify-human paused for human review because `tests/check-structure.sh` and `tests/run-tests.sh` are CLI consuming surfaces and human approved on "looks good". The DEBUG-TELEMETRY-INCONCLUSIVE 4th-scenario coverage was a P3.7 stretch goal deferred at impl time per the plan's "drop if scenario design is intractable" clause — structurally hard to convey "3 rounds of telemetry already happened" in a fixture without embedding telemetry results in the fixture itself, and the model tends to suggest more rounds rather than escalating; surfaced as low-priority follow-up backlog item. Phase 3 verify-codify hit a FLAKY-on-retry on the PRE-EXISTING `DEBUG-BISECT-GATE-MET` scenario (output-shape pattern documented in `SURFACE-2026-06-09-F16-TRIAGE-AMBIGUOUS-FLAKY-SOFT-PASS-ON-SONNET`); triaged as unrelated-infrastructure, no action. One small calibration miss caught at verify-auto: the plan's `^## (...|Pitfalls|Termination)$` regex was anchored at `$` but the `debug-bisect-known-good` precedent uses `## Pitfalls (load-bearing — read before iterating)` with a parenthetical suffix; relaxed the regex to match established convention (1 small WIP-side edit, no impl rework). Shipped as commit `2aeabea` on `origin/main`.
- **Backlog resolved:** SURFACE-2026-05-22-DEBUG-EMPIRICAL-TELEMETRY-SKILL — closed by feature `debug-empirical-telemetry-skill`. Applied the backlog entry's `**Suggested action:**` field exactly (author `skills/debug-empirical-telemetry/SKILL.md` following the `debug-*` category convention with all 6 mandatory sections + Gate Check + `DEBUG-TELEMETRY-*` tokens + `RETURN-TO:` line; conjunctive gates per (a) ≥2–3 failed static attempts AND (b) runtime-value-required bug shape with 6 example categories; instrument → run → read → iterate procedure; cleanup discipline before exit; three discoverability surfaces — caller-prose mentions + orchestrator AGENTS.md rows + transitions.md mention). The new sidebar is the second member of the `debug-*` family (first was `/debug-bisect-known-good` codified 2026-05-13); both skills can now be agent-pulled from `feature-build`, `incident-investigate`, or `task-act` when their respective trigger profiles hold (known-good sibling exists vs. runtime-evidence-required bug-shape).
- **Task closed:** `cheat-sheet-agents-drift` — extended `tests/check-structure.sh` Phase 9 with a new drift assertion (Phase 9b) that compares per-skill cheat-sheet table VALUES across all 8 affected feature SKILL files (`feature-{spec,research,plan,build,verify-auto,verify-self,verify-human,verify-codify}/SKILL.md`) against the canonical pause-policy table in `agents/feature-workflow/AGENTS.md`. Implementation is a single Python heredoc that (a) parses both source tables via a shared `parse_table` helper bounded by section-heading regex, (b) normalizes cell values (strips markdown bolding, takes the first policy token `AUTO|PAUSE|SKIP|AUTO-SKIP`, maps the "pause already taken at entry" sentinel to PAUSE, treats `n/a` cells as out-of-scope), and (c) applies a static row-mapping dict for the many-to-one cases (all back-loop transitions across the 8 files collapse to AGENTS.md's "Back-loops (F6, F9, F9b, F12, F14, F23, F24)" row; F22 → "REDIRECT (F22)"; F25/F26 → "SURFACE F<N>" rows). Output lines (`PASS\t<desc>` / `FAIL\t<desc>\t<detail>`) are read by bash and dispatched through the existing `check` helper so PASS/FAIL counts roll up into the summary block. Rows with no canonical counterpart (build's F27 incident-interrupt; verify-human's F13/F11/F11-AUTO-SKIP exit rows that describe post-pause orchestrator behavior with no AGENTS.md row) are explicitly mapped to `None` and skipped from comparison with an inline comment explaining why. The drift bite-test (T4 in the WIP plan, applying the P-NEW `task-workflow-needs-lite-verify` discipline preemptively) was the load-bearing verification step: temporarily flipped `feature-plan` Mode 3 from `AUTO` to `**PAUSE**` in AGENTS.md, ran `./tests/check-structure.sh`, confirmed exactly 2 FAILs surfaced (the 2 feature-plan rows, both naming Mode 3, with `expected 'pause' (from AGENTS.md), got 'auto' (from SKILL.md)`), reverted AGENTS.md, re-ran, confirmed 175/175 PASS back. `./tests/check-structure.sh` now reports 175 PASS / 0 FAIL (was 150/150 at session start; +25 from the new assertion set: 1 AGENTS.md-table-parse PASS + 24 per-row value-match PASSes across 8 SKILL files). 2 plan-side misses caught and fixed at T3 with a 1-minute fix-and-re-run loop each: (1) AGENTS.md skill-row keys are backtick-wrapped (`` `feature-spec` ``) not plain (`feature-spec`) — caught by dumping the parsed dict; (2) verify-human's exit-transition rows have no canonical counterpart in AGENTS.md (they describe post-pause chaining behavior, not invocation-pause policy) — resolved by mapping to `None`. Both surfaced as a `[SURFACED-2026-06-10]` Discovery in the WIP file flagging "plan-time row-label inspection needs literal-string capture, not just key-name extraction" as another instance of the literal-payload-object / array-length-add / function-signature subcase family documented in CLAUDE.md's plan-time downstream-contract-impacts grep convention. Single-file edit to `tests/check-structure.sh` (~120 lines added including the Python heredoc + bash dispatch loop + extended Phase 9 section-header comment block); no SKILL.md changes, no AGENTS.md changes, no harness changes outside the Python block, no new fixtures, no behavioral scenario (this is structural-only coverage). Runtime registry updated for the new 18s wall-clock observation (was 17s — the new Python block adds ~1s).
- **Feature shipped:** `reproduce-as-redirect-from-build` — added a REDIRECT path from `feature-build` into `feature-reproduce` (F36) when an agent mid-build realizes the fix cannot be confirmed without first reproducing the bug, with F37/F37b return transitions that resume build with the reproduce artifact (or could-not-reproduce Discovery) in hand. Mirrors the existing F22 (build → research REDIRECT) shape across the state machine. F35 (terminate) and F34 (preventive hardening reset to spec) are explicitly disallowed from F36-entered reproduce — terminating mid-build is the wrong outcome (use F37b instead) and the feature is already past spec (framing reset is meaningless). Entry-via-F36 detection uses a sentinel line in WIP file's `## Current Node` (`**Redirect source:** build (F36 — Phase N)`) written by build at F36 exit and read by reproduce at §1.5 entry — reuses the existing Current Node pointer mechanism, no new file fields. The reproduce artifact lands in a dedicated `## Reproduction Artifact (mid-build, from F36)` section parallel to how F31-entered features own a top-level `## Reproduction Attempt` section. Four-phase Size-M feature shipped end-to-end in autopilot Mode 3 across a single session with zero F12/F22/F23 back-loops, zero in-place-fix shortcuts, zero F26 escalations, zero test-triage on own scenarios: Phase 1 added F36/F37/F37b rows to `docs/product/transitions.md` (Full Transition Table + narrative + Change Log + duplicated "Pause policy by mode — feature workflow" sync) and `agents/feature-workflow/AGENTS.md` (transition table + 2 new pause-policy rows `REDIRECT (F36)` + `Return-from-REDIRECT (F37, F37b)` + Your Role REDIRECT mention); Phase 2 updated `skills/feature-build/SKILL.md` with F36 in valid transitions + cheat-sheet row + new §4 sibling case to F22 (with sentinel-write + placeholder-section instructions) + §9 emit token (intentionally shipped with 1 transient FAIL in check-structure.sh because the ROW_MAPPING update belongs in Phase 3 — see "Downstream contract-impact pass" section of the plan); Phase 3 updated `skills/feature-reproduce/SKILL.md` (the SKILL's first cheat-sheet block + §1.5 sentinel detection + §4 branched-by-`f36_entry` outcome logic + §5 emit list) and extended `tests/check-structure.sh` ROW_MAPPING with `("F36 ", "REDIRECT (F36)")` on feature-build + new `feature-reproduce` entry mapping F32/F33/F34/F35 to None (no per-transition AGENTS.md rows for these, matching the verify-human precedent for F13/F11) and F37/F37b to `Return-from-REDIRECT (F37, F37b)`; Phase 4 added 3 fixtures (`feature-build-needs-repro.md`, `feature-reproduce-from-build-success.md`, `feature-reproduce-from-build-could-not.md` — the latter two carry the F36 sentinel in Current Node) and 3 scenarios in `tests/scenarios/feature.yaml` (F36 model=sonnet per F22 precedent; F37 + F37b haiku-default). All 3 transition scenarios PASSed strictly on first attempt (F36 sonnet 16s, F37 haiku 27s, F37b haiku 27s, total ~$0.33). `./tests/check-structure.sh` 175 → 178 PASS / 0 FAIL (+3: F36 row drift on feature-build + F37/F37b row drift on feature-reproduce — exactly the plan-time prediction). Phase 2's plan-declared 1-FAIL transient state closed by Phase 3 P3.6 ROW_MAPPING update as predicted. F37b haiku output noted "Structured match on F37b but also mentioned: F34, F35" — the well-known prose-leak pattern from CLAUDE.md (entry-state transitions echoing disallow-rule tokens); harness PASSed correctly on transition_id match, sonnet-tag promotion declined per CLAUDE.md convention ("Don't tag preemptively") — flagged for future sonnet promotion if it ever produces a real SOFT_PASS or FAIL. Auto-skip gate at verify-human correctly fell through to human PAUSE on all 4 phases because every phase had an integration boundary (transitions.md/AGENTS.md consumed by check-structure.sh Phase 9b parser; feature-build/feature-reproduce SKILL.md consumed by the same parser; check-structure.sh itself self-consuming; scenarios+fixtures consumed by `./tests/run-tests.sh`). The integration-boundary rule fired on every phase as expected, and human approvals went cleanly. Shipped as commit `43cb517` on `origin/main` (was `7fa1625..43cb517`, 10 files, +339/−3). The 5 open spec questions all resolved cleanly at plan-time with no surprises during build — F36 mirroring F22 was the right precedent and the 4-phase structure mapped 1:1 onto implementation.
- **Backlog resolved:** SURFACE-2026-05-08-REPRODUCE-AS-REDIRECT-FROM-BUILD — closed by feature `reproduce-as-redirect-from-build`. Was P4 in the ordered TODO list; removed cleanly from `workflow/backlog.md` at finalize time. The medium-shape feature (4 phases, 22 leaves total, 5 SKILL/AGENTS files + 3 fixtures + 3 scenarios + 1 harness file touched) mapped 1:1 onto implementation with no plan revisions — confirming the backlog entry's `**Priority:** low (deferred — wait until we observe the need in practice)` was the right call (the design space was already well-shaped by F22's precedent so the feature was tractable as a one-shot when the pickup window came).
- **Backlog resolved:** SURFACE-2026-05-17-CHEAT-SHEET-AGENTS-DRIFT — closed by task `cheat-sheet-agents-drift`. Applied the backlog entry's `**Suggested action:**` 3-step procedure exactly (parse AGENTS.md table into a dict; for each of 8 SKILL.md files, parse the cheat-sheet table; assert every per-skill row matches the corresponding row in the canonical table). Originally surfaced 2026-05-17 by `incident:resolve (autopilot-pause-policy-recheck-regression)` as the structural-test gap left open when behavioral red→green coverage was abandoned (per `SURFACE-2026-05-17-CLAUDE-PRINT-AGENTIC-LOOP-SUPPRESSES-PAUSE-DECISION`); Phase 9 caught outright deletion / imperative weakening, but the drift case (AGENTS.md updated, SKILL.md cheat-sheets silently keep old values) was uncovered. Phase 9b now covers it; the drift bite-test confirmed the new assertion catches a flipped Mode 3 cell with precise diagnostic detail (file, row, mode-index, expected, got).

## 2026-06-09

- **Feature shipped:** `verify-self-in-place-fix-shortcut-policy` — formalizes the ad-hoc "in-place fix" shortcut that had emerged organically in `feature-verify-self` across three real cycles (v3 WP3 Phase 2 on 2026-05-29, v3 WP11 Phase 1 on 2026-06-06, verify-human-auto-skip-when-no-integration-boundary Phase 2 on 2026-06-07) into an explicit triple-gated sub-clause in `skills/feature-verify-self/SKILL.md` §3 "In-place fix shortcut (BLOCKING-fail handling — narrow exception)" with anti-abuse "What this shortcut is NOT" paragraph; the clause permits applying a fix in-place (skipping the F9b → build → verify-auto → verify-self back-loop, saving 3 Skill invocations per shortcut in autopilot) when (a) the fix is a trivial extension of the just-completed leaf, (b) re-verification runs through a fresh model invocation (fresh Playwright subagent or fresh `Skill` invocation of `feature-verify-self`, not same-agent re-reading), and (c) an audit-trail entry `[SHORTCUT-<YYYY-MM-DD>] <leaf-id> — <what+how>` is appended to the WIP `## Discoveries` section; project `CLAUDE.md` line ~240 updated from "ready to formalize" to "formalized 2026-06-09" cross-link pointing at the authoritative SKILL.md clause; regression coverage: 2 new `grep_check` structural pins in `tests/check-structure.sh` (132→134 PASS / 1 unrelated pre-existing FAIL — the documented P4 settings-fixture drift) anchoring the "In-place fix shortcut" heading + the `[SHORTCUT-<YYYY-MM-DD>]` audit-trail token convention, plus new `F10b-shortcut` behavioral scenario in `tests/scenarios/feature.yaml` (PASS strict on haiku, 31s, untagged-default correct — no recon needed) framing a single BLOCKING fail that is a one-character SQL DEFAULT-clause fix to a migration the agent itself just wrote, with subagent results pre-seeded so the prompt is unambiguously inside all three gates; new fixture `tests/fixtures/wip/feature-verify-self-trivial-fix.md` provides the trivial-fix-shaped WIP for the scenario; 2-phase Size-S feature with clean linear flow (Phase 1: SKILL.md edit + CLAUDE.md cross-link; Phase 2: structural-check assertion + behavioral scenario + fixture) — no F12 back-loops, no F23 plan revisions, no F9b verify-self back-loops; both verify-human nodes auto-skipped per drive_mode=autopilot 4-gate (this feature dogfooded the auto-skip gate shipped in the previous feature `verify-human-auto-skip-when-no-integration-boundary` on 2026-06-07); one case-sensitivity miss caught by build-time grep discipline at Phase 1 (the "trivial extension" outcome required lowercase, first edit produced "Trivial extension" capitalized — fixed in-place by adding a lowercase mention adjacent to the gate language, no back-loop needed). Shipped as commit `b097ac0` on `origin/main`.
- **Backlog resolved:** SURFACE-2026-05-29-VERIFY-SELF-IN-PLACE-FIX-SHORTCUT-POLICY — closed by feature `verify-self-in-place-fix-shortcut-policy`. Option (a) chosen (explicit "In-place fix shortcut" sub-clause in SKILL.md §3 + structural-check pins + F10b-shortcut behavioral scenario); option (b) (auto-fast F9b → F8 chain for trivial-fix back-loops) explicitly out of scope per backlog entry's "either route" framing.
- **Feature shipped:** `check-structure-sigterm-propagation` — replaces the `total=$(./tests/run-tests.sh --dry-run | ...)` command-substitution invocation in `tests/check-structure.sh` (which took ~240s and could orphan its process tree when the parent was killed) with an inlined Python YAML scenario count that walks `tests/scenarios/*.yaml` directly and matches `run-tests.sh`'s own counting semantics (TOTAL increments once per scenario after filtering; with no flags, every scenario passes — verified count = 139); side effect drops `tests/check-structure.sh` wall-clock runtime from ~240s to ~16s, eliminating the orphan-child surface entirely (no subprocess to clean up); two prior build attempts at trap-based propagation failed verify-self for the same root cause uncovered during build — bash command-substitution subshells are *siblings* of the script (under the same wrapper bash PPID) not *descendants*, so no `pgrep -P $$` walk can reach them (proved via pid-tree diagnostic showing script PID 37304 PPID 37302 and command-substitution subshell PID 43298 also PPID 37302); switched approach via F23 → research → F5 chain after user redirect to option-3 (bypass-entirely); dead `_kill_descendants` recursive walker function and `trap` line installed during the failed builds removed in the same commit; `runtimes.md` updated for the new fast runtime (Last: ~16s, Use timeout: 84000, history bullet prepended with `post-dry-run-bypass` comment); 4 new structural `grep_check` pins in `tests/check-structure.sh` codify the chosen approach and forbid the regressed forms from returning — (a) "does NOT invoke the prior dry-run subprocess form" using `\./tests/run-test[s]\.sh --dry-run` regex where the `[s]` character class avoids the pin self-matching its own source, (b) "does NOT define/call the failed walker helper" matching `_kill_descendants[[:space:]]*\(` so bare-string references don't trigger, (c) "counts scenarios via inlined python3 heredoc" anchoring the chosen mechanism, (d) "runtimes.md has at least one **Use timeout:** entry" pinning the load-bearing field; pin design hit 2 HIGH-confidence Obsolete-test triage entries during Phase 1 codify (self-matching regex bug + `grep -c ... || echo 0` interaction with `pipefail` producing `n="0\n0"`), both auto-fixed per the §3b triage discipline; Phase 2 codify caught a duplicate `runtimes.md` shape-frontmatter pin (already existed at Phase 3 line ~114) and removed it; 2-phase Size-S feature but with 2 F9b/F23 back-loops in Phase 1 (recursive-walker attempt failed verify-self, plan revised) plus 1 F23 → research → F5 chain (user pause to redirect to research after second build failure, then research spike picked Design B); both Phase 1 verify-human (3 judgment leaves manually approved) and Phase 2 verify-human (auto-skipped per Mode 3 4-gate — no integration boundary, `runtimes.md` is an isolated tracking doc consumed by a discipline not a named system component) handled per drive_mode=autopilot policy; final sweep: 138 PASS / 1 FAIL (only pre-existing P4 settings-fixture drift, unchanged by this feature). Shipped as commit `d1cd105` on `origin/main`.
- **Backlog resolved:** SURFACE-2026-06-07-CHECK-STRUCTURE-DRY-RUN-CONCURRENCY-FRAGILE — closed by feature `check-structure-sigterm-propagation`. Took the "Optional" path from the backlog entry's suggested action (bypass `run-tests.sh --dry-run` entirely via inlined Python YAML count) rather than fixes (1) `trap 'pkill -P $$' EXIT` (proved structurally insufficient — subshells are siblings not descendants) or (2) the project CLAUDE.md "Tier-1 dev command runtimes" doc-note (already addressed by the global Long-running-commands runtime-registry pattern shipped 2026-06-09; `runtimes.md` updated with the new ~16s entry for this command).
- **Task closed:** `run-all-unbound-forward-args` — fixed two pre-existing `set -euo pipefail` interaction bugs in `tests/run-all.sh` that broke the documented end-to-end harness invocation: (T1) `${FORWARD_ARGS[@]}` on lines 42 + 49 triggered `unbound variable` on bash 3.2 when the array was empty (i.e. `./tests/run-all.sh` with no args), aborting before Pass 1 — replaced with the `${FORWARD_ARGS[@]+"${FORWARD_ARGS[@]}"}` conditional-expansion idiom; (T1b — sibling bug exposed only after T1 let the script reach line 43 for the first time) `P1_FILE=$(ls -1t … | grep -v combined | head -1)` on lines 43 + 50 triggered SIGPIPE on `ls` when `head -1` closed the pipe early, then `set -o pipefail` propagated exit 141 and `set -e` killed the script between Pass 1 and Pass 2 — wrapped each pipeline in `{ … ; } || true` so the SIGPIPE no longer aborts. T1b was a true T6 back-loop in shape (discovered during T2 verification) but handled in-line within the same act session because trivial extension of the same file + same bug-family + same end-to-end test surface; audit-trail entry in the WIP `## Discoveries`. Verified by real `./tests/run-all.sh --group debug` end-to-end run (exit 0, both passes complete, combined JSON merge writes `tests/results/run-2026-06-09-191951-combined.json`, 2 PASS / 1 SOFT_PASS pre-existing haiku-noise / 0 FAIL) plus populated-args `--dry-run` regression check (exit 0) plus isolated `set -u` empty-array idiom test. User's explicit "verify the test still works before closing" instruction (over-rode the plan's narrower `--dry-run` verification) was load-bearing — only the real end-to-end run surfaced the pipefail/SIGPIPE bug. Shipped as a task commit on `main`.
- **Backlog resolved:** SURFACE-2026-06-06-RUN-ALL-UNBOUND-FORWARD-ARGS — closed by task `run-all-unbound-forward-args`. Applied the suggested `${FORWARD_ARGS[@]+"${FORWARD_ARGS[@]}"}` idiom on lines 42 + 49 (T1) and additionally fixed a sibling `set -o pipefail` + SIGPIPE-from-`head` bug at lines 43 + 50 (T1b) that was previously masked by the earlier T1 crash; net effect: `./tests/run-all.sh` (no args — the documented end-to-end invocation) now runs both passes and the combined JSON merge to completion.
- **Task closed:** `settings-fixture-model-drift` — removed the orphan `"model": "opus[1m]"` line (and trailing comma on the prior line) from `tests/fixtures/settings.json`, which had been asserting a top-level `model` key that no longer exists in live `~/.claude/settings.json`; `./tests/check-structure.sh` Phase 7 drift detector now reports `OK` and the full structural sweep returns to **139/139 PASS, FAIL: 0** (was 138/139 with the long-standing settings-fixture drift FAIL — the same pre-existing FAIL the prior two same-day features had been carrying along as a known-unrelated noise item). Option (a) (drop the field) chosen over option (b) (add to `INTENTIONAL_DIFFS`) per the backlog entry's lean — fixture should track live unless an intentional-diff reason exists, and none did. Verified by inline drift walker (no diff outside intentional set) AND full check-structure.sh run (17s, registered to `runtimes.md`).
- **Backlog resolved:** SURFACE-2026-06-06-SETTINGS-FIXTURE-MODEL-DRIFT — closed by task `settings-fixture-model-drift`. Single-file JSON edit (1 line + comma); no harness or convention changes needed.
- **Task closed:** `verify-codify-scenarios-need-sonnet-tag` — applied the project's documented recon discipline (`see haiku failure → run on sonnet → confirm strict PASS → tag`) to 8 verify-codify-area scenarios in `tests/scenarios/feature.yaml` that the backlog entry had flagged as haiku-noise candidates; sonnet sweep (`./tests/run-tests.sh --id F13-prefiltered,F13,F14,F15,F16-triage-ambiguous,F16-triage-flaky,F16-triage-regression,F-boundary-codify --model sonnet`, 197s, $0.94) yielded 6 strict PASSes which received `model: sonnet` tags + one-line haiku-flake-shape comments matching the F4/F22 precedent (F13-prefiltered, F13, F14, F15, F16-triage-regression, F-boundary-codify — the last re-confirmed since the 2026-05-13 recon never applied the tag); 2 candidates (F16-triage-ambiguous, F16-triage-flaky) SOFT_PASSed on sonnet too — refuting the backlog hypothesis "likely all 6 fall into this category" — and were intentionally NOT tagged but surfaced as a follow-up SURFACE for scenario-design / skill-prose investigation rather than swept under the sonnet rug; verification via `./tests/run-tests.sh --dry-run --filter-model sonnet` confirmed the sonnet partition correctly grew from 3 → 9 scenarios (+6 new tags route as intended); `runtimes.md` gained a bookkeeping note recording ~25s/scenario as the sonnet estimator for future `--id <N-batch> --model sonnet` invocations. Recon discipline did exactly what it's designed to do — empirical sweep refused to confirm 2 of the 6 as haiku-noise, surfacing them as distinct issues instead of letting an undiscriminated tag mask them.
- **Backlog resolved:** SURFACE-2026-05-13-VERIFY-CODIFY-SCENARIOS-NEED-SONNET-TAG — closed by task `verify-codify-scenarios-need-sonnet-tag`. 6-of-8 recon outcome (not 6-of-6 as initially suspected); F16-triage-ambiguous + F16-triage-flaky split into new follow-up SURFACE-2026-06-09-F16-TRIAGE-AMBIGUOUS-FLAKY-SOFT-PASS-ON-SONNET for their underlying assertion-shape vs. skill-prose-drift root causes to be diagnosed separately.

## 2026-06-07

- **Feature shipped:** `verify-human-auto-skip-when-no-integration-boundary` — `feature-verify-human` SKILL.md §2 now elides the "do you agree to skip?" prompt under a 4-gate check (drive_mode is autopilot/full-autopilot AND verify-self all-PASS AND no integration boundary AND no consuming-surface outcome), emitting `TRANSITION: F11` directly with a one-line `Auto-skipped per drive_mode=<mode>` note alongside the still-printed affirmation block (read-time veto for the operator); pause-policy table in `agents/feature-workflow/AGENTS.md` gained an AUTO-SKIP annotation + explanatory paragraph naming all four gate conditions; downstream prose in `docs/product/transitions.md` (drive-mode description, autopilot policy table row, drive-mode menu, F11 row) and `skills/session-start/SKILL.md` (S12 description, drive-mode table mode 3, drive-mode menu mode 3, runtime AUTO/SKIP note — also corrected the pre-existing `session-start/SKILL.md:147` typo that incorrectly said "SKIP for verify-human in Mode 3"); `tests/check-structure.sh` gained 3 new structural `grep_check` pins (126→129 PASS / 1 unrelated FAIL) anchoring the Auto-skipped affirmation line, the 4-gate heading anchor, and the AGENTS.md AUTO-SKIP annotation; `tests/scenarios/feature.yaml` gained 2 new F-scenarios (`F-verify-human-auto-skip-fires` asserting F11 + auto-skip prose under clean gates, `F-verify-human-auto-skip-blocked` asserting checklist path + no auto-skip prose when boundary applies — initially shaped with `transition_id_any: [F13, F12]` but produced SOFT_PASS per CLAUDE.md "entry-state-vs-exit-state test shape" so reshaped in-place to a human-approves prompt driving `transition_id: F13`); new fixture `tests/fixtures/wip/feature-verify-self-passed-isolated.md` (drive_mode=autopilot, verify-self all `[x]`, phase only adds isolated new artifacts — new preferences module, new admin endpoint, new standalone CLI tool — all no-consumer); existing fixture `tests/fixtures/wip/feature-verify-wire-into-existing.md` updated with drive_mode frontmatter + verify-self advanced to `[x]` with 3 sub-leaves. 2-phase Size-M feature with clean linear flow (no F12 back-loops, no F23 plan revisions); one in-phase fix at P2 verify-self (the F-blocked scenario reshape) handled via the verify-self in-place-fix-shortcut policy. Spec deferred condition (e) probe/decision-artifact gate per user direction — known false-positive: probe phases in autopilot will still auto-skip incorrectly; mitigation is the printed affirmation block as the operator's read-time veto. Shipped as commit `fba842c` on `origin/main`.
- **Backlog resolved:** SURFACE-2026-05-28-VERIFY-HUMAN-AUTO-SKIP-WHEN-NO-INTEGRATION-BOUNDARY — closed by feature `verify-human-auto-skip-when-no-integration-boundary`. Landed scope: 4-gate auto-skip in Mode 3, affirmation block as read-time veto, pause-policy table dual-location updates, 3 structural pins + 2 behavioral scenarios. Deferred scope: condition (e) probe/decision-artifact gate (WP2 false-positive case) — re-open if it bites in practice.
- **Feature shipped:** `long-cmd-timeout-and-exclusive-resource-concurrency` — adds GLOBAL `## Long-running commands` section + `### Runtime registry` subsection to `~/.claude/CLAUDE.md` (via `CLAUDE.snippet.md` + `install.sh`) codifying three rules (consult per-project `runtimes.md` → estimate fallback → explicit Bash `timeout`; never start a second instance against shared exclusive resource — wait for `BashOutput` notification on auto-background; subset-first when uncertain), plus a per-project `<proj-root>/runtimes.md` convention with canonical file shape (`shape: runtime-registry` frontmatter + per-command H2 + `**Last:**` / `**Use timeout:**` / `**History:**` fields) and read+update discipline (formula `ceil(observed_seconds * 1.5 + 60) * 1000` clamped to 600000 ms); this repo's `runtimes.md` seeded with `./tests/check-structure.sh` (232s → use-timeout 408000) and `./tests/run-tests.sh` (3097s clamped to 600000 with explicit auto-background note); 3 new structural `grep_check` pins in `tests/check-structure.sh` (130→132 PASS / 1 unrelated FAIL) guarding the section heading, the subsection heading, and the runtimes.md frontmatter marker; 2-phase Size-M feature with 1 F23 plan revision (Phase 2 added mid-flight at verify-human after user surfaced the registry-mechanism gap — a single-phase prose-only rule degraded to per-session re-estimation); build-time meta-validation — Rule 1 + Rule 2 each exercised end-to-end during the build itself (Rule 1 read `**Use timeout:** 600000` from runtimes.md and applied at P2.7; Rule 2 waited for `BashOutput` completion on the 51.6-min run-tests.sh measurement at P2.4 without re-invoking). Shipped as commit `f0d86ed` on `origin/main`.
- **Backlog resolved:** SURFACE-2026-05-29-LONG-CMD-TIMEOUT-AND-EXCLUSIVE-RESOURCE-CONCURRENCY — closed by feature `long-cmd-timeout-and-exclusive-resource-concurrency`. Phase 1: ecosystem-general 3-rule GLOBAL section in CLAUDE.snippet.md. Phase 2 (added via F23 plan revision at verify-human): per-project `runtimes.md` registry convention + read+update discipline subsection + seed entries for this repo. 3 structural pins codify the section + subsection + file-frontmatter marker.

## 2026-06-06

- **Feature shipped:** v3 WP10 — Day-view row-density mitigation — Day view now auto-hides project rows with zero session activity in the currently visible viewport and sorts surviving rows by latest viewport-overlapping segment end-time descending (alias-alphabetical tiebreak), with a session-local "show all"/"re-enable auto-hide" escape-hatch chip that toggles the entire mitigation off/on for the current page session — resolves the steady-state "too many rows after a week of tracking" pain point (`SURFACE-2026-05-26-CLAUDE-TIME-VIZ-DAY-VIEW-ROW-DENSITY`); strategy (a)+(c) per spec elicitation (viewport-aware auto-hide + recency-sort with escape hatch + Day-only), other options (b min-activity-threshold chip, d virtualization) explicitly out-of-scope; first user-visible UX-polish WP of v3 Phase 3 and the only WP in the cycle whose plan-time decision was non-mechanical; new `_applyRowDensityMitigation(projects, viewport, dwCtx)` pure-function helper in `dashboard.jsx` flattens `sessions.flatMap(s => s.segs)`, applies per-session `day_iso` offset in multi-day mode (matches the existing minimap pattern at dashboard.jsx:3066-3072), viewport-intersects each segment, computes `hasActivity = any(seg overlaps viewport)` and `latestEnd = max(min(seg.end+offset, viewport.visible_end_min))` over overlapping segs, drops `!hasActivity` projects, sorts survivors by `latestEnd` desc then `alias.localeCompare` asc; gated by a new `view` prop on `DayTimeline` (default `'day'`) which `_interactive_dashboard()` passes as `view={isCustom ? 'custom' : 'day'}` so Custom view (also rendered via DayTimeline per `isDayLike` union) bypasses the mitigation; session-local `useState(true)` for `autoHideOn` per spec — no URL hash key reserved (default-elision discipline, the toggle is a "show me everything for a moment" affordance not a persistent user pref); escape-hatch chip renders when `view === 'day'` AND (`droppedCount > 0` OR `!autoHideOn`) — always-render path with `data-row-density-chip` selector + `[data-show-all]` button when `autoHideOn` (text: `<N> project(s) hidden — show all`) + `[data-show-all-off]` button when `!autoHideOn` (text: `Showing all projects — re-enable auto-hide`); new container-level data attrs `data-row-density-mitigation="on"|"off"` + `data-row-density-dropped="<N>"` on the project-rows scroll container, per-row `data-project-row` + `data-project-alias="<alias>"` selectors (React.Fragment → div wrapper to carry the attrs); WP9 per-project filter precedence preserved (mitigation operates on the post-WP9-filter `visibleProjects` list); `viz_render.py::_wire_bar_click` transform anchor updated to match the new `DayTimeline` signature (literal-string match would have silently broken emit otherwise — caught by `test_visualize_cli.sh` first-emit check); +7 source-shape pins in `test_visualize_cli.sh` (205→212/0: `_applyRowDensityMitigation` helper present, DayTimeline signature carries `view='day'` default, `data-row-density-{mitigation,dropped}` container attrs present, chip selectors triple present, per-row `data-project-{row,alias}` markers present, `_interactive_dashboard` routes view by `isCustom` for Day-only scope gate, mitigation useMemo invokes helper with viewport + dwCtx) + 6 behavioral pins in `test_visualize_interactive.js` container suite (89→95/0: Day-view marker + row markers, narrow viewport drops ≥1 project + chip text matches `N projects hidden`, `[data-show-all]` click toggles mitigation off, `[data-show-all-off]` click toggles back on, Week-view has no mitigation marker / no chip / no `data-project-row` Day-only-scope, recency-sort reacts to viewport with morning vs evening slice orderings differing); one existing WP9-P4 outside-click test migrated from `await page.click('[data-seg-id]')` (first-DOM-order, geographically random post-WP10 because recency-sort changes which project's first segment is leftmost) to a `mousedown`-dispatch on a `start < 720` (pre-noon) segment for order-agnostic robustness — Test Triage: obsolete-test high confidence (single sentence: test depended on implicit DOM-order assumption that WP10 intentionally invalidates); one Test Triage entry at codify itself: behavioral 2-6 needed `page.reload()` after hash-only `goto` to force React state re-init from new hash, pattern lifted from earlier v3 pins (e.g. WP9 P2 line 1862) where the same lesson was caught — **5th observed instance of `SURFACE-2026-05-29-WP3-PLAN-DOWNSTREAM-CONTRACT-MISS`** (cross-layer contract migration miss: plan didn't grep `viz_render.py` for literal-string matches against the DayTimeline signature, caught at first emit) — pattern is now firmly established with 5 confirmed instances, the doc-side convention in CLAUDE.md remains the fallback until codified into `feature-plan` SKILL.md as a mechanical step. Shipped as commit `c719e1b` on `origin/main`.
- **Backlog resolved:** SURFACE-2026-05-26-CLAUDE-TIME-VIZ-DAY-VIEW-ROW-DENSITY — closed by v3 WP10 (strategy a+c, viewport-aware auto-hide + recency-sort + session-local escape hatch, Day-only).
- **Milestone:** v3 WP10 — Day-view row-density mitigation completed in the `claude-time-visualize-v3` cycle; **10 of 12 WPs shipped** (Phase 0 ✅, Phase 1 ✅, Phase 2 ✅, Phase 3 1 of 3 — WP10 done, WP11 + WP12 remain). First user-visible UX-polish WP of Phase 3; resolves the only week-of-tracking pain point in the cycle.
- **Feature shipped:** v3 WP11 — Collapsible project rows + per-project pills + away-total visibility — project rows now default to **collapsed** (one row per project, segments merged into a single track via merge-by-kind union of all `kind` in `{active, subagent, reading, thinking, away}` across the project's sessions; multi-day-aware via `dayOffsetForSession`); chevron click expands to per-session rows with hash-key `expanded=alias1,alias2` round-trip (default-elision when set is empty; hash-restore initializer applies on mount; debounced hash-write effect on state change); per-project pill `[data-active-pill]` on the row label renders **filter-aware** `active+subagent` total (chip toggles project through `totalsByProject`); per-project away pill `[data-away-pill]` beside the active pill on BOTH collapsed and expanded rows (Q5=A) using `CT_TOKENS.awayBase` muted background (visually distinct from the vivid `CT_TOKENS.active` blue); `HeadlineCard` extended with **4th tile** labeled `Away` (Q3=A) beside the existing Active session / Human activity / AI effort trio, sourced from `_computeAwayMsForWindow(dayPayloadsByIso, view.window.start, view.window.end, filterKinds)` — same trailing-7-day window the other 3 tiles use; **single-source-of-truth filter gate** — `_computeAwayMsForWindow` AND `_computeProjectAwayMin` both check `filterKinds.away !== false`, so toggling the `away` chip OFF zeros the headline tile AND both per-project pill variants in lockstep; new `CollapsedTrackRow({ project, totals, alt, onToggle, dayOffsetForSession })` component renders the merged-by-kind intervals as colored bands with kind-preserved `segStyle()` styling, render-order `[away, reading, thinking, active, subagent]` so active bursts visually dominate; new selectors `data-collapsed-track`, `data-collapsed-seg`, `data-chevron-toggle`, `data-session-row`, `data-session-id`, `data-away-pill`, `data-expanded` for behavioral pinning; `ProjectHeaderRow` gained `onToggle` prop wiring the chevron span as an interactive control with `role="button"` + `cursor: pointer` + `e.stopPropagation()` to prevent timeline pan-gesture interference; expanded-branch wrapper switched from `<div data-project-row data-expanded="true">` to `<React.Fragment key={p.id}>` to dedupe the `data-project-row` selector (inner `ProjectHeaderRow` already carries it — F9b back-loop fix at Phase 1 verify-self); `viz_render.py::_wire_bar_click` cross-file edit-time transform anchor extended to append BOTH `onSelectSeg` AND `onToggleExpand` to the `DayTimeline` signature at emit time, keeping the source-canvas signature stable so the static design-canvas wrapper (which calls `<DayTimeline>` without those props) keeps working unchanged; spec locked decision set Q1=2 (two phases) / Q2=A (full-window away, frontend-only) / Q3=A (4th tile) / Q4=B (merge-by-kind union) / Q5=A (away pill beside active pill); 2-phase Size-M feature, Phase 1 had 1 F9b back-loop at verify-self catching two source-side selector gaps (P1.4-extension: SessionRow missing `data-session-row`; P1.7-extension: expanded-branch wrapper duplicating `data-project-row`) both auto-fixed in-skill with live-Playwright re-verify gate confirming; 3 Test Triage entries this WP all high-confidence auto-fixes — (a) P1 codify own-test selector typo `[data-filter-chip]` → `[data-filter-kind]`; (b) P1 codify WP9-P4 outside-click migrated to union selector `[data-seg-id], [data-collapsed-seg]` for new collapsed-default state (test pre-dated WP11 and assumed `[data-seg-id]` was always in DOM); (c) P2 codify WP10-P2 HeadlineCard `tileIds.length === 3` assertion relaxed to `>= 3` to accommodate WP11's 4th `away` tile — **5th observed instance of `SURFACE-2026-05-29-WP3-PLAN-DOWNSTREAM-CONTRACT-MISS` family** introducing a NEW subcase (array-length-add, distinct from the literal-payload-object key-add subcase tracked at WP5); CLI 212 → 230/0 (+18 source-shape pins across P1 + P2: CollapsedTrackRow + _mergeProjectIntervalsByKind + setExpandedProjects toggle + DayTimeline signature with onSelectSeg+onToggleExpand + collapsed-track/seg/chevron selectors + session-row selectors + data-active-pill emitted on both row variants + React.Fragment wrapper regression-pin + DayTimeline onToggleExpand wiring + hash-write default-elision + _computeAwayMsForWindow helper + _computeProjectAwayMin helper + HeadlineCard tiles array id:'away' entry + HeadlineCard signature awayMs prop + awayMs plumb call + data-away-pill on both row variants + CT_TOKENS.awayBase background regression-pin + filterKinds.away !== false gate); behavioral 95 → 106/0 (+11 net WP11 pins; 2 obsolete-test migrations in-flight: WP9-P4 outside-click + WP10-P2 HeadlineCard 3-tile assertion); Python (viz_data 79/0 + viz_render 10/0 + reclassify 42/0) unchanged. Shipped as commit `f8f6b3a` on `origin/main`.
- **Milestone:** v3 WP11 — Collapsible project rows + per-project pills + away-total visibility completed in the `claude-time-visualize-v3` cycle; **11 of 12 WPs shipped** (Phase 0 ✅, Phase 1 ✅, Phase 2 ✅, Phase 3 2 of 3 — WP10 + WP11 done, WP12 multi-instance overlap viz remains). Second user-visible UX-polish WP of Phase 3; resolves v2 WP13 (SUPERSEDED 2026-05-26) carry-over scope.
- **Feature shipped:** v3 WP12 — Multi-instance overlap visualization — when two or more Claude Code sessions ran in parallel on the same wall-clock minute, the dashboard now detects the overlap client-side at render time (`_detectSessionOverlaps`, O(N²) pairwise across all viewport-visible projects' sessions, day_iso-aware predicate for cross-day Custom-view aggregation, filter-gated to return `{}` when both active+subagent kinds are off) and surfaces it across four UI affordances: (1) **collapsed-row marker** — subtle 2px red-ish vertical hairline on `CollapsedTrackRow`'s merge-by-kind band at each within-project overlap interval, **scoped to same-project peers only** per user direction at P2 verify-human (two terminals open in the same cwd is the actionable signal — cross-project overlaps belong on the other surfaces), `data-overlap-marker` + `data-overlap-peer="<peer_id>"` selectors + hover tooltip "Overlaps with <peer_id> for Nmin"; (2) **expanded-row overlay** — `OverlapOverlayLayer` renders a translucent reddish bottom-half strip in `SessionRow`'s timeline column at each peer-overlap subrange (ALL peers including cross-project — overlay surface is project-agnostic by design), `data-overlap-peer` + `data-overlap-start` + `data-overlap-end` selectors covering the half-row height so the underlying full-height `SegmentBar` is unchanged; (3) **HeadlineCard 5th tile** — `data-metric-tile="parallel"` shows `_computeOverlapMsForWindow(dayPayloadsByIso, view.window.start, view.window.end, filterKinds)` total (sums per-session `overlapMs` across all days in window ÷ 2 for pair-double-counting), same trailing-7-day window the other 4 tiles use, same filter-gating discipline as `_computeAwayMsForWindow` (single source of truth — divergence between tile and per-row rendering is structurally prevented); (4) **SidePanel "Overlaps with" section** — `data-side-panel-overlaps` conditional section renders when peers exist for the selected session, lists each peer as `[data-overlap-peer-row="<id>"]` with the 8-char `id` + `HH:MM–HH:MM` range + `Nmin` duration (cross-project peers included — section is project-agnostic by design); new `OverlapsContext = React.createContext(null)` carries `{ overlaps: {sid: {overlapMs, peers: [...]}}, sessionToProject: {sid: alias} }` lifted up to wrap both `DayTimeline` AND `SidePanel` at `_interactive_dashboard` (P2.3 build-time refactor — SidePanel is a SIBLING of DayTimeline so the Provider must sit one level above both); `OverlapMarkerLayer` filters `sessionToProject[peer.id] !== project.alias` to enforce same-project scope at render time without changing the detector's project-agnostic output (kept as single source of truth for the other 3 surfaces); demo data `viz/data.js` extended with `s8` (claude-time, 22:00–23:00) + `s9` (agent-handoff-protocol, 22:30–23:30, 30min cross-project overlap with s8) + `s10` (claude-time, 22:15–22:45, 30min within-project overlap with s8) so `claude-time visualize --demo` end-to-end smoke-renders both overlap types without requiring a real DB; demo `hour_range` extended `[6, 23]` → `[6, 24]` to fit late-night sessions; spec locked decision set Q1=marker-on-collapsed (collapsed mode prioritizes density — half-height stagger inside a single-line track becomes unreadable; marker is existence signal, expansion is inspection affordance) + Q2=client-side-detection (reclassifier's `cross_session_overlap_ms` covers gap-overlap math only, NOT session-interval overlap during active bursts — they're orthogonal; client-side detection is contained and re-evaluates on filter change naturally via `useMemo`) + Q3=demo-data-extension (smoke-test end-to-end on a clean checkout); HeadlineCard latent empty-caption code bug surfaced + fixed in-place at verify-self — `tiles.every(t.value_ms === 0)` over 5 tiles was wrong since counterweight tiles (away, parallel) compute INDEPENDENTLY of the `metrics` aggregator; was masked at WP11 P2 because the 4th tile happened to be 0 on demo data, exposed when WP12's 5th tile had 30m of overlap data on demo (`isEmpty` returned false → caption suppressed even though all 3 primary metrics were 0); fixed by restricting `isEmpty` to PRIMARY tile IDs `['engaged-session', 'human', 'ai-effort']` via new `isPrimary(t)` helper and gating em-dash placeholder per-tile (counterweights now render their actual values regardless of primary-empty state); 2-phase Size-M feature with 1 F12 verify-human back-loop (within-project marker scope refinement) handled via build → verify-auto → verify-self → verify-human re-entry chain — refined OverlapMarkerLayer to filter same-project peers + extended OverlapsContext with `sessionToProject` map + added s10 to demo for testable within-project pair + updated 4 affected behavioral pins (1: 30m→75m parallel total, 2: marker scope assertions, 3: 3-peer expanded overlay set, 4: 2-peer SidePanel section); 8 Test Triage entries at codify formalizing in-flight auto-fixes — 5 obsolete-tests + 1 real code regression (HeadlineCard empty-caption) + 1 own-test regex fix (P2 behavioral 6's `/0m/` regex vs actual `0s` render from `_fmtDurMs(0)`) + 1 pre-existing future-proof relaxation; **3 new instances of `SURFACE-2026-05-29-WP3-PLAN-DOWNSTREAM-CONTRACT-MISS` family** during this WP (6th = WP12 P2 array-length subcase, 7th = WP12 P2 literal-function-signature subcase, 8th = WP12 P2 F12 back-loop literal-variable-binding-name subcase) — total 8 observed instances across 4 subcases now documented in CLAUDE.md convention bullet with the 4-subcase grep recipe; **3rd cumulative instance of `SURFACE-2026-05-29-VERIFY-SELF-IN-PLACE-FIX-SHORTCUT-POLICY` observed friction pattern** across the v3 cycle (past the rule of three — worth promoting to official policy in a future cycle); CLI 230 → 248/0 (+8 P1 codify + +10 P2 codify + 0 net obsolete-test relaxations = +18 source-shape pins across both phases), behavioral 106 → 112/0 (+6 P2 behavioral pins exercising HeadlineCard 5-tile / within-project marker scope / expanded overlay 3-peer set / SidePanel section presence + omission paths / filter-gating to zero), Python (viz_data 79/0 + viz_render 10/0 + reclassify 42/0) unchanged. Shipped as commit `1bfb96f` on `origin/main`.
- **Milestone:** v3 WP12 — Multi-instance overlap visualization completed in the `claude-time-visualize-v3` cycle; **12 of 12 WPs shipped — v3 cycle COMPLETE** (Phase 0 ✅, Phase 1 ✅, Phase 2 ✅, Phase 3 ✅ all 3 — WP10 + WP11 + WP12 done). Third user-visible UX-polish WP of Phase 3; resolves v2 WP12 (SUPERSEDED 2026-05-26) carry-over scope. v3 cycle deliverables across 6 weeks: range-aware data layer (WP1-2), unified CLI surface (WP3-4), 5 frontend sub-payload routings (WP5-9), 3 UX polish (WP10-12). Cycle close on `/product-finalize` — resync durable docs + archive cycle-scoped docs to `docs/product/archive/claude-time-visualize-v3/`.
- **Product cycle complete:** claude-time-visualize-v3 — 12 of 12 WPs shipped across 4 phases (Phase 0 data layer, Phase 1 CLI surface, Phase 2 frontend sub-payload routing, Phase 3 UX polish), spanning 2026-05-26 (v3 WBS approved) through 2026-06-06 (WP12 ship). The cycle inverted the v2 emit model — one CLI invocation now emits a 90-day default window (MTD-2, calendar-anchored) with ALL sub-payloads pre-rendered, and the frontend handles every Day/Week/Month/Compare/Custom slice as instant client-side state swaps — unlocking instant Day-arrow nav, instant Week-arrow nav, accurate hash-restore for shareable URLs, content-refresh on every preset sub-tab click (resolves v2 WP11 P2A.verify-human.3 PARTIAL finding), plus v2-deferred UX (multi-instance overlap viz, collapsible rows, per-project pills, away-total visibility) and v2-surfaced pain points (Day-view row density). Trade-off: +~500ms emit time + +~200-400KB emit size, both user-confirmed acceptable. Final test baselines at cycle close: CLI source-shape **248/0**, container behavioral **112/0**, Python **131/0**. Durable docs (`arch.md`, `vision.md`, `roadmap.md`, `transitions.md`) describe the workflow harness and were not modified by this dogfooding cycle. Cycle-scoped `wbs.md` archived to `docs/product/archive/claude-time-visualize-v3/`.
- **Feature shipped:** `docker-init-randomize-host-ports` — `skills/product-context/SKILL.md` Variant A (Docker Mandate) gains a "Randomize host ports" guidance bullet directing project-init to randomize every host:container left-hand port from the ephemeral range (49152-65535) rather than using well-known defaults, with pick-and-verify commands and an inline-comment example, to prevent the multi-project host-port collision failure mode observed in Replicator-1.0 (`5173:5173` collided with another local Vite project and verify-self landed in the wrong app). Initial plan placed the rule in `CLAUDE.snippet.md` + a `feature-verify-self` detection clause; both placements rejected at verify-human (global snippet is wrong namespace for per-project init; verify-self is observe-only and detection logic belongs in a future `debug-*` sidebar) and the F23 plan revision relocated scope to the single product-context bullet. Shipped as commit `5872554` on `origin/main`.
- **Backlog resolved:** SURFACE-2026-06-06-DOCKER-INIT-RANDOMIZE-HOST-PORTS — closed by feature `docker-init-randomize-host-ports`. Landed scope: short guidance bullet in `skills/product-context/SKILL.md` Variant A. Deferred scope: verify-self detection clause dropped per user direction (right home is a future `debug-environment-misrouting` sidebar if pattern recurs).
- **Task closed:** `codify-randomize-host-ports-test-coverage` — added two structural `grep_check` pins in `tests/check-structure.sh` as the regression net for the randomize-host-ports bullet (bite-verified to fire on bullet deletion: 126 PASS → 124 PASS + 2 new FAILs when mutated, restored to 126 PASS), extended the SKILL.md bullet with a Maintenance sentence so the rule persists for future agent additions to existing `docker-compose.yml` files (not just project init), and surfaced two harness limitations to backlog (`VERIFY-SH-NO-HARD-CONTENT-ASSERT` — `verify_result` short-circuits on `transition_id` match so `contains_any` cannot hard-assert content; `RUN-ALL-UNBOUND-FORWARD-ARGS` — `tests/run-all.sh` crashes on empty FORWARD_ARGS under `set -u`); a behavioral P10b scenario was attempted with both weak and SKILL.md-literal-prose anchors but both failed bite-verification (verify.sh short-circuit + model-prior surfacing), removed entirely rather than ship confidence theater.

## 2026-06-03

- **Feature shipped:** v3 WP5 — Day view sub-payload routing — Day view now reads pre-rendered day payloads from `window.CT_DATA.day_payloads_by_iso[dayIso]` instead of the v2 `today` alias key, adds ‹/› client-side Day-nav buttons to the toolbar's date strip (disabled at window boundaries, no out-of-window reload-redirect — in-window-only nav for Day arrows), and introduces a `date=YYYY-MM-DD` URL hash key with default-elision when `dayIso === window.end` (sharable URLs round-trip correctly across reloads / new tabs); first frontend WP of v3 Phase 2 — establishes the sub-payload routing pattern that WP6/WP7/WP8/WP9 will copy; `today` alias key stays populated by the CLI for v2-frontend coexistence (WP9 verify-codify removes it); 6 Day-view-only `today.X` reads in `viz_render.py::_interactive_dashboard` migrated to `dayPayload.X` (selected-session resolution, dayTotals, dateLabel, date-header strip, ProjectFilterPopover projects prop, DayTimeline + Minimap data); `dashboard.jsx::Toolbar` gained a new `view === 'day'` ternary arm rendering ‹ button + IconCalendar+dateLabel span + › button (mirrors the existing month-nav visual treatment, individual props `dayIso`/`onPrevDay`/`onNextDay`/`prevDayDisabled`/`nextDayDisabled`); +4 source-shape pins in `test_visualize_cli.sh` (178→182), +9 behavioral pins in `test_visualize_interactive.js` container suite (46→55); CLAUDE.md "Per-consumer URL-hash key reservations" table gained a `v3 WP5 (day iso)` row; 2 Test Triage entries handled in-phase — (a) existing 5-branch hash-dispatch pin obsolete (high confidence) due to new `date:` key, updated to 6-key shape; (b) WP5 behavioral 3a/3b obsolete (high confidence) because `page.goto(#hash)` is same-document nav and doesn't re-mount, added `page.reload()` to force fresh mount of the hash-reading `useState` initializer; reinforces `SURFACE-2026-05-29-WP3-PLAN-DOWNSTREAM-CONTRACT-MISS` as a 4th instance (pattern now well-established — convention exists in CLAUDE.md but planning skills don't yet execute it as a mechanical step). Shipped as commit `820cba7` on `origin/main`.
- **Milestone:** v3 WP5 — Day view sub-payload routing completed in the `claude-time-visualize-v3` cycle; Phase 2 (frontend state-routing refactor) entry-point WP done, unblocks the parallelizable WP6 (Week) / WP7 (Month) / WP8 (Compare) / WP9 (Custom-range) which all copy the WP5 sub-payload routing pattern.
- **Feature shipped:** v3 WP6 — Week view sub-payload routing + Week-arrow nav — Week view now reads pre-rendered week payloads from `window.CT_DATA.week_payloads_by_monday[mondayIso]` instead of the v2 `week` alias key, adds ‹/› client-side Week-nav buttons to the toolbar's date strip (disabled at window boundaries, in-window-only nav mirrors WP5 Day pattern), and introduces a `week=YYYY-MM-DD` URL hash key (Monday-anchored) with default-elision when `mondayIso === current_week_monday` (the Monday of the ISO-week containing `window.CT_DATA.window.end`); second frontend WP of v3 Phase 2 — mechanical analog of WP5; `week` alias key stays populated by the CLI for v2-frontend coexistence (WP9 verify-codify removes it); 9 Week-view-specific `week.X` reads in `viz_render.py::_interactive_dashboard` migrated to `weekPayload.X` (weekActiveTotal/weekProjectActive/weekActiveDays calc block, dateLabel ternary, date-header strip label, projects.length count, ProjectFilterPopover projects prop, `<WeekTimeline data={...} />`); `dashboard.jsx::Toolbar` gained a new `view === 'week'` ternary arm (second arm, between Day and Month) rendering ‹ button + IconCalendar+dateLabel span + › button (mirrors WP5 visual treatment exactly, individual props `mondayIso`/`onPrevWeek`/`onNextWeek`/`prevWeekDisabled`/`nextWeekDisabled`); +4 source-shape pins in `test_visualize_cli.sh` (182→186; the existing five-branch hash-dispatch pin updated in-place from six-key to seven-key per the plan-time literal-payload-object grep pre-empt — zero Test Triage entries at verify-codify), +9 behavioral pins in `test_visualize_interactive.js` container suite (55→64, all PASS on first run because `page.reload()` workaround was baked in from the start via the WP5 verify-codify lesson); CLAUDE.md "Per-consumer URL-hash key reservations" table gained a `v3 WP6 (week monday)` row + 2 convention bullets from prior session (literal-payload-object grep + v3 useMemo fallback pattern) picked up per the WP4 finalize precedent; cleanest WP cycle in v3 so far — both plan-time conventions persisted at WP5 paid off immediately (zero verify-codify back-loops), validating the convention-validation-lap hypothesis (mechanical-analog WP after a learning-heavy WP should be near-frictionless). Shipped as commit `36ad7a6` on `origin/main`.
- **Milestone:** v3 WP6 — Week view sub-payload routing completed in the `claude-time-visualize-v3` cycle; 6 of 10 WPs shipped (Phase 0 ✅, Phase 1 ✅, Phase 2 2 of 5 — WP5 + WP6 done, WP7/WP8/WP9 remain).
- **Feature shipped:** v3 WP7 — Month view sub-payload routing — Month view now reads pre-rendered month payloads from `window.CT_DATA.month_payloads_by_iso[monthIso]` instead of the v2 `months` alias key while preserving the existing in-window/out-of-window dispatch (`onPrevMonth`/`onNextMonth` swap client-side when target month is in the v3 map; emit `MonthNavToast` with a `claude-time visualize --window <iso>-01:<iso>-LL` command for out-of-window targets); third frontend WP of v3 Phase 2 — mechanical analog of WP5 (Day) + WP6 (Week) with one wrinkle preserved from v2: the existing Toolbar Month-nav UI (`data-month-nav="prev"|"next"`, `data-month-iso=<iso>` at `dashboard.jsx:421-440`) was already in place from v2 so the migration cost reduced to just (a) swap the in-window-presence check from `monthsMap[X]` to `monthPayloadsByIso[X] || (monthsMap && monthsMap[X])` at the dispatch site + (b) migrate 4 consumer-site expressions (date-header label, projects-count, ProjectFilterPopover, MonthView render-guard + payload prop) to the new `monthPayload` useMemo accessor; `months` alias key stays populated by the CLI for v2-frontend coexistence (WP9 verify-codify removes it); `_initMonthIso` IIFE rewritten to validate `hash.month` against `monthPayloadsByIso` first then `monthsMap` fallback, default landing chain `currentMonthIso` (= `windowEndIso.slice(0,7)`) → first sorted key of `monthIsoKeys` → v2 fallback; `_initView`'s month-precondition guard refactored to a shared `_hasMonthData = monthIsoKeys.length > 0 || (monthsMap && Object.keys(monthsMap).length > 0)` helper used at both hash-restore and `CT_INITIAL_VIEW` sites; +4 source-shape pins in `test_visualize_cli.sh` (186→190; existing 7-key hash-write five-branch dispatch pin needed ZERO updates because WP7 added no new URL hash key — `month:` was already in the dispatcher from WP6, confirmed at plan-time literal-payload-object regrep), +5 behavioral pins in build P1.10 (in-container Playwright suite asserting `month_payloads_by_iso` 2-month emit coverage, initial monthIso = window.end month, prev-month in-window swap with hash update, and crucially `WP7 behavioral 2c` asserting the prev-month iso is a key in `window.CT_DATA.month_payloads_by_iso` — verifies the migration is *live*, not just the v2 alias still serving) + 3 behavioral pins in verify-codify codifying the human-confirmed `P1.verify-human.4` out-of-window hash fallback (`#month=2099-01` → graceful fallback to valid in-window month, grid renders day cells, zero JS console errors); container suite 64→72/0, all 8 new behavioral pins PASS on first run (no Test Triage replay needed, `page.reload()` workaround baked in from start via WP5 lesson); ZERO Test Triage entries at verify-codify — third consecutive WP cycle (WP5, WP6, WP7) shipping with zero back-loops under autopilot drive mode, validating the combined plan-time discipline (literal-payload-object grep pre-empt + downstream-contract-impacts pass) + build-time discipline (page.reload baked into behavioral pins) as a stable equilibrium. Shipped as commit `51d2393` on `origin/main`.
- **Milestone:** v3 WP7 — Month view sub-payload routing completed in the `claude-time-visualize-v3` cycle; 7 of 10 WPs shipped (Phase 0 ✅, Phase 1 ✅, Phase 2 3 of 5 — WP5 + WP6 + WP7 done, WP8/WP9 remain in Phase 2; Phase 3 — WP10/WP11/WP12 — unblocked once Phase 2 completes).
- **Feature shipped:** v3 WP8 — Compare view sub-payload routing — `CompareView` now reads `window.CT_DATA.compare_payloads_by_preset[currentPreset]` via a new `comparePayload` useMemo (v2-alias fallback to `window.CT_DATA.comparison` per the WP5–WP9 sub-payload routing convention) instead of the v2-alias `comparison` key directly — preset sub-tab click (wow / today-vs-trailing / mom) now produces an instant client-side *content* swap, not just a hash + active-attr swap; resolves the v2 WP11 P2A.verify-human.3 PARTIAL finding (preset content-not-refreshing) that was the original trigger for the v3 architectural pivot; fourth frontend WP of v3 Phase 2 and the **first non-mechanical-analog** WP in v3 (resolves a real v2 limitation, has 4-preset state with mixed pre-rendered vs re-emit semantics) — the WP5–WP9 useMemo pattern applied cleanly anyway, validating that "easy WPs were easy" was an incomplete characterization (the pattern is genuinely portable, not just suited to mechanical analogs); Custom preset preserved as-is — useMemo's null-fallback returns `window.CT_DATA.comparison` (= wow alias) for `preset === 'custom'`, keeping the v2 RangePicker pair + reload-redirect-toast flow intact (instant Custom swap is WP9's concern); `comparison` top-level alias key stays populated by the CLI as `compare_payloads_by_preset.wow` for v2-frontend coexistence (WP9 verify-codify removes it); v2 archive annotation at `docs/product/archive/claude-time-visualize-v2/wbs.md:224` updated with a "RESOLVED 2026-06-03 by v3 WP8 (commit `ab65a26`)" sub-bullet linking the v2 known-limitation finding to the v3 ship; ZERO new URL hash keys (the `preset:` and `ranges:` keys were already in the compare branch from v2 WP11 — confirmed at plan-time literal-payload-object regrep — the literal-payload-object hash-write pin at `test_visualize_cli.sh:988-996` needed ZERO updates, second consecutive WP to leave the hash-write pin untouched after WP7); +4 source-shape pins in `test_visualize_cli.sh` (190→194: `comparePayloadsByPreset` sibling var, `comparePayload` useMemo with v2-alias fallback, JSX consumer routed via `comparePayload`, legacy `<CompareView comparison={window.CT_DATA.comparison}>` form GONE regression-pin), +6 behavioral pins in `test_visualize_interactive.js` container suite (72→78: WP8 1a-1f covering content-differs-on-preset-click via the `[data-compare-row="engaged-session"] [data-compare-col="a"]` selector — round-trip across wow → today-vs-trailing → mom → wow asserts each preset click produces a DOM-text-content swap, plus Custom-preset RangePicker mount check + no-console-errors invariant); two verify-auto iterations needed to find the right metric row (`parallelism-multiplier` collapses to "1.00×" on low-density fixtures where each window has at most one session per day, `human` collapses to "0m" since seeded sessions classify as `ai_agent` not `human`; `engaged-session` was the only row whose composite cell content reliably differed across the three A-windows on the existing 14-day compare_real fixture) — caught at verify-auto exactly as designed (fast indicator before verify-self), documented in the test file comment for posterity, surfaces a candidate global learning ("behavioral-test row-choice has fixture-density coupling: check the metric's derivation chain against the fixture seed at plan time") not yet persisted; the Subagent Re-Verification Heuristic fired at verify-self when the subagent reported BLOCKING FAIL on outcome 5 (fresh-load `#preset=mom` hash-restore) — direct orchestrator-side re-verification in a fresh browser context PASSed (subagent's FAIL was Playwright SPA-style hash-only nav state-carryover from outcome 4's `preset=custom;ranges=...` ending state, not a real bug), saving one back-loop to build hunting a non-bug in `_initPreset` — heuristic remains load-bearing; ZERO Test Triage entries at verify-codify — **fourth consecutive WP** (WP5, WP6, WP7, WP8) shipping with zero back-loops under autopilot, the convention-validation-lap pattern (parked at 2 confirmations after WP6/WP7) now has 4 consecutive applications. Shipped as commit `ab65a26` on `origin/main`.
- **Milestone:** v3 WP8 — Compare view sub-payload routing completed in the `claude-time-visualize-v3` cycle; 8 of 10 WPs shipped (Phase 0 ✅, Phase 1 ✅, Phase 2 4 of 5 — WP5 + WP6 + WP7 + WP8 done, only WP9 remains in Phase 2; Phase 3 — WP10/WP11/WP12 — unblocked once WP9 ships). **Resolves the v2 WP11 P2A.verify-human.3 PARTIAL finding** — preset content-refresh was the original trigger for the v3 architectural pivot, now operationally resolved by WP8 ship.
- **Feature shipped:** v3 WP9 — Custom-range view sub-payload routing + v2 alias-key strip — two-phase WP closing the v3 sub-payload routing convention. **Phase 1** (Custom-view routing): new `_aggregateDayPayloads` JS helper does cross-day union over `day_payloads_by_iso` for `[range.start..range.end]`, tagging sessions with `day_iso` so multi-day renderers place them on the correct row (JS-side analog of Python's `build_range_data` aggregation); new `customPayload` useMemo + `dayLikePayload = isCustom ? customPayload : dayPayload` routing slot substituted at all 6 `isDayLike` consumer surfaces (dayStats, date-header label + count, ProjectFilterPopover, body DayTimeline + EmptyState, Minimap data + render-gate); `onRangeChange` extended with out-of-window detection that triggers `setNavToast` with the `claude-time visualize --window <start>:<end>` reload command; new `data-custom-range` selector on the date-header strip. **Phase 2** (v2 alias-key strip + useMemo fallback cleanup): the v2 alias keys `today`, `week`, `comparison`, `months`, `meta` removed from CLI emit at v3 cycle close — kept top-level `snapshot` field as a 1-field replacement for `meta.snapshot` since `meta.{start,end,day_count}` were duplicates of `window.{start,end,day_count}`; `const { today, week } = window.CT_DATA` destructure removed from `_interactive_dashboard`; all 4 useMemo v2-alias fallbacks pruned (each useMemo now primary-only with defensive empty-shape return); 7 init paths migrated off alias keys onto canonical sub-payload maps (`_initView`, `_initCompareRanges`, `_initMonthIso`, `range`, `expandedProjects`, `_defaultViewport`, `onPrev/NextMonth`); **new cross-file consumer discovered at P2.1 audit**: `dashboard.jsx::_initialViewport` was NOT in original P2.4 enumeration but read `window.CT_DATA.today.{target_iso, meta, hour_range_by_day, day_window, hour_range}` — absorbed into P2.4 scope, migrated to `day_payloads_by_iso[window.end]`, dead v2 multi-day `target_iso` branch pruned (`build_window_data` never sets it); Toolbar `snapshot` prop migrated from `(window.CT_DATA.meta && window.CT_DATA.meta.snapshot) || null` to `window.CT_DATA.snapshot`. **WP9 reveals + fixes a latent WP5 bug** — Custom view was reading `dayPayload` which fell back to `today` (single day at window.end) instead of multi-day aggregation; users didn't notice because the visible data still "looked day-like" but project/session counts and rendered segments were for a single day, not the picked range — WP9 Phase 1 is the WP that actually fixes Custom view for v3. +6 source-shape pins in `test_visualize_cli.sh` (199→205: alias-key absence audit, top-level snapshot field present, destructure removed, useMemo fallback expressions all removed (5 patterns), `_initView` custom branch migrated, Toolbar snapshot prop sourced from new path) + 9 obsolete-test flips (WP6 `.today` key, WP11-P1 comparison alias, WP11-P1B `{a,b}.metrics` × 3 migrated to `compare_payloads_by_preset.wow.{a,b}.metrics`, WP3-P2 emit boundary contract flipped to v3-only, WP7 monthPayload useMemo + onPrev/NextMonth dispatch, WP9-P1.6b customPayload useMemo) handled as **single contract-migration Test Triage entry** (high confidence, treating 9 sites as one root cause preserves audit trail without scattering the same justification); +5 WP9-P1 behavioral pins + +6 WP9-P2 behavioral pins in `test_visualize_interactive.js` container suite (78→89). CLAUDE.md "v3 sub-payload routing pattern: useMemo with v2-alias fallback" convention retired as **HISTORICAL** (applied to 5 of 5 sub-payload views Day/Week/Month/Compare/Custom; status block + retirement-condition prose added; pattern history preserved for future transition cycles as a template-pattern reference). The bridging useMemo's v2-alias fallback turned out to be the *correctness mechanism* for Custom-view coexistence during WP5–WP9-P1 (not just defensive belt-and-suspenders as originally framed at WP5 plan) — `_aggregateDayPayloads` made that correctness explicit, and Phase 2 could safely strip the alias keys. ZERO Test Triage entries at codify itself (the contract-migration triage was at build time). Final test baselines at ship: CLI 205/0, container interactive 89/0, Python 89/0, structure 92/0. Shipped as commit `98546b2` on `origin/main`.
- **Milestone:** v3 WP9 — Custom-range view sub-payload routing + v2 alias-key strip completed in the `claude-time-visualize-v3` cycle; **9 of 10 WPs shipped** (Phase 0 ✅, Phase 1 ✅, Phase 2 ✅ — all 5 of 5 WPs done; only Phase 3 — WP10 + WP11 + WP12 — remains). **Closes the WP5–WP9 sub-payload routing convention** — pattern applied to 5 of 5 sub-payload views, v2 alias keys stripped at v3 cycle close, convention note retired as HISTORICAL. Phase 3 (UX polish: row-density mitigation + collapsible project rows / pills / away total + multi-instance overlap viz) is unblocked.

## 2026-05-29

- **Feature shipped:** v3 WP3 — Unified `--window` flag (MTD-N / Nd / explicit range) — replaces the v2 sprawl of `--date`/`--week`/`--month`/`--range`/`--compare`/`--compare-range`/`--context-days-*` with a single `--window` flag accepting three parser-discriminated forms: `MTD-N` (month-to-date plus N prior calendar months, ending today), `Nd` (rolling N days ending today), and `YYYY-MM-DD:YYYY-MM-DD` (explicit inclusive range); default when omitted is `MTD-2` (current calendar month + 2 priors, calendar-anchored — chosen at spec time over rolling-90 so Month-view payloads always contain full prior months, not a near-empty leading month); new config key `viz_window_max_days` default 365 caps explicit ranges; `--window` routes through `build_window_data` (v3 WP1) to produce the pre-rendered sub-payload maps AND populate legacy alias keys (`today`, `week`, `comparison`, `metrics`, `meta`, `months`) so the v2 frontend keeps rendering until WP5–WP9 wire the new maps (WP9 verify-codify removes the aliases); `--window` + `--demo` is rc=2 with a guard error while bare `--demo` continues to use the v2 demo path; legacy v2 flags coexist as silent no-ops in WP3 and get deleted entirely in WP4; Phase 2 verify-codify cleanup deleted 33 obsolete v2-flag test scenarios from `test_visualize_cli.sh` and rerouted 6 source-HTML emissions from v2 flags to `--window 7d` so downstream source-shape pins (renderer multi-day, RangePicker UI, MonthView UI) survive; +11 new shell-test scenarios (1 help-text contract + 2 boundary-contract + 8 parser-correctness) end-state `test_visualize_cli.sh` 175/0, Python suite 130/0, structure pins 125/0. Shipped as commit `b7718ae` on `origin/main`.
- **Milestone:** v3 WP3 — Unified `--window` flag completed in the `claude-time-visualize-v3` cycle; Phase 1 of v3 now has 1 of 2 WPs shipped (WP3 done, WP4 legacy flag removal next), unblocking Phase 2 (WP5–WP9 frontend sub-payload routing) and Phase 3 (UX polish).
- **Feature shipped:** v3 WP4 — Legacy flag removal (`--date`/`--week`/`--month`/`--range`/`--compare`/`--compare-range`/`--context-days-prior`/`--context-days-after`) — 8 v2 view-selecting flags deleted from the `visualize` subparser; their ~333-line handler branch in `_cmd_visualize` and 5 v2-only helper functions (`_parse_month_flag`, `_prev_month`, `_parse_range_flag`, `_compare_window_bounds`, `_build_demo_comparison`, `_parse_compare_range_flag`) deleted (~270 LOC); `--demo` absorbed into the v3 branch (uses `_load_demo_data` to graft demo segments into `day_payloads_by_iso[today]` and synthesizes the v3 sub-payload skeleton so the consumer contract is uniform); top-of-file `viz_context_days_prior`/`after` config keys kept for back-compat with user `~/.claude-time/config.json` files but no longer consumed; user-facing toast strings in `viz_render.py` (`_navCmd` replaced with purpose-specific `_navCmdForDay` → `--window iso:iso` and `_navCmdForMonth` → `--window M-01:M-LL` with leap-year-correct last-day math) and `viz/dashboard.jsx` (CompareView empty-state, MonthView source comment) rerouted to `--window` equivalents; README Dashboard section examples updated to `--window` form; 17 obsolete test scenarios rewritten (WP11-P1/P1B Compare-view shape pins via `--window 30d` real-DB instead of `--demo --compare wow`; WP11-P2A effectiveness-panel JSX pins via real-DB emit) + 2 adaptive-hour-ruler scenarios merged + 3 obsolete `--help` shape pins deleted + 8 interactive-test JS invocations + 2 toast/preset assertions rerouted; mid-Phase-3 contract regression caught + auto-fixed (`comparison.{a,b}.metrics` silently dropped on real-DB emit because Phase 1 deleted the v2 CLI-layer `_metrics_for_window` attachment without restoring it at the data-layer; fixed by attaching `build_metrics()` per preset window inside `build_window_data`, codified by new `test_compare_payloads_attach_per_window_metrics` unit test in `BuildWindowDataTests`); net commit -360 LOC (595 insertions / 955 deletions); final test baseline `test_visualize_cli.sh` 178/0, Python 131/0, `test_visualize_interactive.sh` (container) 46/0, structure 125/0. Shipped as commit `04386d9` on `origin/main`.
- **Milestone:** v3 WP4 — Legacy flag removal completed in the `claude-time-visualize-v3` cycle; **Phase 1 of v3 now fully shipped** (WP3 + WP4), unblocking Phase 2 (WP5–WP9 frontend sub-payload routing) which can now be entered without dragging the v2 CLI surface along.

## 2026-05-28

- **Feature shipped:** v3 WP1 — Unified emit-window coordinator (`build_window_data`) — new top-level coordinator in `tools/claude-time/viz_data.py` that takes `(start_iso, end_iso)` and pre-renders the full v3 emit payload: `day_payloads_by_iso`, `week_payloads_by_monday`, `month_payloads_by_iso`, `compare_payloads_by_preset` (wow / today-vs-trailing / mom anchored on window end), plus window-level `metrics`; delegates to the existing `build_day_data` / `build_range_data` / `build_week_data` / `build_comparison_data` / `build_metrics` workers (mirrors v2 `build_comparison_data`'s coordinator-on-top-of-workers pattern); +6 `BuildWindowDataTests` covering empty 3-day shape, single-day shape, "anchor = window end (not real-world today)" semantic, metrics cross-check against direct `build_metrics` call, end<start ValueError, and 90-day smoke (130/0 Python, 122/0 structure check); WBS WP4 revised mid-cycle from "advisory aliases" to "legacy flag removal" per user direction (single-user tool, no external consumers); WP1 is the data-layer foundation — CLI wiring lands at WP3, frontend consumers at WP5–WP9.
- **Milestone:** v3 WP1 — Unified emit-window coordinator (`build_window_data`) completed in the `claude-time-visualize-v3` cycle; Phase 0 (data layer) entry-point WP done, unblocks WP2 perf probe and downstream Phases 1–3.
- **Feature shipped:** v3 WP2 — Emit-time perf probe + 90-day default CONFIRMED — new measurement script `tools/claude-time/test/perf_window_data.py` runs `build_window_data` 5 times against the user's real DB across 30/60/90/120-day windows and reports wall-clock + emit-size as a Markdown table; first run (2026-05-28) showed 90-day window emits in avg 1012ms (50% of the 2s budget) at 431KB (86% of the 500KB budget) with the unexpected finding that the curve is essentially flat across 30→120 days — cost is dominated by fixed overhead (SQLite open, compare-preset compute, import-time machinery), not per-day work, so the window-size knob is approximately free in this range; decision = 90-day default CONFIRMED, recorded in WBS Phase 0 (no changes to Decisions-locked #1 or WP3.4); codified as +3 durable pins in `tests/check-structure.sh` Phase 5b (existence, py_compile, `--help` exit 0); numeric thresholds intentionally NOT pinned (host wall-clock variance is a flaky-CI risk; the script is the artifact, re-run manually when `viz_data.py` materially changes); structure check 122 → 125 pins (0 FAIL), Python suite 130/0 (baseline preserved); also rolled in the WP1-cycle `build_metrics` empty-window contract bullet to CLAUDE.md `## Conventions` (modified by session-reflect at the end of the previous session, never committed). Shipped as commit `64fb865` on `origin/main`.
- **Milestone:** v3 WP2 — Emit-time perf budget + 90-day default completed in the `claude-time-visualize-v3` cycle; Phase 0 (data-layer refactor) now fully shipped (WP1 + WP2), unblocks Phase 1 (WP3 unified `--window` flag, WP4 legacy flag removal) and Phase 2 (WP5–WP9 frontend sub-payload routing).

## 2026-05-26

- **Task closed:** Promoted the verify-self subagent re-verification heuristic — covering the false-BLOCKING-FAIL pattern where a Playwright subagent reports a single FAIL whose outcome is mechanically implied by sibling PASSes on a JIT-compiled / lazy-mount / async-fetch page, observed 5-of-5 times across the claude-time-visualize-v2 cycle (WP5 P3 + WP7 P2 + WP8 P2 + WP9 + WP10) — from the project-local learning draft at `.claude/learnings/2026-05-22-verify-self-subagent-jit-false-fail.md` into `skills/feature-verify-self/SKILL.md` as a new dedicated `## Subagent Re-Verification Heuristic` section (rule statement verbatim, 3 conjunctive trigger conditions, 5-step procedure with `browser_wait_for` + React-fiber `reactProps[fiberKey].onClick()` direct-invocation workaround, explicit "what this heuristic is not" guard against override-on-inconvenience), plus a pointer from §3 (Parse subagent results) so the gate is read at result-parse time, not just at section-read time; structure check 122/0 PASS post-edit; no scenario assertions pinned on the affected section structure.
- **Backlog resolved:** SURFACE-2026-05-22-LEARNING-VERIFY-SELF-SUBAGENT-JIT-FALSE-FAIL — closed by promotion into `skills/feature-verify-self/SKILL.md`; the pattern was 3-of-3 at the WP7 P2 "definitely promote" gate and reached 5-of-5 by WP10 ship; future feature-verify-self invocations now have the heuristic in their loaded context so the orchestrator-side re-verification fires before back-looping to build on snapshot-timing noise.
- **Feature shipped:** WP11 — Comparison view (effectiveness lens) — `claude-time visualize --compare {wow,today-vs-trailing,mom}` and `--compare-range A:B,C:D` render the Compare view answering "am I leveraging Claude Code more WoW / MoM?" via 4 headline productivity-ratio rows (parallelism multiplier, AI effort / human wall, blocking split, concurrency mix) + 4 supporting absolute rows (AI agent, tool calls, human, engaged sessions) in A · B · Δ three-column layout sourced from per-window `comparison.{a,b}.metrics` trees; shipped as 3 phases including a mid-cycle re-spec from rejected delta-lens design to effectiveness-lens (Phase 2 superseded by Phase 2.A); +25 CLI pins (197/0), +14 Playwright behavioral pins (46/0), +11 Python unit tests (124/0); known limitation deferred to v3 cycle (preset content-not-refreshing — folds into the v3 unified-time-range emit model).
- **Milestone:** WP11 — Comparison view (effectiveness lens) completed in the `claude-time-visualize-v2` cycle; WP12 + WP13 superseded by user pivot decision to fold their concerns into the next product cycle (v3 unified-time-range emit model).

## 2026-05-24

- **Task closed:** `tests/fixtures/settings.json` updated to include the 8 claude-time hook entries (`UserPromptSubmit` / `PreToolUse` / `PostToolUse` / `PostToolUseFailure` / `SessionStart` / `SessionEnd` / `SubagentStart` / `SubagentStop`, each invoking `~/.claude/hooks/claude-time-hook.pl`) plus `env.CLAUDE_TIME_TRACKING: "1"` and root `fileCheckpointingEnabled: false`, mirroring the documented install end-state from `tools/claude-time/README.md`; `tests/check-structure.sh` Phase 7 now reports 122/0 on a clean working tree (was 121/1 with a 10-line drift block obscuring real regressions).
- **Backlog resolved:** SURFACE-2026-05-18-SETTINGS-FIXTURE-DRIFT-CLAUDE-TIME — closed via option (a) from the SURFACE entry: the fixture is now the documented install end-state for any contributor who has run the claude-time hook install steps; `INTENTIONAL_DIFFS` in `check-structure.sh` unchanged (the claude-time entries are now expected presence, not expected diff).
- **Feature shipped:** `claude-time visualize` custom-range view (WP8 of the `claude-time-visualize-v2` cycle) — the dashboard's previously-disabled `Custom` toolbar tab is now functional, with a date-range picker (two `<input type=date>` controls + "→" separator in the Toolbar's `dateLabel` slot, `data-range-picker="start"|"end"` Playwright-stable selectors, buffer-then-commit pattern that only propagates valid tuples on blur/Enter) and a new client-side `validateRange` helper mirroring Python's `_parse_range_flag` with 4 rules (shape, end>=start, end<=today, days<=cap) that drives red-border + tooltip on invalid input; URL hash plumbs `view=custom;range=YYYY-MM-DD:YYYY-MM-DD` per the CLAUDE.md schema (semicolon separator, debounced 100ms write, default-elision drops both keys when view==='day', three-branch dispatch for custom/week/day), and a new `_initView` IIFE in `_interactive_dashboard` prefers `hash.view`/`hash.range` over `CT_INITIAL_VIEW` so shareable URLs restore correctly; the CLI gained a peer `--range START:END` flag (validation rules mirrored Python-side via new `_parse_range_flag` helper with rule-naming stderr messages on rc=2; mutual-exclusion with `--demo`; warning when combined with `--context-days-prior/--context-days-after` since range owns its own window) and a new `viz_custom_range_max_days` config key (default 90, validated through `load_config`); single-source-of-truth cap threading from Python config → new `{{CT_MAX_RANGE_DAYS}}` template placeholder → `window.CT_MAX_RANGE_DAYS` → client-side `validateRange`; Custom view shares the existing `DayTimeline` rendering path (data shape is identical — `build_range_data` is the same multi-day union as WP5b's Day view) via new `isCustom` + `isDayLike` constants that land at 9 consumer surfaces (Body timeline, Minimap gate, Date-header label/projects/sessions count, filterChips, dayStats, ProjectFilterPopover projects prop, SummaryStrip stats); EmptyState reused with a Custom-flavored date string (`${range.start} to ${range.end}`); 25 new WP8 codify pins (14 Phase 1 + 11 Phase 2; test_visualize_cli 76 → 102 PASS) plus an in-phase P1.disc.1 hardening of test #1's flag-count regex (anchored at column-3 + EOL/space so wrapped help-text continuation lines mentioning sibling flag names can't false-match); WP9's filter chips + per-project popover inherit unchanged into Custom view via the WP9 `useFilter()` context. Phase 2 verify-self had 2 BLOCKING subagent false-fails (picker-edit→hash, hash-restore-on-reload) that orchestrator re-verify via React-fiber `reactProps.onBlur()` confirmed were Playwright synthetic-event boundary artifacts (same class as `SURFACE-2026-05-22-PLAYWRIGHT-SYNTHETIC-WHEEL-DOESNT-REACH-REACT` — synthesized DOM events don't always cross React's synthetic event system on JIT-compiled Babel-standalone pages); both behaviors actually work, verified by direct fiber inspection — the `SURFACE-2026-05-22-LEARNING-VERIFY-SELF-SUBAGENT-JIT-FALSE-FAIL` heuristic (N-1 of N PASS + the FAIL is mechanically implied by the PASSes → re-verify directly) saved a back-loop to build. Shipped as commit `14a1cfc` on `origin/main`.
- **Milestone:** WP8 (Custom-range view) of the `claude-time-visualize-v2` cycle complete — eighth of 14 work packages; Phase 2 now has 4 of 5 WPs shipped (WP5b + WP6 + WP8 + WP9), leaving WP7 (Month view, size L) as the last Phase 2 work package. Phase 3 (WP10–WP13) untouched.
- **Feature shipped:** `claude-time visualize` Month view (WP7 of the `claude-time-visualize-v2` cycle) — the dashboard's previously-disabled `Month` toolbar tab is now functional, rendering a 7-column Monday-first calendar grid where each day-cell is a single-tile GitHub-contribution-graph-style monochrome saturation encoding daily intensity (`day_total_active_min / month_max_min` normalized into 6 oklch buckets in the active-blue 268° hue) — the primary signal Month view communicates is 1D "how busy was this day", not 2D project composition (Day view answers that via drill-down); day-cells are `aspectRatio: '2 / 1'` so the whole month fits in one screen height without scrolling (user-tuned at verify-human from initial 1.7:1 — the binding contract was fit-in-viewport-height, not GitHub-graph-shape per se); the CLI gained a peer `--month YYYY-MM` flag with new `_parse_month_flag` helper enforcing 3 validation rules (shape `^\d{4}-\d{2}$`, month in 01..12, not-in-future) with rule-naming stderr messages on rc=2; mutual-exclusion with `--range` and `--demo`; initial-view precedence `--month > --range > --week > default`; `_cmd_visualize` packs `window.CT_DATA.months[YYYY-MM]` with two months (active + previous via new `_prev_month(y, m)` helper) so prev-month nav is a pure client-side state swap (D1 hybrid pre-load — instant) while next-month / prev-of-prev / click-day surface a non-modal `MonthNavToast` (bottom-right, auto-clipboard-copy via `navigator.clipboard.writeText`, auto-dismiss after 6s, × dismiss button) showing the equivalent `claude-time visualize --date YYYY-MM-DD` / `--month YYYY-MM` command (file:// dashboard has no server to navigate to, so the toast-and-paste pattern is the honest UX for the static-page constraint — P2.5 resolution); URL hash carries `view=month;month=YYYY-MM` per the CLAUDE.md schema (default-elision drops both keys when `view !== 'month'`); hash dispatcher extended from three- to four-branch (day/week/custom/month), surfaced an obsolete WP8-P2-8 assertion that was triaged + auto-updated in-flight to the new contract; `_initView` + new `_initMonthIso` IIFEs read hash → emit-time default → fallback chain; 6 new month-helper functions (`_monthIsoToParts`, `_monthIsoToLabel`, `_prevMonthIso`, `_nextMonthIso`, `_daysInMonth`, `_mondayIndex`); D6 fallback (active-month payload mirrored to top-level `data.today`) keeps Day/Week tabs functional in `--month`-emit mode; filter chips visible-but-inert in Month view per D4 (state persists across view switches, MonthView ignores it); `viz_render.render_html` signature unchanged — the new `months` key flows through `json.dumps(data)` verbatim; 23 new codify pins (17 source-shape in `test_visualize_cli.sh` + 6 behavioral in `test_visualize_interactive.js` via new `renderMonthDashboard()` helper with multi-day seeded sqlite + `{name: [paths]}` config schema + globally-unique 12-char session IDs to dodge `SURFACE-2026-05-22-VIZ-DATA-SESSION-ID-TRUNCATION`); Phase 2 went through one substantial design-pivot back-loop at verify-human (D5 vertical-strip per-project density → D5' single-tile monochrome intensity, root realization that Month view's primary axis is 1D not 2D) + one aspect-tuning back-loop (1.7:1 still too tall → 2:1 fits the binding fit-in-viewport contract); SURFACE-2026-05-22-PLAYWRIGHT-SYNTHETIC-WHEEL heuristic (React-fiber `reactProps[fiberKey].onClick()` direct invocation) was load-bearing across all behavioral tests + the third recurrence of the pattern across the cycle, putting `SURFACE-2026-05-22-LEARNING-VERIFY-SELF-SUBAGENT-JIT-FALSE-FAIL` past its codification threshold. Full claude-time suite 219 → 264 PASS (+45 WP7 assertions); structure check 122/0. Shipped as commit `ce1c7ec` on `origin/main`.
- **Milestone:** WP7 (Month view) of the `claude-time-visualize-v2` cycle complete — ninth of 14 work packages; **Phase 2 of the cycle is now fully complete** (5 of 5 WPs shipped: WP5b + WP6 + WP7 + WP8 + WP9). Phase 3 (WP10–WP13) is the cycle's remaining work.
- **Feature shipped:** `claude-time visualize` metrics surface (WP10 of the `claude-time-visualize-v2` cycle, spec-bundled with `SURFACE-2026-05-24-CLAUDE-TIME-VIZ-AGGREGATE-METRICS-PANEL`) — a HeadlineCard pinned above the timeline showing three load-bearing numbers (active session wall-clock with away-gaps excluded, human activity wall-clock = typing+reading+thinking, AI effort-time = agent burst effort_ms without tool/subagent double-counting) plus a chevron-toggle that expands a MetricsPanel with six metric sections (engaged_session, ai_agent + subagent sub-row, tool_call + top-5 tools sub-table, human + typing/reading/thinking sub-rows, concurrency stratification k=1/2/3/4+, blocking metrics for both directions), each row showing wall-clock | effort-time | ×multiplier so the parallelism gap is the load-bearing visible insight; the date-range indicator "Past 7 days · YYYY-MM-DD → YYYY-MM-DD" lives on the collapsed HeadlineCard (P2.verify-human.2 back-loop moved it there from MetricsPanel's header to keep window context always-visible). Backed by a new Python aggregator `build_metrics(events, start, end) -> dict` in `viz_data.py` (with `_merge_intervals`, `_sum_intervals`, `_build_engaged_intervals` private helpers transcribed from the user's `/tmp/usage_analysis_v3.py` reference) emitting a fully-shaped 6-metric tree with reconciliation invariants codified as unittests (concurrency wallclock sum ≡ engaged wallclock; subagent ⊆ ai_agent both axes; human.multiplier ≡ 1.0; concurrency[i].effort ≡ wallclock × k; blocking.agent_blocking_human ≡ ai_agent.wallclock; |top| ≤ 5; multiplier in valid range), backed by two new interval-returning helpers in `reclassify.py` (`tool_intervals`, `subagent_intervals` — siblings of the existing effort-time-only helpers) so wall-clock (merged) and effort-time (plain sum) can both be computed; wired into `_cmd_visualize` so `window.CT_DATA.metrics` is emitted with a trailing-7-day window (today + prior 6, computed from snapshot_dt, view-mode-independent across Day/Week/Month/Custom); `_computeMetricsView(metrics, filterKinds)` projection in JSX collapses both HeadlineCard and MetricsPanel to a single filter-aware SoT so kind chips affect headline + panel cells consistently (subagent OFF drops AI-effort by the subagent contribution; reading/thinking OFF drop human activity; active OFF collapses everything to 0); URL hash carries `metrics=expanded` with default-elision (CLAUDE.md key reservation table extended); empty-window caption variants distinguish "no data at all" from "filters hide everything"; demo path emits an empty-shape metrics tree (planned simplification — `viz/data.js` is JS-mock, not event-dicts; building a JS→event shim deferred). Phase 1 (aggregator + emit) shipped 38 unittests + 9 source-shape pins; Phase 2 (UI) shipped 15 source-shape + 9 behavioral pins (including the 5th consecutive use of the WP7-discovered React-fiber `reactProps[fiberKey].onClick()` workaround per SURFACE-2026-05-22-PLAYWRIGHT-SYNTHETIC-WHEEL across 9+ different click handlers in the cycle). Plan-level downstream-contract-impacts caught at the changing phase: P1.5 emit-wiring exposed two WP5b assertions using `! grep -q '"day_count"'` as a single-day-shape proxy (the new metrics tree always carries its own `day_count: 7`); scoped to `grep -c day_count == 1` in P1.7 alongside the changing emit (not deferred to codify — the CLAUDE.md rule working as intended). Real-DB cross-check vs `/tmp/usage_analysis_v3.py` matched on every metric: 22 numeric assertions exact (engaged 33h22m, ai_agent 18h30m / ×1.51, subagent 2h15m / ×1.09, tool 5h48m / ×1.11, typing 2h19m, reading 2h49m, thinking 6h07m, concurrency k=1..4+ = 21h48m / 6h57m / 4h35m / 46s, top-5 tools identical order with identical values, blocking metrics agent=18h30m / human=8h56m, session_count=47); floats matched to 4 decimal places. One verify-human back-loop (P2.verify-human.2 cosmetic, window indicator placement), fixed same-day with +2 source-shape pins and +3 codify-gap behavioral pins (window persistence across collapse/expand; subagent-OFF drop magnitude via the panel sub-row; card mounted across view-mode switches). Full claude-time suite 264 → 335 PASS (+71 net WP10 assertions across 113 unittest + 29 cli + 156 visualize_cli + 37 interactive); structure check 122/0. Shipped as commit `fc4fe2a` on `origin/main`.
- **Backlog resolved:** SURFACE-2026-05-24-CLAUDE-TIME-VIZ-AGGREGATE-METRICS-PANEL — closed by WP10 ship; bundled at feature-spec (option 3 from the bundling-decision dialog) so the headline-card and metrics-panel design questions resolved in one spec pass instead of two separate WPs; per-project breakdown explicitly out-of-scope per spec Q7.
- **Milestone:** WP10 (Headline-stats card → Metrics surface) of the `claude-time-visualize-v2` cycle complete — tenth of 14 work packages; **Phase 3 has its first WP shipped** (3 remaining: WP11 Comparison view, WP12 Multi-instance overlap, WP13 Collapsible rows + away total + pills).

## 2026-05-23

- **Feature shipped:** `claude-time visualize` multi-day data window for Day view (WP5b of the `claude-time-visualize-v2` cycle) — Day view now loads ±21 days of context around the requested day (defaults `prior=14, after=7`, configurable via `viz_context_days_prior` / `viz_context_days_after` in `~/.claude-time/config.json` and per-invocation overridable via `--context-days-prior N` / `--context-days-after M` CLI flags; `--demo` forces single-day; `--context-days 0/0` preserves byte-identical pre-WP5b single-day shape including flat `iso` + `hour_range` keys); `_cmd_visualize` calls `build_range_data` and attaches `target_iso` so the renderer can center the initial viewport on the user-requested day; renderer extended with `dayOffsetMin(day_iso, window_start_iso)` UTC-anchored helper, `DataWindowContext` plumbing `windowStartIso + dayCount` to `HourRuler` / `HourGridBackground` / `SessionRow` / `Minimap` without prop-drill, `SegmentBar` `dayOffset` prop applied via `viewportPct(seg.start + dayOffset, seg.end + dayOffset, viewport)`, `_initialViewport` centering on `target_iso`'s `hour_range_by_day[target_iso]` (viz_render.py wrapper consolidated to delegate to it, eliminating the double-path drift that produced the Phase 2 verify-self viewport-init bug), `pickTickInterval` scale set extended to `[1440, 360, 60, 30, 15, 10, 5, 1]` keeping the 8–30 tick band across full-window 21-day zoom-out, `ticksInViewport` emitting `MMM DD` at day-level and on midnight-crossing ticks via the new `_formatDayLabel` helper, `DayTimeline + Minimap` clamping viewport to `[0, day_count*1440]`, NOW marker handling target-day offset (`isToday = todayISO in [meta.start, meta.end]`, `effectiveNowMin = nowMin + dayOffsetMin(todayISO, meta.start)`), Minimap density bars pre-shifted by day-offset so cross-day distribution spreads across the full minimap width (caught at verify-human as the third double-path bug), and `SessionRow key={s.day_iso ? \`${s.day_iso}:${s.id}\` : s.id}` preventing React duplicate-key warnings when `build_range_data` unions sessions sharing session_id across days (render-side mitigation of the still-open `SURFACE-2026-05-22-VIZ-DATA-SESSION-ID-TRUNCATION-CAN-COLLIDE`); `test_visualize_cli.sh` extended with 9 new Phase 2 codify assertions (3 explicit regression-pins for the double-path bug fixes) + 6 new Phase 1 codify assertions, plus 2 in-flight test-triage fixes (wrap-tolerant `--help` text match, loosened `viewportPct` call-site pattern); full claude-time suite 165 → 174 PASS (+9 net); 3 in-flight bug fixes during Phase 2 verification (viewport-init double-path between `dashboard.jsx::_initialViewport` and `viz_render.py` Dashboard wrapper, SessionRow duplicate keys from cross-day session aggregation, Minimap left-bunching from missing day-offset on density bars) all caught at the verify-self/-human gates and codify-pinned so they cannot return silently. Shipped as commit `02d6237` on `origin/main`.
- **Backlog resolved:** SURFACE-2026-05-22-CLAUDE-TIME-VIZ-DAY-VIEW-MULTI-DAY-DATA-WINDOW — closed by WP5b shipping the multi-day data window directly as user-prioritized at WP5 verify-human ("this feature really matters to me!"); `_cmd_visualize` now calls `build_range_data` instead of `build_day_data` when `prior + after > 0`, CLI flags + config keys + ISO-day-aware ruler labels + extended `pickTickInterval` scale set + day-offset segment math all in place.
- **Milestone:** WP5b (Multi-day data window for Day view) of the `claude-time-visualize-v2` cycle complete — fifth-and-a-half of 14 work packages (was 13; WP5b added at WP5 finalize 2026-05-22 to capture the user-prioritized Day-view extension); Phase 2 of the cycle now has Day-view multi-day rendering on top of WP5's viewport machinery, leaving WP6 (Day rename), WP7 (Month view), WP8 (Custom-range), WP9 (filter chips), WP10–WP13 ahead.
- **Feature shipped:** `claude-time visualize` "Today" → "Day" toolbar tab rename (WP6 of the `claude-time-visualize-v2` cycle) — the shipped dashboard's first toolbar tab now reads "Day" to match the multi-day data window WP5b introduced (the v1 label "Today" was actively misleading once the Day view loaded ±21 days of context); the load-bearing edit is in `viz_render.py::InteractiveToolbar` (the emit-time-appended shipped toolbar) — `tabBtn('Today', 'day', ...)` → `tabBtn('Day', 'day', ...)` plus the section comment; matching edits to `viz/dashboard.jsx::Toolbar` (design-canvas static prototype that `viz_render.py` strips at emit) kept for design-canvas reference consistency but dead from shipped-UI perspective; rename-scope decision: keep `window.CT_DATA.today` data-layer key unchanged (WP5b stabilized six consumers `_initialViewport` / `SessionRow` / `DayTimeline.dwCtx` / `Minimap.allSegs` / `viz_render.py` wrapper / `claude-time:586` on the `.today.*` shape, and WP3's `meta.start/end` escape hatch keeps a future data-key rename cheap if needed — the contract-key/label mismatch is a documented trade-off captured as an inline comment above the View tabs section); `README.md` line 157 also rewritten ("The `Day` and `Week` toolbar tabs are interactive"); `test_visualize_cli.sh` extended 59 → 62 PASS with three new WP6 codify assertions (positive consuming-surface pin on the shipped `tabBtn('Day', 'day', view === 'day', true)`, negative regression-pin against the legacy `tabBtn('Today',...)` form catching the exact F9-back-loop regression class, and a data-layer `.today` preservation pin); plan-defect caught at F9 verify-auto back-loop — initial 5 leaves edited only the design-canvas Toolbar, F9 added P1.6 + P1.7 to edit the actual shipped InteractiveToolbar (the WBS task 6.1 text "the emit-time-appended interactive Dashboard wrapper, not the byte-pinned source" was correct but the plan mis-mapped it); `.gitignore` now excludes `tmp/` (local-only verify-self emitted HTML). Shipped as commit `217cfe3` on `origin/main`.
- **Milestone:** WP6 ("Today" → "Day" rename) of the `claude-time-visualize-v2` cycle complete — sixth of 14 work packages; Phase 2 now has 2 of 5 WPs shipped (WP5b + WP6), leaving WP7 (Month view, size L), WP8 (Custom-range, size M), WP9 (filter chips, size S) ahead in Phase 2 plus Phase 3 (WP10–WP13).
- **Feature shipped:** `claude-time visualize` interactive filter chips + Toolbar duality collapse (WP9 of the `claude-time-visualize-v2` cycle) — five clickable kind chips (active/reading/thinking/subagent/away) replace the static color-key Legend (`data-filter-kind=<kind>` + `data-filter-on=true|false` per chip, reduced opacity + strikethrough on OFF state), `SegmentBar` returns null when `useFilter().kinds[seg.kind] === false` so toggling hides all segments of that kind across the timeline with layout stability preserved (positions of other kinds unchanged because per-segment hide, not per-row hide), `SessionRow.totalActive` is filter-aware so the visible per-row duration label reflects what's actually rendered (`session.segs.filter(s => filterKinds[s.kind] !== false)`); `SegmentBar` also gained `data-kind={seg.kind}` as a stable Playwright/CSS selector; filter state persists in the URL hash as `#filters=active,subagent` (canonical order `active,reading,thinking,subagent,away`, hash-restore on init + debounced 100ms write on change + `replaceState` no history pollution + default-elision drops the key when all kinds are ON + malformed-hash sanity-guard falls back to all-ON) per the project `CLAUDE.md` "Claude-time visualize URL-hash state" schema (the `filters=` key was reserved there ahead of time); new `ProjectFilterPopover` component (rendered next to the now-clickable Legend in the date-header strip, NOT in the toolbar — same documented plan deviation as the kind chips, since the toolbar stays clean for view + date controls while filter affordances cluster) opens a checkbox list of all projects in the current view with `data-project-filter-item=<projectId>`, unchecking hides the project's row entirely from `DayTimeline` via `visibleProjects = data.projects.filter(p => projectFilter[p.id] !== false)` (per-project hide reflows layout naturally), hidden-count badge on the trigger, outside-click dismiss via `document.mousedown` listener with proper `useEffect` cleanup (scope: Day view only — WeekTimeline's rollup aggregation deferred to a future WP); bundled bonus: Phase 1 collapsed the design-canvas/InteractiveToolbar duality (option a from `SURFACE-2026-05-23-CLAUDE-TIME-VIZ-DESIGN-CANVAS-INTERACTIVE-TOOLBAR-DUALITY`) — `viz_render.py::InteractiveToolbar` deleted and its body moved verbatim into `viz/dashboard.jsx::Toolbar` (the shipped Dashboard wrapper now renders `<Toolbar ...>` directly), `viz_render.py` docstring updated to reflect that the toolbar is no longer an emit-time-appended component, project `CLAUDE.md` "Design-as-data" convention gets a WP9 postscript recording the collapse rationale — future toolbar-touching WPs (WP10 headline-stats, WP12 multi-instance overlap) edit a single file; one in-phase discovery (P2.disc.1, surfaced + fixed within Phase 2 build): `viz_render.py::_strip_design_wrapper`'s hardcoded-dash-count strict marker had drifted from the source (~30 vs source's ~33 trailing ─), silently falling through to a naive `find("Dashboard wrapper")` substring fallback that false-matched a new prose comment mentioning "Dashboard wrapper" and stripped the whole file body — replaced with a dash-count-tolerant regex that requires the `/* ── ... ── */` section-header comment-block context, six new unit tests in new `test_viz_render.py` pin the regex behavior (short/long/drifted dash counts, prose-mention false-match guard, real-source-still-strips integration); test coverage delta: `test_visualize_cli.sh` 62 → 76 PASS (+14 WP9-prefixed static-emit pins distributed across per-phase verify-codify: P1 duality 2 / P2 chips+context 3 / P3 hash schema 4 / P4 popover contract 5), `test_visualize_interactive.js` 10 → 22 PASS (+12 WP9 behavioral Playwright assertions covering hash round-trip + popover open/uncheck/restore/outside-click), new `test_viz_render.py` 0 → 6 PASS (strip-marker regex regression coverage); planned Phase 5 (centralized codify-cleanup) was triaged as obsolete-phase — every P5.* leaf was a direct duplicate of pins already PASSING from per-phase verify-codify, documented as a plan-discipline lesson in the WIP triage entry (consuming-surface pins land with their consuming code, not in a separate "codify everything" phase). Shipped as commit `f5a1123` on `origin/main`.
- **Backlog resolved:** SURFACE-2026-05-23-CLAUDE-TIME-VIZ-DESIGN-CANVAS-INTERACTIVE-TOOLBAR-DUALITY — closed by WP9 Phase 1 collapsing the duality (option a): `viz_render.py::InteractiveToolbar` deleted, body moved into `viz/dashboard.jsx::Toolbar`, design-canvas reference still works via a no-op pass-through call (`<Toolbar view={isDay ? 'day' : 'week'} onViewChange={() => {}} dateLabel={...} />`), CLAUDE.md "Design-as-data" convention postscript records the rationale, regression-pinned by two WP9-P1 codify assertions (`exactly one Toolbar function` + `no <InteractiveToolbar> JSX usage`) so the duality cannot return silently.
- **Milestone:** WP9 (Interactive filter chips) of the `claude-time-visualize-v2` cycle complete — seventh of 14 work packages; Phase 2 now has 3 of 5 WPs shipped (WP5b + WP6 + WP9), leaving WP7 (Month view, size L) and WP8 (Custom-range, size M) ahead in Phase 2 plus all of Phase 3 (WP10–WP13). Bundled bonus: Phase 1's Toolbar duality collapse closes a structural tech-debt class that would have bitten WP10 (headline-stats card) and WP12 (multi-instance overlap viz).

## 2026-05-22

- **Feature shipped:** `claude-time` test-infrastructure containerization — Docker-based test environment bundling Python 3.12, Perl 5, sqlite3, jq, Node 22, Playwright, and Chromium so the full claude-time test suite (and WP5 Phase 4's forthcoming Playwright behavioral test) runs without touching the host OS; persistent + on-demand lifecycle via `tools/claude-time/test/run-in-container.sh` ({`start`|`stop`|`restart`|`status`|`exec`|`logs`|`help`}, exit-codes 0/1/2/3/64); project root bind-mounts read-write at `/work`; pinned base `mcr.microsoft.com/playwright:v1.49.1-noble`; new `playwright_smoke.js` readiness signal; full 165-PASS claude-time suite runs identically inside the container; `test_container_image.sh` added with 30 structural assertions on Dockerfile + wrapper + smoke + README; `tools/claude-time/README.md` gains a "Running tests" section documenting the container as the canonical test path. Shipped as commit `de89aef` on `origin/main`. Unblocks WP5 Phase 4 of the `claude-time-visualize-v2` cycle (paused earlier today on the Docker Hard-Blocker rule).
- **Feature shipped:** `claude-time visualize` zoomable + draggable multi-track timeline (WP5 of the `claude-time-visualize-v2` cycle) — replaces the v1 day-window-fitted timeline with a viewport state machine (`useViewport()` Context-plumbed `{visible_start_min, visible_end_min}`) that supports pan via drag-on-ruler/empty-row (cursor-anchor invariant, gutter-excluded), zoom via wheel+cmd/ctrl + trackpad pinch + keyboard `+`/`-`/`0`/arrows/Home/End (cursor-anchored, rAF-throttled via `scheduleSet`), adaptive ruler density via `pickTickInterval` picking densest interval from `[60, 30, 15, 10, 5, 1]` minutes producing 8–30 visible ticks (HH:00 for hour, HH:MM for finer), a `Minimap` overview with draggable visible-window rectangle (drag → pan, edge-drag → zoom, click-elsewhere → re-center), and URL-hash state persistence (`#viewport=<start>:<end>`) via shared `parseHash`/`updateHash`/`serializeHash` helpers (semicolon-separated `key=value` pairs, URL-encoded, `replaceState` not `pushState`, default-elision via `null` patch) — the convention is codified in project `CLAUDE.md` with a per-consumer key reservations table covering WP5/6/7/8/9/13 so every downstream cycle WP follows the same shape; perf measured at 60.2 fps avg / 59.5 fps min over 5s active gestures against an 1800-segment 1-month synthetic dataset, three independent runs identical, so DOM-per-segment stays and canvas fallback is NOT needed; `__perfRecord` rAF-sampler + `window.__dashboardViewport` introspection hook gated by `?perf=1` query; new Playwright behavioral test `test_visualize_interactive.{js,sh}` runs inside the test container with 10 assertions covering gesture math + hash round-trip + adaptive ruler density + minimap render + default-elision (1 synthetic-WheelEvent SKIP documented as a Playwright + React limitation, surfaced to backlog); test_visualize_cli.sh extended 19 → 41 source-shape assertions across Phase 1/2/3 plus a regression-pin catching future orphaned consumers of the deleted `DAY_*_MIN` module-level constants (the regression-pin was added in-flight after Phase 3 verify-human caught `InterruptHairlines` orphan-referencing them against real `~/.claude-time` data); full claude-time suite 134 → 205 PASS inside the container (+71 net). Shipped as commit `140de22` on `origin/main`.
- **Backlog resolved:** SURFACE-2026-05-19-CLAUDE-TIME-VIZ-NOW-LABEL-OVERLAPS-RULER-TICK — closed by the `flipNowLeft` branch folded opportunistically into WP5 Phase 2's HourRuler adaptive-density refactor (3-line change: when `nowMin % intervalMin >= intervalMin - 10`, the `NOW · HH:MM` label gets `right: 4` instead of `left: 4`, adapting to any zoom level's tick density).
- **Milestone:** WP5 (Zoomable + draggable multi-track timeline ⭐) — the user-designated critical / foundational work package of the `claude-time-visualize-v2` cycle. Phase 1 (viewport math) complete; downstream WPs (Month, Custom-range, Filters, Comparison, Multi-instance overlap, Collapsible rows) now inherit the viewport + URL-hash machinery. WBS updated at this finalize: new sibling **WP5b** (Multi-day data window for Day view, user-prioritized) added to Phase 2 of the cycle.

## 2026-05-21

- **Feature shipped:** `claude-time visualize` range-aware data layer (WP3 of the `claude-time-visualize-v2` cycle) — extracted `build_range_data(start_iso, end_iso, *, events_by_day, cfg, auto_alias_fn)` in `tools/claude-time/viz_data.py` as a multi-day coordinator that delegates per-day work to the existing `build_day_data` worker (which remains day-anchored via `_ts_to_minutes` and burst pairing — the truthful seam is per-day worker + multi-day coordinator, not the plan's originally-stated "single core" framing); range payload emits `meta: {start, end, day_count}`, `hour_range_by_day: {iso: [start, end]}`, and `day_window: [global_start, global_end]` so downstream WPs (WP5 zoomable timeline, WP7 month view, WP8 custom range, WP11 comparison view) can render arbitrary windows; `build_week_data` re-implemented as a thin wrapper over `build_range_data(monday, sunday, ...)` — output shape unchanged from pre-WP3 (rollup, days, label); range `meta` intentionally NOT propagated into day/week wrapper returns to avoid collision with the CLI's flat-level `window.CT_DATA.meta.snapshot` key that `_cmd_visualize` injects; back-compat: for `day_count == 1` the range payload surfaces flat `iso` and `hour_range` so callers can consume it as a day shape; `test_viz_data.py` extended with 7 new tests (6 `BuildRangeDataTests` covering empty-range default `[6,23]` per-day, single-day back-compat shape equivalence with `build_day_data`, 7-day `day_window` as the union of adaptive per-day ranges, cross-day project aggregation with `day_iso`-tagged sessions, `meta` exact-keys check, and an `ValueError` defensive on inverted ranges; 1 `WrapperPreservationTests.test_day_shape_equivalence` regression gate); 22 pre-existing tests still PASS; full claude-time suite 123/0 (58 unittest + 29 test_cli + 19 test_visualize_cli + 17 test_hook); `tests/check-structure.sh` 121/1 (1 pre-existing settings-fixture drift, unchanged); verify-human approved against live-DB render for 2026-05-19 (6 projects, hour_range [5,23] adaptive); shipped to `origin/main` as commit `9ebad84`.
- **Milestone:** WP3 (Range-aware data layer) of the `claude-time-visualize-v2` cycle complete — third of 13 work packages; closes Phase 0's data-layer foundation for arbitrary windows (only WP4 comparison-data remains in Phase 0).
- **Feature shipped:** `claude-time visualize` comparison data layer (WP4 of the `claude-time-visualize-v2` cycle) — added `build_comparison_data(start_a_iso, end_a_iso, start_b_iso, end_b_iso, *, events_by_day_a, events_by_day_b, cfg, auto_alias_fn)` in `tools/claude-time/viz_data.py` as a thin coordinator on top of WP3's `build_range_data` (two range payloads + a `_compute_deltas` join by alias), returning `{a, b, deltas: {alias: {kind: {abs_min, rel_pct}}}, meta: {a_start, a_end, b_start, b_end, a_day_count, b_day_count}}`; `kind` in the result is the segment-kind axis (active / reading / thinking / away / subagent / synthesised `total_active_subagent`), with `rel_pct = None` when the A-side baseline is zero so the consumer can render N/A rather than a misleading percentage; the WBS-pseudocode `kind={absolute|relative}` parameter was dropped at plan time (kind was ambiguous against the result-axis use, and emitting both `abs_min` and `rel_pct` keeps the data layer policy-free — the consumer picks the lens); two helpers wrap common comparisons — `compare_week_over_week(this_monday_iso)` (A = prev 7 days, B = this 7 days) and `compare_day_vs_trailing_window(target_day_iso, window_days=7)` (build-time rename from the WBS-named `compare_day_vs_median` because the data layer emits raw per-day payloads — the median-vs-mean-vs-sum aggregate is a UI choice deferred to WP10), both partitioning a single `events_by_day` dict into A/B sub-dicts internally; `BuildComparisonDataTests` added with 11 methods covering empty-both, empty-A-only, empty-B-only, identical, regression, meta-shape exact-keys, synthesised `total_active_subagent` invariant, both helpers' window math, `ValueError` guard on `window_days < 1`, and cross-window partition correctness (30 min in A's range vs 90 min in B's range → +60 abs, +200% rel — proves the partition doesn't bleed); full claude-time suite 134/134 PASS (69 unittest + 29 cli + 19 viz_cli + 17 hook), net +11 vs WP3 baseline of 123; no integration boundary (no existing endpoint/UI/CLI consumes the new functions; first consumer lands in WP11); shipped to `origin/main` as commit `4f61904`.
- **Milestone:** WP4 (Comparison data layer) of the `claude-time-visualize-v2` cycle complete — fourth of 13 work packages; closes Phase 0 (data-layer foundations) entirely. Phase 1 begins with WP5, the XL zoomable-timeline milestone everything in Phases 2–3 depends on.

## 2026-05-19

- **Feature shipped:** `claude-time visualize` — a self-contained HTML dashboard subcommand that renders tracked time as a horizontal Gantt across day and week views, reusing the Claude Design extract verbatim as the byte-pinned visual contract (`tools/claude-time/viz/`) with emit-time JSX transforms in `viz_render.py` (strips the design-canvas chrome, wires bar-click → side panel through SegmentBar→SessionRow→DayTimeline, injects an InterruptHairlines component for mid-turn UPS markers, replaces the design's variant-prop wrapper with an interactive `useState`-driven wrapper for view/selection/expansion state); CLI surface adds five flags (`--date YYYY-MM-DD`, `--week`, `--demo`, `--no-open`, `--out PATH`) reading from `~/.claude-time/events.sqlite` via the existing reclassify pipeline and emitting a single self-contained `.html` to `~/.claude-time/visualize.html` (React 18 UMD + Babel-standalone from `unpkg.com` CDN, JSX parsed in-browser, snapshot per invocation, no daemon); one viz-session per `session_id` with reading/thinking/away gaps rendered inline (no time-based splitting since the harness preserves session_id across `/resume`); adaptive hour-window with `[6, 23]` fallback on empty days; empty-state card when zero events; project aliases via the existing `_auto_alias_for_cwd` (git-basename | misc) match `report --by cwd` exactly; 35 new test assertions across `test_viz_data.py` (22) + `test_visualize_cli.sh` (13), plus 5 new `ActiveBurstsTests` in `test_reclassify.py` (24 → 29) directly covering the extracted shared helper, plus check-structure Phase 5b additions (viz_data.py exists + py_compile + test_viz_data run + test_visualize_cli run) and Phase 5c (byte-pinning the 4 viz/ design files against silent drift); shipped to `origin/main` as commit `fcd570f`.
- **Backlog resolved:** SURFACE-2026-05-18-CLAUDE-TIME-RECLASSIFY-SESSION-ACTIVE-MS-CONSECUTIVE-UPS-UNDERCOUNT — closed in-feature during Phase 2 back-loop: extracted `reclassify.active_bursts(events)` as the shared burst-pairing source of truth (returns per-session `[{start_ts, end_ts, interrupts}]` lists with the narrow definition canonicalized — last UPS before Stop anchors the burst; earlier UPSes recorded as `interrupts`); both `session_active_ms` (refactored to consume it) and `viz_data._bursts_for_session` (new consumer) now agree exactly modulo per-burst minute-truncation, eliminating the previously observed 6.6% drift on real data.
- **Feature shipped:** `claude-time visualize` adaptive hour-ruler (WP1 of the claude-time-visualize-v2 cycle) — `dashboard.jsx` now derives `DAY_HOURS` / `DAY_START_MIN` / `DAY_END_MIN` / `DAY_RANGE_MIN` from `window.CT_DATA.today.hour_range` at runtime (with a `[6, 23]` fallback when CT_DATA is absent), instead of the hardcoded `[6..22]` literal; the Python data layer was already emitting `hour_range` adaptively per-day, so the change is JSX-side only; bundled with the v2-cycle convention shift relaxing the byte-pin in `tests/check-structure.sh` Phase 5c for the now-editable `dashboard.jsx` and `data.js` (existence + JS-parse checks remain; `index.html` and `design-canvas.jsx` stay pinned), the `CLAUDE.md` "Design-as-data" convention rewrite encoding the historical-origin + current-state form with the unlock-condition lesson, and a new `test_visualize_cli.sh` assertion #14 seeding a 14:00–15:00 narrow-window DB and asserting emitted HTML contains `"hour_range": [13, 16]`; shipped to `origin/main` as commit `2760c6b`.
- **Backlog resolved:** SURFACE-2026-05-19-CLAUDE-TIME-VIZ-DAY-HOURS-NOT-ADAPTIVE — closed by the adaptive hour-ruler feature above (the data layer already emitted `hour_range`; the JSX just needed to consume it).
- **Milestone:** WP1 (Adaptive hour-ruler) of the `claude-time-visualize-v2` cycle complete — first of 13 work packages.
- **Feature shipped:** `claude-time visualize` NOW marker client-side + snapshot caption (WP2 of the claude-time-visualize-v2 cycle) — v1's `NOW_MIN = 17 * 60 + 22` module-level constant and `NOW · 17:22` string literal in `viz/dashboard.jsx` froze the marker at HTML-emit time; replaced with a `useNowMin()` React hook (React.useState + React.useEffect with `setInterval(60_000)` and `clearInterval` cleanup) returning `{nowMin, todayISO}` from the system clock; `DayTimeline` gates `nowFrac` on `(showNow && isToday && inWindow)` where `isToday = data.iso === todayISO` (hides the marker on past days viewed via `--date 2026-05-15`) and `inWindow = nowMin >= DAY_START_MIN && nowMin < DAY_END_MIN` (hides outside the adaptive hour-range); `HourRuler` now takes a `nowLabel` prop and renders `NOW · {nowLabel}` dynamically; `_cmd_visualize` in `tools/claude-time/claude-time` injects `meta = {snapshot: HH:MM, snapshot_iso: ISO8601}` into the data payload (both --demo and live-DB paths); `InteractiveToolbar` in `viz_render.py` accepts a `snapshot` prop and renders a mono-font `snapshot: HH:MM` caption next to the date label with a hover tooltip explaining the live-cursor + snapshot-data duality; `test_visualize_cli.sh` extended 14 → 19 assertions covering absence of v1 hardcoded values + presence of client-side timestamp source + `meta.snapshot` key + `clearInterval` cleanup; full claude-time suite 116/0 (51 unittests + 19 viz CLI + 29 report CLI + 17 hook); shipped to `origin/main` as commit `3a3f42c`.
- **Milestone:** WP2 (NOW marker client-side + staleness indicator) of the `claude-time-visualize-v2` cycle complete — second of 13 work packages.

## 2026-05-18

- **Feature shipped:** `claude-time` — opt-in, hook-driven time-tracking for Claude Code (`tools/claude-time/`), implementing a Perl 5 hook script that fast-fails in ~4ms when `CLAUDE_TIME_TRACKING` is unset and writes timing-relevant events to `~/.claude-time/events.sqlite` (WAL mode, schema bootstrapped on first write) for all 10 Claude Code hook events (`UserPromptSubmit` / `PreToolUse` / `PostToolUse` / `PostToolUseFailure` / `Stop` / `Notification` / `SessionStart` / `SessionEnd` / `SubagentStart` / `SubagentStop`) at ~14ms/call with privacy-preserving per-event meta payloads (prompts logged length-only, never text; tool input/output never logged); plus a Python 3 stdlib reclassifier CLI `claude-time report [--weekly] [--date YYYY-MM-DD] [--session <uuid>] [--cwd <path>] [--db <path>]` that buckets per-session `Stop → UserPromptSubmit` gaps into reading/thinking/away with cross-session reattribution (subtracts other sessions' typing-debit-equivalent during the gap window); 55 test assertions across 7 test scripts (`test_hook.sh` 17, `test_cli.sh` 10, `test_reclassify.py` 24, `privacy_check.sh` 1, `bench.sh` 1, `multi_instance.sh` 1, `stress_concurrent.sh` 1) wired into `tests/check-structure.sh` Phase 5b; structure suite 110/0; shipped to local branch `feature/claude-code-time-tracking-phase-1` (not merged to main by user choice).
- **Backlog resolved:** SURFACE-2026-05-10-CLAUDE-CODE-TIME-TRACKING — closed by the `claude-time` feature above; v1 delivers the storage / agent-side instrumentation / privacy-by-default sub-items from the original exploration outline, with the offline-vs-idle hybrid deferred to v2 (spec's `## Out of Scope`).
- **Backlog resolved:** SURFACE-2026-05-18-CLAUDE-TIME-HOOK-PERF-BUDGET-INFEASIBLE — closed via F23 plan revision during Phase 1 build; spec acceptance #10 amended from "< 50ms / 10 calls on any platform" to "< 200ms on macOS, < 50ms on Linux" after empirical measurement showed bash+jq+sqlite3+python3 stack at ~95ms/call vs Perl single-process at ~10ms/call on stock macOS.
- **Feature shipped:** `claude-time report --by <dim>` grouped output (`cwd | session | day`) for per-group rollups of the existing per-metric report, plus `project_names` aliasing in `~/.claude-time/config.json` for explicit cwd-to-label mapping (e.g. worktree + main repo under one project name), plus zero-config auto-alias for `--by cwd` that resolves git-repo cwds to `basename(repo_root)` and non-git cwds to a single `misc` bucket; precedence is explicit > auto-alias > raw cwd; `test_cli.sh` extended from 10 → 25 assertions (15 new) covering grouping, sort order, sum-vs-relabel for aliases, silent-drop for malformed config, git-repo basename resolution, misc-bucket aggregation, and explicit-over-auto precedence; shipped to local branch `feature/claude-code-time-tracking-phase-1` as commit `66788a3` (not pushed).
- **Backlog resolved:** SURFACE-2026-05-18-CLAUDE-TIME-REPORT-BY-PROJECT — closed by the `--by` + project_names + auto-alias feature above; mid-build user request for a per-row total column + per-column TOTAL row was split out as a separate high-priority follow-up surface (`SURFACE-2026-05-18-CLAUDE-TIME-TOTAL-COL-ROW`).
- **Feature shipped:** `claude-time report --by <dim>` gains a rightmost `total` column — each grouped data row sums its 5 metric cells (tool + active + reading + thinking + away), and the bottom TOTAL row gains a corresponding bottom-right grand-total cell that equals both sum-down-of-column-totals and sum-across-of-per-row-totals (cross-pivot sanity check at integer-ms); 6 LOC effective diff in `render_grouped` only, `render_report` (default non-grouped) untouched; `test_cli.sh` extended from 25 → 29 assertions (4 new: header rightmost token, 6-cell row count, integer-ms math cross-check via direct `reclassify` import, empty-window regression guard); full claude-time suite 70/70; shipped to local branch `feature/claude-code-time-tracking-phase-1` as commit `3c605f0` (not pushed).
- **Backlog resolved:** SURFACE-2026-05-18-CLAUDE-TIME-TOTAL-COL-ROW — closed by the per-row total + grand-total cell feature above (user-designated next feature after the `--by` grouping shipped earlier today).

## 2026-05-17

- **Incident resolved:** P1 autopilot-pause-policy regression — agent stopped after clean `TRANSITION: F8` emission instead of chaining to verify-auto in Autopilot Mode 3, recurring at the next AUTO transition in the same session; mitigated by adding an `## Orchestrator Pause Policy (cheat-sheet)` block to 8 feature SKILL.md files (spec, research, plan, build, verify-auto, verify-self, verify-human, verify-codify) with per-skill rows from the canonical pause-policy table plus a load-bearing `Hard rule for AUTO exits` imperative requiring the orchestrator to invoke the next `Skill` immediately when the emitted transition is AUTO; reproduction abandoned (Claude `--print` family runs an opaque internal agentic loop with no programmatic pause-decision surface — logged as SURFACE-2026-05-17-CLAUDE-PRINT-AGENTIC-LOOP-SUPPRESSES-PAUSE-DECISION), so codify took Path B with `tests/check-structure.sh` Phase 9 (24 assertions: heading + imperative anchor + 4-mode table row per file) as the structural pure-prose contract; negative-control verified by stripping the block from feature-build/SKILL.md → 3 FAILs, restore → 94/94 PASS.

## 2026-05-16

- **Feature shipped:** Session-slice capture tooling (salvaged from the abandoned session-replay-harness feature) — `tools/capture-session-slice.sh` extracts a slice of a Claude Code `.jsonl` session log, rewrites `sessionId` to a fresh uuid, applies Tier-1 redaction across 13 token patterns (Anthropic / OpenAI / GitHub PAT / AWS / Stripe / Google / SendGrid / Slack / JWT / Facebook / private-key BEGIN), emits a side-by-side diff sidecar, and prompts the human for Tier-2 audit before commit; `tests/sessions/` introduced with the first captured slice (2026-05-16 autopilot F8 pause) plus `AUDIT-LOG.md` convention and Tier-2 reviewer checklist; `tests/check-structure.sh` Phase 8 adds 7 regression-guard checks on the capture tool's contract.
- **Feature abandoned:** Session-replay-harness (single-shot variant) — single-shot `claude --resume`-based replay proven inadequate for reproducing the autopilot-pause-policy bug class (3/3 PASS on current buggy codebase even with system_prompt_extra stripped), per Phase 2 P2.5 pivot-point analysis in `workflow/archive/session-replay-harness.md`; infrastructure salvaged (see preceding entry), runner code path and scenario S26 reverted; multi-turn replay extension filed as `SURFACE-2026-05-16-MULTI-TURN-REPLAY-HARNESS` (high priority) as the immediate next feature.
- **Backlog resolved:** SURFACE-2026-05-11-PER-PHASE-CHAINING-SCENARIO-COVERAGE (partial) — S24 (Mode 3 build→verify-auto chain) and S25 (Mode 3 verify-auto→verify-self chain) added to `tests/scenarios/session.yaml` during the incident-autopilot-pause-policy-recheck reproduce phase; closes the Mode-3-equivalent gap for the build→verify-auto and verify-auto→verify-self chain points (Mode 2 was already covered by S21). Other per-phase chain points still uncovered — the deeper coverage answer is the multi-turn replay feature.

## 2026-05-14

- **Feature shipped:** `debug-*` skill category introduced as a new namespace for agent-pulled sidebar techniques that workflow states (`feature-build`, `incident-investigate`, `task-act`) can invoke when standard debugging stalls; first member `debug-bisect-known-good` codifies the known-good-bisection technique with two conjunctive gates (known-good sibling present AND ≥3 failed straight-line attempts) enforced at SKILL entry, returns to the caller without consuming a workflow transition ID.
- **Task closed:** Default drive mode in `/session-start` flipped from Orchestrated (Mode 2) to Autopilot (Mode 3) — Enter / blank / "yes" now maps to Mode 3 in both `skills/session-start/SKILL.md` and `docs/product/transitions.md`; the stale 3-mode prompt example in transitions.md was refreshed to the live 4-mode menu in the same pass.
- **Backlog resolved:** SURFACE-2026-05-13-DEFAULT-DRIVE-MODE-AUTOPILOT — closed by the default-flip task above; no test scenarios needed updating since S10's substring check on "Orchestrated" still holds and S22 didn't reference the default.
- **Feature shipped:** Entry-skill product-context loading — 6 entry-point skills (`task-plan`, `feature-spec`, `feature-plan`, `feature-reproduce`, `incident-report`, `product-vision`) now consult `docs/product/*.md` per a load-discipline convention defined in `CLAUDE.snippet.md` (pointer-default for all, eager-read for spec+plan, conditional-read for task-plan+incident-report on trigger phrases, 300-line size guard, no `context.md` anywhere), with per-skill `## Step 0: Available product context` sections and 8 new `grep_check` assertions in `tests/check-structure.sh` guarding the contract.
- **Backlog resolved:** SURFACE-2026-05-11-ENTRYPOINT-SKILLS-LOAD-PRODUCT-CONTEXT — closed by the entry-skill product-context loading feature above; canonical mapping landed in `CLAUDE.snippet.md`, cross-level note in `docs/product/transitions.md`, per-skill Step 0 sections in all 6 entry-point SKILL.md files, regression-gated by 8 `grep_check` assertions in `tests/check-structure.sh`.
- **Backlog resolved:** SURFACE-2026-05-13-SETTINGS-FIXTURE-EFFORTLEVEL-DRIFT — closed as side-effect of the entry-skill product-context loading feature; root cause was an inadvertent drop of `effortLevel` from `~/.claude/settings.json` during effort-mode switching, fixed by re-adding `"effortLevel": "xhigh"` to live settings (the fixture asserts the user's actual default).

## 2026-05-13

- **Feature shipped:** Three-layer defense against the finalize-before-ship order-flip bug — `Unvisited:` field tightened to ordered/sequence-of-execution semantics, `agents/feature-workflow/AGENTS.md` pins the post-verify-codify ship→finalize order, `skills/feature-finalize/SKILL.md` gains a §0 precondition guard that refuses to run when ship has not happened, and `skills/feature-verify-codify/SKILL.md` F16 prose forbids enumerating downstream steps.
- **Backlog resolved:** SURFACE-2026-05-06-FINALIZE-BEFORE-SHIP-ORDER-FLIP — closed by the three-layer defense above; regression-gated by new scenario `F16-order-flip` in `tests/scenarios/feature.yaml` using the reproduction fixture from the bug's confirmed reproduction.

## 2026-05-12

- **Feature shipped:** Per-project `CHANGELOG.md` auto-populated by terminal-close skills — `feature-finalize`, `task-close`, `incident-resolve`, and `product-finalize` now append one-line entries on close, per the `## CHANGELOG.md convention` section of `CLAUDE.snippet.md`.
- **Backlog resolved:** SURFACE-2026-05-10-FINALIZE-RETROSPECT-LOST-IN-GIT-MV — closed by per-project-changelog feature; all four closing SKILLs now document the append-before-`git mv` operational sequence.
- **Feature shipped:** `/session-start` now surfaces the top-3 open backlog items as candidate work when no paused session, active WIP, in-progress product doc, or `{{args}}` is present — ranked by priority tier then SURFACE date descending, with a "more backlog" affordance and SURFACE-ID-anchored numbering to defend against sub-list reindexing bugs.
- **Backlog resolved:** SURFACE-2026-05-11-SESSION-START-SUGGEST-FROM-BACKLOG — closed by the session-start backlog-surfacing feature; step 1 of `skills/session-start/SKILL.md` now reads `workflow/backlog.md` when no other active work is found, and step 2 resolves backlog references back to the matching entry.

## 2026-05-11

- **Backlog resolved:** SURFACE-2026-05-11-ORCHESTRATED-PAUSES-BETWEEN-PER-PHASE-STEPS — fixed via incident workflow: added `### Emit Transition` sections to all 5 per-phase feature SKILLs so the orchestrator has a canonical `TRANSITION: <id>` signal instead of falling back to "Run /x" prose; added anti-example to `session-start/SKILL.md`; regression-gated by new scenario `S21`.

## 2026-05-10

- **Backlog resolved:** SURFACE-2026-05-08-INCIDENT-CODIFY-EQUIVALENT — implemented `incident-codify` skill between mitigate and resolve, with transitions I17–I20 and speed-aware paths (reuse-reproduce, write-from-scratch, defer); wired across incident-workflow AGENTS.md, transitions.md, three existing SKILLs, and new test scenarios.

## 2026-05-09

- **Backlog resolved:** SURFACE-2026-05-06-FEATURE-WORKFLOW-MISSING-REPRO-STEP — implemented new `feature-reproduce` and `incident-reproduce` skills with red-green discipline; feature workflow gained F31–F35, incident workflow gained I13–I16, session-start gained S18 routing for bug-shape language.

## 2026-05-08

- **Backlog resolved:** SURFACE-2026-05-08-SETTINGS-JSON-ALLOWLIST-CRUFT — deleted four token-hardcoded GET allowlist entries (getUpdates x3, getWebhookInfo); kept the generic POST pattern as fallback.

## 2026-05-06

- **Backlog resolved:** SURFACE-2026-05-06-S9-S11-S14-DUAL-IDENTITY — added `transition_id_any` support to `tests/lib/verify.sh`; S9/S11/S12/S13/S14 updated to use the union form, S9 now PASSes via S9|F19.
- **Backlog resolved:** SURFACE-2026-05-06-S10-S13-ROUTING-OVERRIDES-DRIVE-MODE — applied `transition_id_any: [S10, S3]` and `[S13, F8]` to the affected scenarios; residual haiku-noise SOFT_PASS shape remains.
- **Backlog resolved:** SURFACE-2026-05-05-D2 — added `not_contains_strict` opt-in to `tests/lib/verify.sh`; strict mode applied to S12 and S14, surfaced a real S12 prose-leak (spun out as a separate open SURFACE).
- **Backlog resolved:** SURFACE-2026-05-05-HIDDEN-FAIL-F4 — tagged scenario `model: sonnet`; PASSes via `tests/run-all.sh` sonnet pass.
- **Backlog resolved:** SURFACE-2026-05-05-HIDDEN-FAIL-S3 — added Valid transitions section to `session-start/SKILL.md` and tagged S3 `model: sonnet`; sonnet PASSes consistently.
- **Backlog resolved:** SURFACE-2026-05-05-HIDDEN-FAIL-S6 — added Valid transitions section to `session-resume/SKILL.md`; S6 PASSes on haiku.
- **Backlog resolved:** SURFACE-2026-05-05-F22-FLAKY-REGRESSED-TO-FAIL — tagged scenario `model: sonnet`; PASSes via `tests/run-all.sh` sonnet pass.
- **Backlog resolved:** SURFACE-2026-05-05-HIDDEN-FAIL-S10 — applied `transition_id_any: [S10, S3]` (see ROUTING-OVERRIDES-DRIVE-MODE).
- **Backlog resolved:** SURFACE-2026-05-05-HIDDEN-FAIL-S13 — applied `transition_id_any: [S13, F8]` (see ROUTING-OVERRIDES-DRIVE-MODE).
